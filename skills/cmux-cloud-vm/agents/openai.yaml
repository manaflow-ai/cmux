interface:
  display_name: "cmux Cloud Machines"
  short_description: "Drive cmux Cloud VMs safely from the CLI"
  default_prompt: "Use $cmux-cloud-vm to route work, run detached agents, transfer files, inspect terminals, and publish protected or public VM URLs; read `cmux vm <verb> --help` before using a new flag."
