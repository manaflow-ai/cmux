/// Where the file-preview PDF viewport anchors when capturing/restoring scroll position:
/// the vertical center of the clip view, or its leading (top) edge.
public enum FilePreviewPDFViewportAnchor {
    case center
    case top
}
