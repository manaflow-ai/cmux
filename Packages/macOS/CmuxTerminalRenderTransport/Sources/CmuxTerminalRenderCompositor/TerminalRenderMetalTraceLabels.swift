/// Stable process-scoped labels used by Metal System Trace acceptance evidence.
enum TerminalRenderMetalTraceLabels {
    static let hostCommandBuffer = "cmux host compositor: one IOSurface blit"
    static let hostBlitEncoder = "cmux host compositor: no Ghostty rendering"
}
