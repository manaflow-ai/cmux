# CmuxSurfaceSelection

`CmuxSurfaceSelection` contains the UI-independent selection event contract and
its cancellation-aware coalescing publisher. AppKit/WebKit adapters and the
cmux event-bus integration stay in the application target.
