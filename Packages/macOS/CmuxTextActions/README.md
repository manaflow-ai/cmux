# CmuxTextActions

Foundation-only value types behind `type: "text"` config actions (snippets):
the validated text payload with its delivery plan, and the category-grouped
model that backs the terminal right-click Snippets submenu. No AppKit, no
app-module dependencies, so both are unit-testable in isolation.
