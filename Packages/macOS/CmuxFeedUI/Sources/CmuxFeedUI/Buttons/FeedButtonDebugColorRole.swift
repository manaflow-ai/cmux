#if DEBUG
/// DEBUG-only color slot a ``FeedButton`` kind exposes to the style
/// playground: its resting background, hover background, and
/// foreground (label) color.
public enum FeedButtonDebugColorRole: String {
    case background
    case hoverBackground
    case foreground
}
#endif
