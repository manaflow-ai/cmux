#if DEBUG
/// One resolved hex triple (background / hover / foreground) for a
/// single ``FeedButton/Kind`` under a ``FeedButtonDebugPalettePreset``.
struct FeedButtonDebugPalette {
    let background: String
    let hoverBackground: String
    let foreground: String
}
#endif
