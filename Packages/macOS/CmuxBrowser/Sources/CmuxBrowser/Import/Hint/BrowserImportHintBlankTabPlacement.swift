/// Where, if anywhere, the import-data hint renders on a blank browser tab.
public enum BrowserImportHintBlankTabPlacement: Equatable, Sendable {
    case hidden
    case inlineStrip
    case floatingCard
    case toolbarChip
}
