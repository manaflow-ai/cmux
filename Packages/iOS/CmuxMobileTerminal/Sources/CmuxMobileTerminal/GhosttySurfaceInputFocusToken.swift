#if canImport(UIKit)
/// Captures the terminal surface whose hidden input was resigned for a temporary overlay.
public struct GhosttySurfaceInputFocusToken {
    weak var view: GhosttySurfaceView?
}
#endif
