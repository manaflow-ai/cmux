@_exported import GhosttyVt

/// A Swift-facing alias for the underlying libghostty-vt module. Consumers
/// `import GhosttyVT` and use `ghostty_terminal_*` directly. The thin
/// wrapper types live in TerminalEmulator.swift.
public enum GhosttyVTModule {}
