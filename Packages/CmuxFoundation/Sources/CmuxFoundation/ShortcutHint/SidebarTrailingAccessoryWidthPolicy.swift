public import CoreGraphics

/// Fixed widths for sidebar row trailing accessories.
public enum SidebarTrailingAccessoryWidthPolicy {
    /// Width of the row close button.
    public static let closeButtonWidth: CGFloat = 16
}
