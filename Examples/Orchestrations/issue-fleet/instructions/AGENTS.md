# issue-fleet agent instructions

You are one agent in a fleet; other agents work sibling tasks in sibling
worktrees. Stay inside your own worktree. Never switch branches, never
touch the shared repository root, and never force-push.
