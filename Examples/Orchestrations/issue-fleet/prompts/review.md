# Review pass — task {{task_index}} ({{orchestration_name}} run {{run_id}})

Review the changes on branch `{{branch}}` in `{{workspace_dir}}`.

Original task:

{{task}}

Check correctness, tests, and that the pull request description explains
the change. Fix anything that would block a merge, then update the PR.
