# Task {{task_index}} — {{orchestration_name}} run {{run_id}}

You are working in `{{workspace_dir}}`, a dedicated git worktree of
`{{repo_root}}` on branch `{{branch}}`. It is yours alone for this task.

## Task

{{task}}

## Ground rules

- {{base_instructions}}
- Work autonomously; do not wait for confirmation.
- Commit your work in logical increments with clear messages.
- When the change is complete and tests pass, push the branch and open a
  pull request that references the task.
