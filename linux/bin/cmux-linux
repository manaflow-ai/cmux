#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PREFIX=$(CDPATH= cd -- "$SCRIPT_DIR/.." && pwd)

export PYTHONPATH="$PREFIX/lib${PYTHONPATH:+:$PYTHONPATH}"

PYTHON_BIN="${CMUX_PYTHON:-python3}"
if ! "$PYTHON_BIN" -c 'import gi' >/dev/null 2>&1 \
    && [ -x /usr/bin/python3 ] \
    && /usr/bin/python3 -c 'import gi' >/dev/null 2>&1; then
    PYTHON_BIN=/usr/bin/python3
fi

exec "$PYTHON_BIN" -m cmux_linux "$@"
