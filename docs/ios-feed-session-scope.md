# iOS Feed: session scope and decisions

This is the living scope record for the Feed work continued in this session. It includes the existing feature carried forward, subsequent user requests, implementation decisions, evidence, and unfinished work. Code being present does not mean the feature has passed a real phone check.

- Updated: 2026-09-24. Markdown and multi-question handling are implemented; final visual and device verification are recorded separately below.
- Branch: `task-ios-feed-tab`.
- Initial feature inventory snapshot: `c8ec337d105`. Later changes are recorded below.
- [Pull request 10218](https://github.com/manaflow-ai/cmux/pull/10218), author: Abdulaziz Albahar (`azooz2003-bit`).
- Current development tag: `fmdblk` at runtime source `20b38a26d7dfa8aa77541b8130ed2fc88ccfb94e`. Mac, Aziz’s iPhone, and the existing task simulator are installed, authenticated as aziz@manaflow.ai, and connected. The phone passed a saved-auth reconnect in 7,088 ms; the simulator passed its connection gate in 26,618 ms. Hosted Markdown and spacing tests pass. Feed spacing was also inspected on the nearby iOS 27 simulator using the production preview fixture.
- Original workspace: `workspace:38`, pane `pane:53`, surface `surface:117`.

## Changing scope

Refer to stable IDs below, for example: “remove F04,” “change D03 to allow another reply,” or “add support for provider X.” Editing this file directly also works. A requested removal changes the scope first; it does not mean its code has already been removed.

After each scope or implementation change, update the affected rows, decisions, evidence, and change log. Keep IDs stable. Mark removed or deferred items instead of deleting their history. Record user requirements separately from engineering choices; changing the latter does not imply the user approved a new product requirement.

**Status:** Built = implementation exists; Partial = part of the requested outcome remains; Pending = unfinished verification or delivery. Evidence is listed separately. All features remain in scope unless explicitly marked otherwise. There are no user-requested removals or deferrals recorded yet.

## Capabilities

### Feed foundation carried forward

These capabilities were already on this feature branch when this session continued. They are part of the current product scope, but their original design choices are not treated as newly approved user requirements.

| ID | Capability and current behavior | Status | Evidence / remaining work |
| --- | --- | --- | --- |
| F01 | Dedicated iOS Feed tab with a full-width, newest-first timeline across paired Macs; separate Notifications tab. | Built | Ordering and per-Mac state tests pass. Latest phone presentation remains pending. |
| F02 | Agent identity, relative time, quoted user prompt, assistant output, plans, stop reasons, and failed tool results. Truncated messages offer inline “… See more”, opening a selectable reading sheet backed by paged Mac reads. | Built | Full-text row and sheet verified in hosted iPhone UI test V30; other row kinds retain their earlier coverage. |
| F03 | Reduce timeline noise: omit routine tool use, successful tool results, and standalone user prompts. Keep prompts as context; unwrap encoded plan envelopes into readable content. | Built | Source and branch history; mixed-history visual check pending. |
| F04 | Permission actions: Allow once, Always, Deny, plus All tools and Bypass in an overflow menu. | Built | Uses the existing Mac decision handler. Provider support depends on its native integration; terminal reply tests do not prove permission support. |
| F05 | Plan actions: approve with the supplied default mode, choose another available mode, deny, or send revision feedback. | Built | Shared Mac decision path; complete visual and provider behavior checks pending. |
| F06 | Question options with explicit Send, accumulated multiple selection, plus Other for free text. | Built | Source inventory; complete question interaction checks pending. |
| F07 | All Activity / Needs Input toolbar filter, tab count, and leading swipe to mark Done or Needs Input. | Built | Badge and local triage state tests pass. Triage changes attention state without answering a request. |
| F08 | Sheet composer with quoted parent message, draft, Cancel, and Reply/Send. Used for terminal replies, Other answers, and plan revisions. Keyboard belongs to the sheet. | Built | Source inventory; keyboard, dismissal, multiline entry, and failure recovery need visual verification. |
| F09 | Live updates and pull to refresh; separate snapshots per Mac, stale revision rejection, coalesced refreshes, and reset/removal handling. | Built | Focused state tests pass. Real reconnect and multiple-Mac checks remain pending. |
| F10 | Capability-gated Feed support, connection/update/loading/empty states, bounded decoding, and tolerance of malformed rows. | Built | State tests cover malformed rows. A newer host advertises `feed.v1`; older hosts cannot provide this feature. |
| F11 | Shared permission/question/plan reply handlers with Mac-side pairing authorization. | Built | Uses `FeedCoordinator.deliverReply`; legacy workspace-scoped tickets must not authorize account-wide decisions. Complete hosted authorization checks remain pending. |
| F12 | English and Japanese Feed strings, accessibility identifiers, and consistent decision controls. | Built | Earlier catalog validation recorded. Current localization and accessibility presentation still need a final audit. |
| F34 | Feed output previews render headings, lists, quotes, code, and inline Markdown. Prompts, option labels, descriptions, quoted context, and replies render inline Markdown. Full-text reading uses the shared document Markdown renderer, with the existing inline See more affordance retained. | Built; hosted UI verified | Foundation semantic block/inline intents drive native preview formatting; full text uses the shared document renderer. V41 verifies both surfaces at db0343155f3, including inline expansion and short-row absence. Package unit regressions are added but have not yet run in an iOS test host. |

### User-requested fixes and delivery

| ID | Requested outcome and implementation | Status | Evidence / remaining work |
| --- | --- | --- | --- |
| F13 | Reply actually submits the prompt. Paste literal text, then send the provider-appropriate submit key through the shared Mac terminal operation. | Built | Real terminal checks and five actual provider editors pass for single-line and multiline text. Provider/model acknowledgement is not returned to the app; see D04 and G02. |
| F14 | A small, conventional reply action replaces the bulky filled button. User then said the button still looked off; it is now a quiet secondary-colored outline arrow and footnote Reply label at text scale, with blue reserved for links. The 44-point target comes from an expanded hit shape, so the row adds no extra height. | Built | `1c84fdd2323` installed as `fmdblk` on the iPhone and task simulator. Live simulator Feed screenshot shows the new control; a tap 9 points above the label opened the composer. User acceptance pending. |
| F37 | iMessage-style quotes. User messages are sent bubbles: a quoted prompt is trailing-aligned, accent-tinted, and has a right tail (user correction: quotes point to the user's message). Agent text is a gray leading bubble. A recorded reply shows the answered agent text as a gray leading quote, then the user's reply as a filled accent bubble on the right. CMUX Labs > Feed Bubble Quotes switches between these and the original leading-bar quotes. | Built | `905856840232` on the task simulator (right-pointing quotes seen live; the Labs switch flipped both ways earlier on `d6db143f32f`) and on Aziz's iPhone (auth gate PASS). The filled reply bubble is still unverified because no live row has a recorded reply. |
| F15 | Reply transitions through a brief spinner with Sending, then a checkmark with Replied only after successful terminal submission. Show the user's reply under the referenced row. | Built | Failure, repeat submission, and refresh state tests pass. Animation and failure recovery need a real UI check. |
| F16 | Avoid duplicate Stopped rows for the same event while retaining any recorded reply. | Built | Duplicate-stop regression test passes. Exact screenshot scenario and rapid separate turns remain to verify; current matching is a time-based heuristic. |
| F17 | Send to the row's owning Mac, workspace, and terminal, including a secondary Mac. An unavailable owner must not redirect to a different instance. | Built | Secondary-owner and offline tagged-owner tests pass. End-to-end phone routing remains pending. |
| F18 | Support other coding providers, including OpenCode, Pi, Cursor, Grok, and Google Gemini CLI, with the same submission path. Preserve provider identity through ingestion. | Partial | Five real editor checks pass; seven-provider routing/identity coverage passes. Broader feature parity and live authenticated provider runs are not established. |
| F19 | Explain or fix why older completed rows have no Reply action. | Partial | Reply currently requires a stop row with both workspace and terminal IDs. Missing historical targets still suppress it; the original older-row screenshot has not been resolved end to end. |
| F20 | Install the current version on Mac and Aziz’s physical iPhone, ready to open Feed and try it. | Built; delivered | Matching `fmdblk` apps at `20b38a26d7d` are installed and authenticated on Mac, phone, and the existing nearby simulator. The phone passed a credential-free reconnect. Live physical-phone Feed interaction remains a separate dogfood check (V46). |
| F21 | Verify the final iOS experience on an isolated simulator and the physical phone. | Partial | Hosted iPhone full-text UI test passes with screenshots and recording. Physical-phone reader, reply animation, keyboard, and paired reply verification remain open. |
| F22 | Keep repeatable regressions for submission, routing, stop deduplication, and provider compatibility. | Partial | Focused tests and manual provider harness pass. Hosted application tests and the new provider workflow still need successful complete runs. |
| F23 | Maintain this Markdown inventory so the user can add, remove, or change scope. | Built | This file, with stable feature/decision/gap IDs and a change log. Maintenance continues with the session. |
| F24 | Refresh rejected personal development credentials through `scripts/setup-team-dev.sh --refresh`, preserving the agent profile and requiring verification before replacement. | Built | Three regression tests pass for successful refresh, rejected credentials, and unavailable sign-in service. Optional password recovery remains available. This session instead used a genuine development server session, so the user did not need to enter credentials or run a command. |
| F25 | Pull the latest `main` into this branch and reinstall matching Mac/iPhone apps with the Feed work preserved. | Partial | Continued from remote feature head `e6b60c29617`, which includes earlier main merges. Main has advanced again and this PR currently has conflicts; reconciliation remains required before merge. |
| F26 | Read the full retained message from a Feed row, with selectable text, scrolling, Close, loading, and retry. New completion hooks retain full text and line breaks across providers. | Built; hosted verification passes | V28–V30 cover pages, provider hooks, and the real reader UI. Reads 16 KiB pages from the exact owning Mac. Physical-phone delivery remains separate; text discarded by older hooks cannot be recovered. |
| F27 | Explain Feed producers, data flow, storage, reconnect/restart behavior, and shipping gaps in an interactive HTML walkthrough. | Built | [Data-flow walkthrough](feed-data-flow.html), pinned to the inspected runtime revision. Browser interactions checked. |
| F28 | Replace View full text with an inline “… See more” control only when additional text exists beyond the visible preview. | Built | TextKit measures the preview with an inline ellipsis and button; the Mac supplies truncation metadata. Hosted UI verification passed, with short-row absence and inline long-row expansion. Latest Mac, phone, and nearby simulator are installed and connected (V46). |
| F29 | Preserve the computer picker, computer list, and settings access across every primary tab, including Feed. | Built | Shared root toolbar is mounted in Feed. Computer selection filters Feed rows and its count using exact Mac/tag identity. Hosted UI checks passed (V36). Latest Mac, phone, and nearby simulator are installed and connected (V46). |
| F30 | Search the active tab, including Feed; hide search for future tabs that do not support it. | Built | Feed has independent query state, a Search Feed placeholder, and loaded-preview/context filtering. Tabs without a search scope hide search. Hosted UI checks passed (V36). Latest Mac, phone, and nearby simulator are installed and connected (V46). |
| F31 | Open an event’s workspace or tab from its row context menu, using one **Open** action and its exact owning Mac. | Built | The single Open action chooses the tab when a live surface exists and otherwise opens the workspace. The store switches to the exact Mac and validates the target. Missing destinations show an alert. UI menu and destination store tests passed (V35–V36). |
| F32 | Notifications also appear in the Feed timeline, with stable notification IDs, workspace/surface provenance, and full-text reading. | Built | `feed.list` merges the durable notification history with Workstream rows and emits `feed.changed` for either source. Current Mac/iOS builds pass (V42–V43); notification fixture verification remains pending. |
| F33 | Persist every Feed event mutation, including terminal reply text and its exact event identity. | Built | Workstream JSONL replays the latest version per event; resolution, expiry, and terminal replies append durable versions. Replies carry `feed_event_id` equal to the row UUID. Current Mac/iOS builds pass; complete hosted package and restart verification remains pending. |
| F35 | Multiple Claude questions in one round use swipeable pages, progress indicators, Previous/Next controls, per-question drafts, and one final submission after every question has an answer. | Built | Ordered human-readable answer composition tests added. Visual and live Claude submission checks remain pending. |
| F36 | Keep the first Feed row close beneath the shared toolbar, without an empty large-title area. | Built; hosted UI verified | The shared inline navigation mode removes the unused title region. V45 catches the original 76-point gap and verifies the corrected gap is within 32 points, with inspected screenshots. |
| F38 | Tapping a Feed row opens its workspace or tab, pushed onto the Feed tab's own navigation stack (deep-link origin `agentFeed`), so Back returns to the Feed as it does in Notifications. Buttons, links, and See more keep their taps; VoiceOver gets an Open action; rows carry `MobileAgentFeedRow-<id>`. | Built | `cb9046582b3`. Store test asserts the `agentFeed` origin. The tagged build is not yet installed because the fleet's only iOS Mac is offline. |
| F39 | When the Feed cannot refresh but has cached rows, a banner says it is offline or that a Mac needs an update. The empty unavailable state has Try Again. | Built | `11cdce5a257`. Not yet seen on a device. |
| F40 | CMUX Labs > Feed Replaces Notifications hides the Notifications tab (iPhone) and its iPad sidebar destination, moving a selected Notifications tab to the Feed. DEBUG only; production keeps the tab. | Built | `809906c8660`. This is a dogfood switch; removing the tab for everyone is D31. |
| F41 | A failed terminal reply leaves a red note on its row with Try Again, which reopens the composer with the text. `notSent` (owning Mac offline or request never built) says the reply was not sent; `unconfirmed` (the request reached the Mac, or its fate is unknown) says delivery could not be confirmed and offers Open Terminal, so a retry is never automatic and the text is not typed twice unseen. | Built | `aaaa967e70d`. Store tests assert both states. Addresses the visible-recovery part of G05. |
| F42 | Markdown links in Feed previews open when tapped, through SwiftUI `openURL`. Other taps pass through to the row. | Built | `182acd77acb`; failing test first in `04e66ed7a5c`. The test has not run in an iOS host yet. |
| F43 | Finished-turn rows drop the "finished a turn" verb, so the author line reads agent · workspace. Every row shows its workspace (title, else cwd folder). Settings > Display > Show Tab in Feed appends the tab as workspace › tab. | Built | `42b622439c9`. User request. Not yet seen on a device. |
| F44 | Workstream log appends form one ordered chain, and `flushPersistence()` awaits it, so a restart replays mutations in the order they happened. | Built | `a29ad0693df`. 16 WorkstreamStore package tests pass locally. |
| F45 | Feed usage events mirror the Notifications tab: opened, closed, filter changed, item opened, reply succeeded, and reply failed with its delivery state (diagnostic taxonomy 667 to 672). Counts only, never content. | Built | `d24aaf275f3`. CMUXMobileCore presentation tests pass locally. |

## Decisions

| ID | Decision | Origin and consequences |
| --- | --- | --- |
| D01 | Keep agent activity and response controls in Feed; retain Notifications as a separate surface. | Carried-forward design. This session does not imply a push-infrastructure redesign. |
| D02 | Use a compact message-style reply action and a composer quoting the parent message. | User requested familiar reply conventions and rejected the large button. The particular arrow, label, and sheet layout are implementation choices awaiting visual acceptance. |
| D03 | A row permits one successful terminal reply. Disable its reply action while sending and after success; retain its reply marker across refreshes and restarts. | Engineering choice to prevent duplicate submission. The Mac's Workstream JSONL is authoritative; the phone rehydrates the marker from `feed.list`, including after the phone process restarts. |
| D04 | Mark Replied only when `mobile.terminal.paste` returns `submitted: true`. | Engineering implementation of the user's success requirement. Today this confirms that the terminal accepted or queued the submit key, not that an agent or model acknowledged the message. Closing this semantic gap is G02. |
| D05 | Share paste/submit behavior between iOS Feed and the Mac Feed reply path. Preserve literal text and internal newlines. | Principled fix: one operation owns paste, submit, and its result. Avoid divergent newline-based submission paths. |
| D06 | Choose the submit key from the active agent process identity, with fallback to existing detection when current identity is absent. Claude multiline input uses Ctrl+Enter; other current cases use Return. | Principled ownership choice: switching providers in a terminal must not retain the original provider's submission rules. Hosted submit-key tests still need to execute successfully. |
| D07 | Separate paste and Enter by a 150 ms asynchronous wait. Reject concurrent submissions to one terminal and recheck its identity and generation before Enter. | The target checks are principled lifecycle protection. The delay is a pragmatic compatibility workaround for provider paste handling, not a readiness acknowledgement; provider changes or severe load remain risks. |
| D08 | Keep Sending visible for an additional 350 ms after a successful response, then record the reply. | Presentation choice to make the transition legible. User asked for brief progress; this exact duration is not a user constraint. |
| D09 | Collapse stop rows within two seconds when Mac device, workstream, provider, and stop reason match. Preserve a reply marker from either row. | Heuristic fix for the reported duplicate. The key omits the Mac instance tag and conversation text; genuine rapid turns or sibling instances can collide. See G04. |
| D10 | Route using the row's owning connection; fail if that owner is unavailable. | Principled routing decision to prevent sending into the selected but unrelated Mac or terminal. |
| D11 | Swiping Done changes local attention state only. Actual permission/question/plan controls follow the authoritative pending state. | Carried-forward design. Marking a request Done must not silently approve or deny it. |
| D12 | Preserve each provider's identity, including an explicit Grok source. Use the shared terminal path wherever possible. | User requested multiple-provider verification. This does not promise identical permission, plan, or question support for every provider. |
| D13 | “Installed” is insufficient for delivery: Mac and phone need the same tag, matching personal account, trusted pairing, usable connection, and Feed open. | User requested immediate testing; repository dogfood contract supplies these acceptance conditions. Physical phone uses the personal profile; isolated simulator uses the agent profile. |
| D14 | Keep the existing pull request open until verification and user dogfood are complete. Merge requires an explicit merge instruction. | Session constraint. No merge authorization is recorded. |
| D15 | Match the installed `xfd2` Mac's API, broker, and sign-in addresses to the phone's existing staging configuration. Keep the personal authentication profile. | Operational repair on 2026-09-15. Both servers responded before repair, so server availability did not explain the rejected password. Future reloads must preserve a matching server configuration. |
| D16 | Use a genuine 24-hour developer session for the existing personal account when the saved password is rejected; preserve the phone’s valid session and refresh its pairing ticket. | User explicitly requested the developer command and no user-run setup. Account identity was verified through Stack and both apps passed the same-account RPC gate. This does not change the password; the temporary session will expire. |
| D17 | Preserve main’s iPhone/iPad navigation structure while adding Feed to each supported destination control. | Merge resolution. Native tabs remain on compact layouts; the existing split-sidebar destination control gains Feed. Existing Feed English/Japanese strings are retained alongside main’s newer translations. |
| D18 | Await the shared terminal submission operation in main’s new macOS notification and relayed-phone reply callers before recording delivery. | Semantic merge repair: the Feed branch made paste/submit asynchronous, so newly merged callers must propagate that contract. Existing partial-paste handling remains unchanged to avoid typing a reply twice. |
| D19 | Keep Feed previews bounded; fetch retained message text only when opened. Never show a partial download as a complete message. | Engineering choice for F26; protects scrolling and transport limits. |
| D20 | Full-text reads use the same authenticated account boundary as feed.list and retain exact Mac tag ownership. Reads do not open paths or send terminal input; remote SSH relay remains denied. | Engineering choice for F26. |
| D21 | The reader displays retained content through the shared selectable Markdown renderer. Historical summaries and text omitted by a provider cannot be reconstructed. Reads exceeding 8 MiB fail explicitly. | Known limit of F26; no backfill promised. |
| D22 | Keep the architecture walkthrough source-backed, with proposed shipping requirements separate from accepted scope. | User requested understanding of producers, transport, and persistence before shipping. |
| D23 | Measure rendered text and reserve space for “… See more”; also honor explicit Mac truncation metadata. | User rejected an always-visible separate full-text button. Native TextKit handles width/font wrapping and composed Unicode characters. |
| D24 | Share computer controls and search coordination across primary tabs. Feed search covers loaded previews/context, not unfetched full messages or disk history. | User requested consistent controls and active-tab search. The search extent is an implementation limit. |
| D25 | Context actions validate the event’s exact Mac/tag and current workspace/tab after any connection switch, then open Workspaces. | User requested workspace/tab access from row menus. Missing targets show a localized error and cannot redirect to another tab. |
| D26 | Treat notification history as a second Feed input, while keeping the Notifications tab and its read state intact. | Notifications serve more use cases than agent prompts. Feed rows use the notification UUID as `event_id`, preserve workspace/surface routing, and deduplicate only when the UUID is already represented by a Workstream row. |
| D27 | The event UUID is the reply identity. The phone sends it as `feed_event_id`; the Mac stores the reply on that WorkstreamItem and exposes it again as `reply_text`. | This gives the agent and the UI one concise, durable reference for the exact event that received a reply. |
| D28 | Use native attributed Markdown in bounded row previews and the shared document renderer in the full-text sheet. Truncate the rendered text, preserving attributes and composed Unicode characters. | Engineering choice for F34. Markdown source offsets differ from rendered offsets because formatting markers and link destinations disappear; measuring raw source can misplace See more or crash. |
| D29 | A multi-question round keeps one draft per question and submits one ordered, labeled answer only after all pages are answered. Swiping and Previous/Next change the visible question without discarding drafts. | Engineering choice for F35. Selection alone does not send a partial round. Visual and live Claude verification remain pending. |
| D30 | Feed uses the shared inline navigation-title mode on its root view in both tab and search presentations. | Principled layout correction for F36: the computer picker already occupies the toolbar title position, so no large-title region is needed. Respect native safe areas instead of offsetting the list with negative padding. |

## Provider coverage

The real-editor harness starts actual provider command-line apps with isolated configuration and a local request recorder using test credentials. A pass means the exact submitted prompt reached the provider's outgoing model-request path. It does not test paid inference, real-account authentication, a completed model response, or the iPhone transport/UI.

| Provider | Version in recorded editor verification | Routing and identity tests | Real editor: single-line / multiline |
| --- | --- | --- | --- |
| Claude Code | Not recorded in this harness | Pass | Not run in this harness |
| Codex | Not recorded in this harness | Pass | Not run in this harness |
| Pi | 0.73.1 | Pass | Pass / Pass |
| OpenCode | 1.18.31 | Pass | Pass / Pass |
| Cursor CLI | 2026.09.10-fd3934a | Pass | Pass / Pass |
| Grok Build | 1.0.30 | Pass | Pass / Pass |
| Google Gemini CLI | 0.59.0 | Pass | Pass / Pass |

Pi's multiline case passed on rerun after a transient terminal screen-read failure. Cursor's recorder handles its HTTP/2 request path. “Google” is interpreted here as Gemini CLI, and “Cursor” as Cursor CLI; other products and unnamed providers are not yet enumerated coverage.

## Verification and delivery record

| ID | Recorded result | Limit |
| --- | --- | --- |
| V01 | `MobileAgentFeedTerminalReplyTests` and `MobileShellAgentFeedStateTests`: 14 tests pass, including 14 routing argument combinations across seven providers and two prompt shapes. | Package behavior tests, not phone UI evidence. |
| V02 | `ControlCommandCoordinatorMobileHostTests`: 8 tests pass. | Shared control-operation coverage. |
| V03 | `WorkstreamStoreTests`: 13 tests pass, including seven provider identity cases. | Ingestion coverage, not every provider's permission protocol. |
| V04 | Real terminal paste/submit checks pass for plain text, multiline Unicode, and literal punctuation. Five-provider editor checks pass as listed above. | Recorded tagged runtime checks; temporary raw evidence is not a durable published artifact. |
| V05 | [macOS build run](https://github.com/manaflow-ai/cmux/actions/runs/34922165927) succeeded. Tag `xfd2` was installed locally. | Includes the runtime fixes, predates later harness-only changes. The original sign-in blocker was resolved in V15–V16; this build predates the main merge. |
| V06 | Current iOS phone archive and simulator build succeeded; signing/export completed. Simulator `cmux-dev-xfd2` was installed. | Installation alone proves neither UI behavior nor pairing. |
| V07 | Physical iPhone installation was queued on 2026-09-14 because Aziz's device was unreachable. | Queue entry: `~/Library/Application Support/cmux-dev/iphone-install-queue/pending/xfd2`. Phone installation, authentication, pairing, and Feed navigation remain pending. |
| V08 | [Hosted submit-key test run](https://github.com/manaflow-ai/cmux/actions/runs/34922167776) failed before tests executed: `WindowAndDragTests.swift` was missing `customSidebarDataContext`. | No passing result can be inferred for `ComposedPromptSubmitKeyTests`. |
| V09 | Provider workflow added; initial [run](https://github.com/manaflow-ai/cmux/actions/runs/34922456272) failed workflow validation. Environment scoping was corrected in `c8ec337d105`. | A successful complete run after that correction is not yet recorded. |
| V10 | Pull request checks were not all passing at the last observation, including CLA policy and web complexity checks. | Do not describe this as ready to merge. Reassess concrete failures before closeout. |
| V11 | 2026-09-15: local server on port 4377, former remote server on port 4577, and staging all returned 200. Staging sign-in returned 200 and after-sign-in redirected with 307. Phone was reachable and `dev.cmux.ios.xfd2` was installed. | Installation queue now reports `needs-auth`, superseding V07's pending installation state. Mac `auth status` was signed out at that observation; superseded by V15. |
| V12 | 2026-09-15: the native development sign-in endpoint returned HTTP 400 with `EMAIL_PASSWORD_MISMATCH` for the saved personal profile. Installation helper was refreshed and the machine setup check passed. | Setup checks validate profile presence; they do not prove the credentials are accepted. The password-based launcher still rejects this saved pair; V15 records the alternate developer-session repair. |
| V13 | 2026-09-15: three personal-account refresh regressions pass; the same tests fail against the preceding setup script. Shell syntax and help checks pass. The repaired Mac bundle was signed, verified, and relaunched. | No app executable changed. The Mac still reports signed out; neither phone pairing nor Feed interaction is claimed verified. |
| V14 | 2026-09-15: the prescribed `scripts/mobile-dev-launch.sh --tag xfd2 --device --device-id 4A52829D-6427-599F-A166-4058881D2DF4 --ensure-mac --auth-profile personal --credentials-file ~/.secrets/cmuxterm-dev.env` flow ran. It relaunched the exact tagged Mac, then failed its signed-in account gate for `aziz@manaflow.ai`. | This is the dev flow result, not a normal browser sign-in result. This historical failure was resolved for the installed pair using the genuine developer session and persisted phone session in V15–V16. |
| V15 | 2026-09-15: created a genuine developer session for the existing personal account using the configured development Stack server credentials. Mac `auth status` verified `aziz@manaflow.ai` and the expected user/team. | Session is limited to 24 hours. No password was changed, and no tokens were included in logs or this document. |
| V16 | 2026-09-15: fresh phone pairing established usable workspace RPC in 14,097 ms. A second launch without credentials or a ticket reconnected in 765 ms. | Applies to the pre-main-merge installed apps. Secret-free receipts are stored under `artifacts/task-ios-feed-tab/connection-repair/`; the reinstalled apps must pass again. Feed UI is not visually verified by this gate. |
| V17 | 2026-09-15: merged latest main (`88945c86fc3`), resolved six conflicts, parsed affected Swift files, validated the project file, and checked wiring for 942 test files. | Syntax/project checks are not compilation. Remote app builds are in progress. |
| V18 | 2026-09-15: 48 of 49 attachment/setup checks passed. The remaining check expected the older receipt shape; its fixture was updated for main’s installed-bundle evidence and the focused rerun passed (five selected tests). The merged localization catalog validates for nine languages. | No application behavior was changed by the fixture update. |
| V19 | [Refreshed hosted submit-key run](https://github.com/manaflow-ai/cmux/actions/runs/35022290439) found new notification callers using asynchronous terminal paste synchronously. Added an awaited-delivery regression and propagated async through both notification paths. | Compilation blocked test execution. The superseded Mac build and dependent provider run were cancelled; rebuilt evidence is pending. |
| V20 | [iPhone/iPad navigation run](https://github.com/manaflow-ai/cmux/actions/runs/35023839799) compiled both apps but selected zero tests. Corrected the dispatch selector to include both target and class; the next attempt timed out during checkout before testing. | Neither run proves navigation behavior. Fixed the Feed time-label free-function convention violation; unrelated namespace-type lint failures inherited from main remain. |
| V21 | The first two hosted Mac rebuilds exposed main API drift (`idleTimeoutNanoseconds`, then `FeedJumpResolver.parse`); both were fixed. | A third run completed successfully after those fixes. |
| V22 | [Mac rebuild run](https://github.com/manaflow-ai/cmux/actions/runs/35030862663) completed and installed tag `xfd2`; auth status still reports `aziz@manaflow.ai`. The matching iOS archive compiled and exported, and the isolated simulator install succeeded. | The physical iPhone was unavailable, so the signed app is queued with the personal account contract. It will auto-install when the phone reconnects; pairing after replacement remains pending. |
| V23 | [Provider verification](https://github.com/manaflow-ai/cmux/actions/runs/35033730472) was dispatched against the successful Mac build. | Awaiting its complete result. |
| V24 | 2026-09-16: pulled `origin/main` through `5994f5e3332` and merged it in `a496ef635cd`. The conflict was limited to the iOS localization catalog; main transport/onboarding strings and Feed strings were retained. | Focused transport sources parse. Initial fleet preparation stalled; subsequent Mac and phone archive results are recorded below. |
| V25 | [Mac rebuild run](https://github.com/manaflow-ai/cmux/actions/runs/35154347153) succeeded and installed `xfd2`. Fleet phone archive `xfd2-8246312c78c2` succeeded, then local signing/export succeeded. The phone archive records `874c67d21c6`. | Phone install failed because CoreDevice reported Aziz's iPhone unavailable. Signed app is retained under the HQ artifact directory. Its pending queue entry was cleared after the credential rejection was confirmed, preventing automatic replacement of the working Mac session. Installation is not claimed. |
| V26 | The combined archive/simulator attempt failed in the simulator leg because a cached Sentry header changed after its compiled module was created. The subsequent phone-only archive succeeded. | The isolated simulator has the earlier build; the latest simulator build and Feed UI remain unverified. |
| V27 | The user updated the personal environment file. A real development Stack sign-in still returned `EMAIL_PASSWORD_MISMATCH`. Created a genuine 24-hour developer session for the existing personal user, aligned the installed Mac's API/Iroh authorities to the phone's staging configuration, and verified Mac `auth status` plus trusted physical-device ticket readiness. | This is a temporary operational workaround, not a password repair. Automatic installation is paused because the password-based launcher would replace the working Mac session with another rejected sign-in. Phone authentication, persisted reconnect, and Feed interaction must be verified after the phone reconnects. No secret values are recorded here. |
| V28 | [Focused hosted tests](https://github.com/manaflow-ai/cmux/actions/runs/35901044549) pass at `86eabf9ee33`: 3 full-text tests, 8 Feed reply/read tests (including provider argument cases), and account authorization cases. | Covers Unicode pages, full assembly, invalid/changed pages, unavailable owning Mac, and credential scoping. Does not prove a physical-phone connection. |
| V29 | The new completion-preservation regression fails on the preceding installed CLI and passes on newly compiled `fdchk` for Codex, Claude, OpenCode, Pi, Cursor, Grok, and Gemini. | Actual CLI hook ingestion against a private recording socket; no provider/model inference claimed. |
| V30 | [Hosted iPhone UI job](https://github.com/manaflow-ai/cmux/actions/runs/35901754390/job/107320474760) passes `testFeedFullTextReadingAndRetry`. Inspected screenshots show the row action and final paragraph after scrolling; the run also retries and closes successfully. | Uses the production row/sheet with an isolated DEBUG data loader. Full workflow is red because the existing conventions check reports 63 unrelated errors, none in changed full-text files. Physical Mac transport is covered separately. |
| V31 | Mac controller jobs `bfcec05eb4c962c614777965` (`fdchk`) and `b15b4602ed1a57a30d69258c` (`fdtext`) succeeded at the same exact source. `fdtext` is installed, its signature verifies, and `auth status` reports aziz@manaflow.ai. | Downloaded Mac metadata retained localhost despite the requested remote origin. Local installation metadata was aligned with the phone server and local resource paths, then re-signed. Packaging complaint `a895ea28fbd19e9037d9dbc4` records this operational workaround. |
| V32 | Current shared phone tooling matches main; the HQ doctor passes both independent auth profiles, expected phone identity, queue installation, and physical reachability. Signing prerequisites also pass. | The all-in-one wrapper incorrectly compares shared tools with this older app branch. Validated current tools and used controller primitives directly, preserving shared files. |
| V33 | Controller iOS job `d5ac298aed5140768e5bf097` compiled its device archive, then rejected its untagged bundle ID. Diagnostic job `266c3600c2958d6a436a8d9b` retrieved the failure log. Replacement job `5b9c90e31f497d05e093be1d` succeeded using the branch-supported `PRODUCT_BUNDLE_IDENTIFIER` setting. Both device and simulator payloads passed exact source/tag/server validation. | Both jobs use source `86eabf9ee33`. The compatibility recipe is isolated to this task; no shared tools or host access were changed. Local simulator installation is omitted because hosted verification already passed. Phone delivery subsequently passed V34. |
| V34 | Physical `fdtext` installation, personal account sign-in, trusted pairing, and usable RPC gate passed. Independent credential-free relaunch passed in 13,177 ms. | Both apps use source `86eabf9ee33` and aziz@manaflow.ai. This proves persisted connectivity, not physical-phone Feed tapping. |
| V35 | The tests-first UI run failed on the old View full text label. Focused package run [35907999363](https://github.com/manaflow-ai/cmux/actions/runs/35907999363) passed full-text, authorization, reply, search matching, and workspace/tab destination tests. | Invalid/deleted tab is rejected while its surviving workspace can still open. Tests run at 96075b91e36; final 6dd5a1 changes only a DEBUG preview switch. |
| V36 | Hosted iPhone UI job [107343262740](https://github.com/manaflow-ai/cmux/actions/runs/35908621864/job/107343262740) passed at 6dd5a1ad6f4. Verified inline See more, no expansion for Stopped, shared controls, reader retry/final paragraph/Close, context menu/tab action, and Feed search. Screenshots and a context-menu recording frame were inspected. | Production UI with a fixture loader/destination callback. Exact destination selection is separately tested in V35. Search coordinator unit run [35908621406](https://github.com/manaflow-ai/cmux/actions/runs/35908621406) cannot compile an unrelated terminal test (GhosttySurfaceWorkQueue missing). Inherited conventions checks remain red. |
| V37 | Final Mac controller job `7d1a09911b67e13246de3ba7` passed at 6dd5a1ad6f4, with artifact digest c067d0e5ba697ba193af9f79908b85ef12b4c9df1709f3b027862fb6b55bd431 and successful worker cleanup. Installed and verified the local signature; auth status reports aziz@manaflow.ai. | Reused the verified tag’s local server/path configuration because the older branch still bakes localhost into download metadata. Mac source and bundle identity are checked before replacement. Phone packaging passed V38; physical delivery is queued in V39. |
| V38 | Matching iOS job `4a073e45487e0d2888a260bf` compiled the phone archive but failed its simulator build on a stale Iroh precompiled header. Diagnostic job `da2daff42f2bb532b7c244b5` retrieved the failure. Retry `8de0f879291b270b015e9cab` passed both device and simulator builds after isolating compiler output by tag and exact source revision. Its artifact digest is 0b98d1661d9c04a17200bc53b6f15341afd71a8e44c14588e88f8d01212113c8; worker cleanup passed. | Same source 6dd5a1ad6f4. The repair is a task-local packaging recipe; shared tooling and access configuration are untouched. Signed export passed; physical installation/authentication is queued in V39. |
| V39 | Signed phone update 6dd5a1ad6f4 is queued at the stable install queue under fdtext. Verified exact app source/tag, personal profile, aziz@manaflow.ai, and all sign-in/attach/setup gates enabled. The queue worker is running. | CoreDevice still reports Aziz paired but with tunnelState unavailable and DDI services unavailable; last connection was 19:28 UTC. A second device-details query confirmed the state. The phone must reconnect before installation and a new saved-auth reconnect check can complete. No current phone UI or connectivity success is claimed. |
| V40 | Controller Mac job `41f995b7846fe73557f77f0d` succeeded at `3855bf311bd`; tag fqpg3 is installed and auth status reports aziz@manaflow.ai. The same-source phone archive compiled and exported. | Device unavailable. Simulator build initially failed on a stale Iroh compiled header. A subsequent code inspection found the raw/rendered Markdown offset bug, so this version is not the final Markdown handoff. |
| V41 | [Hosted iPhone Markdown UI job](https://github.com/manaflow-ai/cmux/actions/runs/35961301031/job/107510653918) passed at `db0343155f38dd9edaaf24ea35a1c4e3b5e66014`: 1 test, 0 failures, 33.989 seconds. Inspected screenshots show formatted preview headings/bullets/code, inline See more, the formatted reader, and the final paragraph. Retry, Close, Open, and Feed search also passed. | Uses production views with deterministic sample data. It does not establish physical-device transport or live provider submission. The separate conventions check retains 63 existing errors outside Feed, so the full workflow is not green. Durable local evidence is under `cmux-assets/task-ios-feed-tab/markdown/` in HQ and open beside the workspace. |
| V42 | Controller iOS job `3a7d6f2681fc82c068e8e912` passed at db0343155f3, including phone archive, simulator app, exact source/tag/server checks, and worker cleanup. Artifact SHA-256: `a6ef9f2d9137fe643889bd518718aadc2524c7caa08fee6c49b8951f6982add2`. Local signing succeeded. | Stable installation tooling queued fmdblk for Aziz with the personal profile, aziz@manaflow.ai, and all sign-in/attach/setup gates enabled. Earlier Feed queue candidates are absent. Physical installation is not claimed while the device is unavailable. |
| V43 | Controller Mac job `227471db53cda3c37f5ef7c2` passed at the same source with worker cleanup. Artifact SHA-256: `900e6a6718fb1ce860d2e75f5d5cf942b5813dafb6b83cfcd60d3257c30c88a4`. Installed fmdblk, verified its signature and source, and confirmed auth status as aziz@manaflow.ai. | The older branch's package retained localhost and remote resource paths. Trusted local metadata repair set the task server, local paths, and matching staging Iroh authorities before re-signing. HQ publication rejected the unpatched metadata; complaint `c97c8c6d3805169467eb8c2f` records this limitation. The local tag opener resolves successfully. |
| V44 | Updated the existing task-owned simulator `2DBB8CB4-C182-4FF3-B568-6DFF63A27563` without booting another simulator. The agent profile resolved aziz@manaflow.ai; the same-tag Mac account check and usable connection gate passed in 27,435 ms. Foreground inspection confirms dev.cmux.ios.fmdblk, and accessibility shows the live workspace list past login. | The nearby pane is `pane:1000000047`, simulator surface `surface:1000000226`, in the original workspace UUID CABF0F94-33D4-482B-9427-4A5A55C0EF4C. It currently opens on Workspaces; Feed presentation is verified separately in V41. The superseded fqpg3 Mac was closed. |
| V45 | [Hosted spacing screen test](https://github.com/manaflow-ai/cmux/actions/runs/36038179800/job/107763581607) passed at runtime source `20b38a26d7d`: 1 test, 0 failures in 8.291 seconds. It requires the first author label to be below and within 32 points of the toolbar control. The screenshot was inspected and added to the existing evidence tab. | [The same test before the fix](https://github.com/manaflow-ai/cmux/actions/runs/36038175921/job/107763576143) fails with a 76-point gap. The previous Markdown check missed this because it checked content rather than layout. The separate repository style check still has existing failures. The same production Feed preview was also inspected on the existing iOS 27 simulator after installation. No user-facing strings changed; localization catalogs need no update. |
| V46 | Controller Mac job `84d5228b830814e1affc59c2` and iOS job `a3ed1a8b804db475a0220801` succeeded at runtime source `20b38a26d7d`, with cleanup receipts. Installed both apps and updated the existing simulator. Mac auth reports aziz@manaflow.ai; phone personal-profile saved-auth reconnect passed in 7,088 ms; simulator agent-profile connection passed in 26,618 ms. | Two iOS archive attempts exposed a missing Xcode 26.6 platform component. Selecting installed Xcode 26.3 for the job, matching the previous successful package, resolved it without changing global Xcode selection. Mac packaging needed the existing local server/path correction. Legacy phone readiness reports launcher source rather than executable source; installation source is verified from the signed package and successful install. |
| V47 | Local package tests on the merged branch: 16 WorkstreamStore tests, 18 Feed store tests (including new failed-reply and Feed-origin assertions), and 13 CMUXMobileCore presentation tests pass. `testFeedRowTapOpensItsDestination` (`8abf2ec35bf`) is added for the hosted iOS lane. | Package logic only; iOS UI compilation and UI tests are left to PR CI. |

## Gaps and next work

These are outstanding parts of existing scope or limits that affect its acceptance. Proposed extensions are identified explicitly; they are not silently added as requirements.

| ID | Gap | Next action / acceptance condition |
| --- | --- | --- |
| G01 | Resolved for `20b38a26d7d`: Mac, physical iPhone, and existing nearby simulator are installed, authenticated, and connected (V46). | No sign-in or installation action required. Physical-phone Feed interaction and broader provider checks remain separate from installation readiness. |
| G02 | Replied currently reflects terminal key acceptance. | Verify the agent actually starts a turn from the phone path. If the product requires a provider acknowledgement before showing Replied, add that acknowledgement contract; it is not implemented today. |
| G03 | Original older rows without Reply remain unexplained at the product level. | Inspect those rows' target metadata. Recover a valid live target where possible and decide the unavailable-session presentation. Do not send to an unrelated terminal. |
| G04 | Stop deduplication uses a two-second heuristic. | Reproduce the two Stopped rows; prove repeated delivery collapses and distinct rapid turns remain visible. Strengthen event identity if the heuristic conflates them. |
| G05 | Composer dismisses immediately on Send; Feed submission failure is logged without a visible recovery flow in this path. | Exercise owner-offline, paste-accepted/key-failed, and connection-loss cases. Define clear failure feedback and draft/retry behavior that cannot paste the text twice. |
| G06 | No latest visual proof for reply styling, progress, completion, or duplicate removal. | Record the exact flow on an isolated simulator, then check the physical phone. Include multiline input, keyboard dismissal, repeated taps, refresh, and failure behavior. |
| G07 | Provider evidence is narrower than full provider feature parity. | Complete requested submission coverage through the paired phone; keep routing, editor submission, real inference, and permission/question support separately reported. Broader provider parity requires an explicit scope choice. |
| G08 | Application tests and provider workflow have no complete passing final result. | Address the concrete test compilation/workflow failures and run the focused checks on hosted or fleet machines. |
| G09 | Triage overrides are phone-local and intentionally do not persist; durable reply state is authoritative on the Mac event record. | Keep triage as a personal view preference. Verify that a reply survives both app restarts and a second paired phone before release. |
| G10 | Presentation and localization acceptance remains open. | Audit current strings, accessibility labels, touch targets, and readable layout; collect user feedback on the familiar reply interaction. |
| G11 | Workstream persistence appends incoming rows, resolution/expiry mutations, and terminal replies; appends are asynchronous and errors are ignored. | Keep the restart test and add crash/write-failure telemetry before release. The current durable record is sufficient for reply identity, while write acknowledgement remains a reliability gap. |
| G12 | Mac history uses one unbounded OS-user JSONL path; phone has no persisted Feed cache or older-history cursor. | Decide retention, deletion, account/tag separation, and offline history expectations before release. |
| G13 | Existing `feed.v1` capability does not distinguish hosts with the new `feed.text` method. | Proposed compatibility decision: negotiate the method or require coordinated app updates. |
| G14 | Notifications parity still missing from the Feed: per-row read/unread and Mark All Read (Mac-authoritative in Notifications), Unread filter, day sections, grouping of bursts, load-more pagination, analytics events, a push tap that lands on the Feed, and a Feed shell UI-test fixture to migrate the Notifications UI tests (cmuxUITests 4402, 4432, 4494, 4565, 4570, 4879, 4949, 5005). | Needed only if D31 removes the Notifications tab. Read state needs a Mac-side contract; the rest is phone-only. |
| G15 | The branch merged main (`b915280649e`, `6e1c986b00b`), 3,490 commits of drift. Local guards flagged this branch's provider workflow (fixed in `8d571f6227d`) and a sleep-based test (fixed in `a29ad0693df`). | The first compile of the merged tree happens in PR CI; the fleet iOS build (job `3be8753bb7735831e36b3cfb`) waits on the offline iOS Mac. |

## Source map

- [Feed overview and existing provider integration semantics](feed.md).
- [Full-text reader](../Packages/iOS/CmuxMobileShellUI/Sources/CmuxMobileShellUI/AgentFeedFullTextView.swift), [retained content and paging](../Packages/macOS/CMUXAgentLaunch/Sources/CMUXAgentLaunch/Workstream/WorkstreamFullText.swift).
- [Timeline, filtering, and swipe actions](../Packages/iOS/CmuxMobileShellUI/Sources/CmuxMobileShellUI/AgentFeedView.swift).
- [Row controls and reply states](../Packages/iOS/CmuxMobileShellUI/Sources/CmuxMobileShellUI/AgentFeedRow.swift), [composer](../Packages/iOS/CmuxMobileShellUI/Sources/CmuxMobileShellUI/AgentFeedReplyComposer.swift).
- [Multi-question answer composition](../Packages/iOS/CmuxMobileShellUI/Sources/CmuxMobileShellUI/AgentFeedQuestionAnswerComposer.swift), [pager tests](../Packages/iOS/CmuxMobileShellUI/Tests/CmuxMobileShellUITests/AgentFeedQuestionAnswerComposerTests.swift).
- [Feed synchronization, routing, submission, and stop matching](../Packages/iOS/CmuxMobileShell/Sources/CmuxMobileShell/MobileShellComposite+AgentFeed.swift).
- [Item identity, reply eligibility, and triage semantics](../Packages/iOS/CmuxMobileShellModel/Sources/CmuxMobileShellModel/MobileAgentFeedItem.swift).
- [Mac terminal submission](../Sources/TerminalController.swift), [active-provider submit-key selection](../Sources/TextBoxAgentDetection.swift).
- [Reply regression tests](../Packages/iOS/CmuxMobileShell/Tests/CmuxMobileShellTests/MobileAgentFeedTerminalReplyTests.swift), [Feed state tests](../Packages/iOS/CmuxMobileShell/Tests/CmuxMobileShellTests/MobileShellAgentFeedStateTests.swift).
- [Real terminal harness](../tests_v2/test_mobile_terminal_paste_submit.py), [provider editor harness](../tests_v2/test_mobile_terminal_paste_providers.py), [hosted provider workflow](../.github/workflows/test-feed-reply-providers.yml).
- [Personal account setup and refresh](../scripts/setup-team-dev.sh), [refresh regression tests](../scripts/setup-team-dev.test.mjs).

## Change log

| Date | Change |
| D31 | Whether the Notifications tab is removed for everyone, and what the Feed badge and app-icon badge count, is the user's decision. Today Feed badges needs-input rows and Notifications badges unread notifications; the icon badge is the Mac's unread count. | Open decision. F40 lets it be dogfooded first. |
| D32 | A failed reply is never resent automatically. When the Mac may already have the text, the user checks the terminal first. | Principled: the phone cannot know whether the Mac typed the text after a transport error, so only the user can safely decide. |
| --- | --- |
| 2026-09-25 | Overnight production-readiness pass toward replacing Notifications: merged main, fixed branch-introduced guard failures, added F38 to F44, recorded D31 and D32 plus parity gap G14. Nothing was installed on a device because the fleet's only iOS builder was offline. |
| 2026-09-24 | F37: after user feedback, user-message quotes now point right (sent style, accent), agent quotes stay left, the reply shows as a trailing accent bubble, and the tail join is smoothed. Built by controller jobs `2850e5d9bce019ba4a39c9e9` and `4ec1625f7a7cabc1d2431134`; installed on the simulator and iPhone. |
| 2026-09-24 | Added F37 iMessage-style Feed quote bubbles behind the DEBUG-only CMUX Labs switch `cmux.mobile.debug.feedBubbleQuotes.v1` (default on in DEBUG, unavailable in production). The first tail geometry flattened the corner and was replaced with the Messages tail curve in `d6db143f32f`. Controller jobs `cc313afc8a158732effbd815` and `eda105cf49e62bcee0f2cf96`. |
| 2026-09-24 | Restyled the Feed Reply action (F14) after the user said it looked off: outline arrow plus footnote label in secondary color, 44-point hit area without layout height. Built by controller job `95172994909cc5c84cc49ef4`, installed and paired on the iPhone and the task simulator. |
| 2026-09-24 | Added Claude multi-question handling to the iOS Feed. Each AskUserQuestion round now keeps per-question drafts, presents one question per swipeable page with page indicators and explicit Next/Previous controls, and submits ordered human-readable answers only after every page is answered. The design follows Apple's [Page controls HIG](https://developer.apple.com/design/human-interface-guidelines/page-controls) and [Scroll views HIG](https://developer.apple.com/design/human-interface-guidelines/scroll-views). |
| 2026-09-24 | Added Markdown rendering across Feed event content. Native previews parse headings, lists, quotes, emphasis, code, links, and strikethrough through Foundation's Markdown attributes, while full-text reading uses the shared artifact Markdown renderer. Quoted prompts, question content, option descriptions, and recorded replies now use the same inline presentation. |
| 2026-09-24 | Fixed Markdown preview truncation to cut rendered attributed text once and append a plain See more control using rendered offsets. Added regression coverage for links, emphasis, Unicode, and short rows; updated the full-text UI fixture for headings, lists, and code. |
| 2026-09-24 | Verified block and inline Markdown in the production preview and reader through the hosted iPhone UI test at db0343155f3. Saved screenshots, recording, and result excerpt in a workspace evidence gallery. |
| 2026-09-24 | Built the matching fmdblk pair through controller jobs. Installed/authenticated Mac and updated the existing nearby simulator, which passed the connection gate. Signed and queued the phone update while the device is unavailable. |
| 2026-09-23 | Added notification-to-Feed projection, combined source revisions, durable Workstream mutation replay, durable reply state, and explicit `feed_event_id` reply identity. Started a fresh `feedsim` Mac build from `9bf90380b426`; the current iOS simulator rebuild is queued after two fleet simulator-phase failures, and the physical phone remains unreachable and queued. |
| 2026-09-23 | Built F28–F31, verified inline expansion, shared controls, Feed search, and event context menus in hosted UI, and passed focused routing/search tests. Saved screenshot/video proof and updated the HTML walkthrough. Installed/authenticated Mac 6dd5a1ad6f4; signed and queued the matching phone update after the device became unreachable (V35–V39). |
| 2026-09-23 | Completed authenticated Mac/iPhone delivery and persisted reconnect (V34). Added the source-backed HTML architecture walkthrough (F27), with durability/ownership/compatibility findings clearly separated from approved implementation scope. User added inline expansion, shared computer controls, active-tab search, and event context navigation (F28–F31). |
| 2026-09-23 | Verified full-text reading in hosted iPhone UI, focused packages, and seven actual CLI hook paths. Installed/authenticated the Mac; documented controller packaging repairs and pending phone delivery in V28–V33. |
| 2026-09-23 | Added F26 for the full-text request. Implemented a reading sheet, paged reads, provider-neutral completion preservation, all nine locale translations, and focused pagination/routing tests. Updated the worktree to upstream branch head e6b60c29617 before edits. Verification and delivery remain separately tracked. |
| 2026-09-15 | Created the session inventory from user requests, current source, branch history, and recorded verification. Distinguished implementation from evidence, and recorded remaining installation, pairing, submission, UI, and test gaps. No product scope added or removed. |
| 2026-09-15 | Investigated the user's inability to connect to the Mac. Confirmed working servers, an installed phone app, and rejected personal credentials; aligned the Mac server configuration and refreshed installation tooling. Added F24 for credential recovery within F20, with D15 and V11–V12 recording the repair and remaining blocker. |
| 2026-09-15 | Ran the requested tagged dev launcher instead of normal sign-in. The launcher reached the exact Mac but failed the personal account gate; added V14. |
| 2026-09-15 | Completed developer sign-in and phone pairing without user commands; a credential-free relaunch passed. Added D16 and V15–V16 and removed the user-run setup requirement from G01. |
| 2026-09-15 | User requested pulling main and reinstalling. Merged 1,646 upstream commits, retained Feed in updated compact and split navigation, and started rebuilding the pair. Added F25, D17, and V17. |
| 2026-09-15 | Reconciled main’s notification reply callers with asynchronous Feed submission, added delayed-success/failure coverage, and expanded the provider workflow to run notification delivery tests. Updated the attachment receipt fixture for main’s evidence metadata. |
| 2026-09-16 | Merged newer transport changes, rebuilt the Mac and phone archive, signed the phone app, checked the updated personal credentials, and restored the Mac through a genuine developer session. Retained the signed phone app and cleared its automatic retry after the credential rejection. Recorded the unreachable phone and stale simulator cache without claiming completed phone delivery. |
| 2026-09-24 | Removed Feed’s empty title region using the shared inline navigation mode. Added a screen regression that fails on the original 76-point gap and passes after the fix; inspected iOS 27 presentation. Corrected shallow checkout for manual iOS checks. Updated Mac, phone, and nearby simulator; verified authentication and persisted phone reconnect. Added F36, D30, and V45–V46. |

## Dictionary

- **RPC:** a request one app sends to another app to perform an operation.
- **Revision:** an increasing number used to reject an older Feed snapshot.
- **Tag:** a name isolating a development Mac/iOS app pair from other installed versions.
- **Dogfood:** trying the development app through the actual user flow before approving the change.
- **Harness:** a program that drives a repeatable test and captures its result.
- **Hosted check:** an automated build or test running outside the user's Mac.
- **CLA:** the contributor license agreement checked for contributions to the repository.
