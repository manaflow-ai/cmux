note: installing cua-driver via the Rust implementation.
==> detected Rosetta-translated shell on Apple Silicon — switching to darwin-arm64
==> using baked release: cua-driver-rs-v0.8.3
==> downloading https://github.com/trycua/cua/releases/download/cua-driver-rs-v0.8.3/cua-driver-rs-0.8.3-darwin-universal.tar.gz
==> extracting
==> installing /Applications/CuaDriver.app
==> symlinked /Users/austinwang/.local/bin/cua-driver -> /Applications/CuaDriver.app/Contents/MacOS/cua-driver
==> stopping any running cua-driver daemons before swap
Telemetry defaults to enabled for new installations; saved preferences and environment overrides are honored.
When enabled, Cua collects a pseudonymous installation ID and bounded, content-free usage metadata.
  No prompts, tool arguments, screen contents, or file paths are collected.
  Disable persistently at any time: /Users/austinwang/.local/bin/cua-driver telemetry disable

cua-driver-rs 0.8.3 installed.


Next steps:

  1. Try it:
       /Users/austinwang/.local/bin/cua-driver --version
       /Users/austinwang/.local/bin/cua-driver list-tools

  2. Grant permissions (macOS — Accessibility + Screen Recording):
       /Users/austinwang/.local/bin/cua-driver permissions status   # report status (read-only; tells you which
                                       # identity the grants belong to)
       /Users/austinwang/.local/bin/cua-driver permissions grant    # the correct way to grant — launches CuaDriver
                                       # so the dialog says "Cua Driver" and the grant
                                       # sticks to the driver, not your terminal.
                                   # Tip: `--autostart` (above) keeps a launchd daemon
                                   # running so you grant once and it persists.

  3. Agent skill pack (optional):
       /Users/austinwang/.local/bin/cua-driver skills install   # fetch + link the cua-driver skill pack
                                   # into Claude Code / Codex / OpenClaw / OpenCode.
                                   # The install never touches your agent dirs.

  4. As an MCP server — run the one matching your client. Each is also
     available via '/Users/austinwang/.local/bin/cua-driver mcp-config --client <name>':

     • Claude Code (computer-use compatibility mode):
         /Users/austinwang/.local/bin/cua-driver mcp-config --client claude
       Copy-paste the printed command into your shell — it uses
       `claude mcp add-json` (not bare `--`) so the long flag travels
       through PowerShell's arg parser intact. This mode grounds Claude
       Code's vision/computer-use-style flow on CuaDriver window
       screenshots — it keeps the normal CuaDriver tools and changes
       only the screenshot tool.

     • Codex (OpenAI):
         codex mcp add cua-driver -- /Users/austinwang/.local/bin/cua-driver mcp

     • Qwen Code:
         qwen mcp add cua-driver /Users/austinwang/.local/bin/cua-driver mcp

     • Factory Droid:
         droid mcp add cua-driver "/Users/austinwang/.local/bin/cua-driver mcp"

     • OpenClaw:
         /Users/austinwang/.local/bin/cua-driver mcp-config --client openclaw

     • GitHub Copilot CLI (paste into ~/.copilot/mcp-config.json):
         {
           "mcpServers": {
             "cua-driver": {
               "type": "local",
               "command": "/Users/austinwang/.local/bin/cua-driver",
               "args": ["mcp"],
               "tools": ["*"]
             }
           }
         }

     • Cursor / OpenCode / Hermes (no add CLI — paste config):
         /Users/austinwang/.local/bin/cua-driver mcp-config --client cursor     # JSON for ~/.cursor/mcp.json
         /Users/austinwang/.local/bin/cua-driver mcp-config --client opencode   # JSON for opencode.json
         /Users/austinwang/.local/bin/cua-driver mcp-config --client hermes     # YAML for ~/.hermes/config.yaml

     • ZCode (Z.ai — GUI app; paste in Settings → MCP Servers):
         /Users/austinwang/.local/bin/cua-driver mcp-config --client zcode

     For other clients accepting the generic mcpServers shape:
         /Users/austinwang/.local/bin/cua-driver mcp-config

Docs: https://github.com/trycua/cua/tree/main/libs/cua-driver/rust

macOS TCC: grant Accessibility + Screen Recording on first run:
  open -n -g -a CuaDriver --args serve
  /Users/austinwang/.local/bin/cua-driver check_permissions
cua-driver ready at /Users/austinwang/.local/bin/cua-driver
cua-driver daemon ready
