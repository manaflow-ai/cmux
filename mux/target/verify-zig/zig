#!/bin/sh
prefix=
prev=
for arg in "$@"; do
  if [ "$prev" = "--prefix" ]; then prefix="$arg"; break; fi
  prev="$arg"
done
if [ -z "$prefix" ]; then echo "missing --prefix" >&2; exit 1; fi
mkdir -p "$prefix/lib"
ar rcs "$prefix/lib/libghostty-vt.a"
exit 0
