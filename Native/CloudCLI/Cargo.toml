[package]
name = "cmux-cloud-cli"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "cmux-cloud"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
