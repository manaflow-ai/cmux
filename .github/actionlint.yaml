self-hosted-runner:
  labels:
    # macOS jobs run on Blacksmith (hardcoded in the workflows). These are the
    # labels referenced directly or offered as manual workflow_dispatch choices.
    # See docs/macos-ci-runners.md.
    - blacksmith-6vcpu-macos-15
    - blacksmith-6vcpu-macos-26
    - blacksmith-6vcpu-macos-latest
    - depot-macos-latest
    - depot-macos-14
