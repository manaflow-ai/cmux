name: cmux-tui

on:
  # Temporarily manual-only beginning 2026-07-13 to pause automatic CI.
  workflow_dispatch:

concurrency:
  group: cmux-tui-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  web-frontend:
    runs-on: ${{ vars.LINUX_RUNNER || 'blacksmith-4vcpu-ubuntu-2404' }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "22.14.0"
          cache: npm
          cache-dependency-path: |
            cmux-tui/bindings/typescript/package-lock.json
            cmux-tui/frontends/web/package-lock.json

      - name: Build TypeScript SDK
        working-directory: cmux-tui/bindings/typescript
        run: |
          npm ci --no-audit --no-fund
          npm run build

      - name: Build and test web frontend
        working-directory: cmux-tui/frontends/web
        run: |
          npm ci --no-audit --no-fund
          npm run build
          npm test

  valgrind-leak-check:
    runs-on: ${{ vars.LINUX_RUNNER || 'blacksmith-4vcpu-ubuntu-2404' }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Init ghostty submodule
        run: git submodule update --init --depth 1 ghostty

      - name: Install valgrind
        run: |
          sudo apt-get update
          sudo apt-get install -y valgrind

      - name: Install zig
        run: ./scripts/install-zig-ci.sh

      - name: Rust version
        run: |
          rustc --version || true
          if ! command -v cargo >/dev/null; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
            echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
          fi

      - name: Build test binaries
        working-directory: cmux-tui
        env:
          # Force libghostty-vt's zig build onto a baseline CPU target so its
          # SIMD codegen stays within what valgrind's instruction emulation
          # supports (see crates/ghostty-vt-sys/build.rs).
          CMUX_GHOSTTY_VT_ZIG_CPU: baseline
        run: |
          mkdir -p target
          cargo test --workspace --locked --no-run --message-format=json > target/cargo-test-binaries.jsonl
          python3 <<'PY'
          import json
          import sys

          seen = set()
          with open("target/cargo-test-binaries.jsonl", "r", encoding="utf-8") as messages:
              with open("target/valgrind-test-binaries.txt", "w", encoding="utf-8") as output:
                  for line in messages:
                      try:
                          message = json.loads(line)
                      except json.JSONDecodeError:
                          continue
                      if not message.get("profile", {}).get("test"):
                          continue
                      executable = message.get("executable")
                      if not executable or executable in seen:
                          continue
                      seen.add(executable)
                      print(executable, file=output)

          if not seen:
              raise SystemExit("cargo did not report any test binaries")
          print(f"Collected {len(seen)} test binaries", file=sys.stderr)
          PY

      - name: Run test binaries under valgrind
        working-directory: cmux-tui
        env:
          # Valgrind's ~30x slowdown breaks the 50ms ws-latency budget on an
          # otherwise-correct build; the guarded regression (events serialized
          # behind a 100ms read poll) inflates far past this bound anyway.
          CMUX_TEST_WS_LATENCY_BUDGET_MS: "2000"
          # Process-exit and PTY-reader tests also use bounded polling. Keep
          # their normal deadlines strict while allowing for instrumentation.
          CMUX_TEST_TIMEOUT_SCALE: "4"
        run: |
          while IFS= read -r bin; do
            [ -n "$bin" ] || continue
            echo "Running valgrind for $bin"
            if ! valgrind \
              --error-exitcode=1 \
              --leak-check=full \
              --show-leak-kinds=definite \
              --errors-for-leak-kinds=definite \
              --track-origins=yes \
              -- "$bin"; then
              echo "Valgrind failed for $bin" >&2
              exit 1
            fi
          done < target/valgrind-test-binaries.txt

  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os == 'macos' && (vars.MACOS_RUNNER_15 || 'blacksmith-6vcpu-macos-15') || 'ubuntu-latest' }}
    timeout-minutes: 40
    strategy:
      fail-fast: false
      matrix:
        os: [macos, linux]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Init ghostty submodule
        run: git submodule update --init --depth 1 ghostty

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y clang libclang-dev pkg-config

      - name: Install zig
        run: ./scripts/install-zig-ci.sh

      - name: Rust version
        run: |
          rustc --version || true
          if ! command -v cargo >/dev/null; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
            echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
          fi

      - name: cargo fmt
        working-directory: cmux-tui
        run: cargo fmt --check

      - name: cargo clippy
        working-directory: cmux-tui
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

      - name: cargo test
        working-directory: cmux-tui
        run: cargo test --workspace --locked

      - name: crossterm parser tests
        working-directory: cmux-tui
        run: cargo test --manifest-path vendor/crossterm/Cargo.toml --lib

      - name: TUI smoke test (scripted pty)
        working-directory: cmux-tui
        run: |
          cargo build -p cmux-tui
          python3 scripts/smoke-tui.py

      - name: Detach/reattach smoke test
        working-directory: cmux-tui
        run: python3 scripts/smoke-attach.py

  bindings-e2e:
    runs-on: ${{ vars.LINUX_RUNNER || 'blacksmith-4vcpu-ubuntu-2404' }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Init ghostty submodule
        run: git submodule update --init --depth 1 ghostty

      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang libclang-dev pkg-config

      - name: Install zig
        run: ./scripts/install-zig-ci.sh

      - name: Rust version
        run: |
          rustc --version || true
          if ! command -v cargo >/dev/null; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
            echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
          fi

      - name: Java version
        run: |
          echo "JAVA_HOME_17_X64=$JAVA_HOME_17_X64"
          echo "$JAVA_HOME_17_X64/bin" >> "$GITHUB_PATH"
          export PATH="$JAVA_HOME_17_X64/bin:$PATH"
          java -version
          javac -version

      - name: Build cmux-tui server
        working-directory: cmux-tui
        run: cargo build -p cmux-tui

      - name: Python conformance fixtures
        run: python3 cmux-tui/bindings/conformance/runner.py

      - name: Binding e2e
        run: bash cmux-tui/bindings/conformance/e2e.sh --require python,typescript,rust,go,java

  windows-gnu:
    name: windows GNU host (x86_64-gnu)
    runs-on: windows-latest
    timeout-minutes: 40
    env:
      BINDGEN_EXTRA_CLANG_ARGS: --target=x86_64-w64-windows-gnu --sysroot=C:/msys64/mingw64
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Init ghostty submodule
        run: git submodule update --init --depth 1 ghostty

      - name: Resolve Ghostty Zig version
        id: ghostty-zig-version
        shell: bash
        run: |
          version="$(bash ./scripts/ghostty-zig-version.sh)"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Install zig
        uses: mlugg/setup-zig@8d6198c65fb0feaa111df26e6b467fea8345e46f # v2.0.5
        with:
          version: ${{ steps.ghostty-zig-version.outputs.version }}

      - name: Install Rust GNU host and cross-test toolchains
        shell: bash
        run: |
          rustup toolchain install 1.91.0-x86_64-pc-windows-gnu --profile minimal
          rustup toolchain install 1.91.0-x86_64-pc-windows-msvc --profile minimal
          rustup target add --toolchain 1.91.0-x86_64-pc-windows-msvc x86_64-pc-windows-gnu
          printf '%s\n' 'C:\msys64\mingw64\bin' >> "$GITHUB_PATH"
          test "$(rustc +1.91.0-x86_64-pc-windows-gnu -vV | sed -n 's/^host: //p')" = x86_64-pc-windows-gnu
          test "$(rustc +1.91.0-x86_64-pc-windows-msvc -vV | sed -n 's/^host: //p')" = x86_64-pc-windows-msvc

      - name: Build libghostty-vt for Windows GNU
        shell: bash
        working-directory: ghostty
        run: |
          zig build -Demit-lib-vt=true -Demit-xcframework=false -Doptimize=ReleaseFast -Dtarget=x86_64-windows-gnu --prefix "$RUNNER_TEMP/ghostty-vt-win-gnu"
          zig ar t "$RUNNER_TEMP/ghostty-vt-win-gnu/lib/ghostty-vt-static.lib" | head

      - name: Build cmux-tui on a native Windows GNU host
        id: native-build
        continue-on-error: true
        working-directory: cmux-tui
        run: cargo +1.91.0-x86_64-pc-windows-gnu build -p cmux-tui --target x86_64-pc-windows-gnu --locked

      - name: Test native Zig target selection
        id: zig-target-test
        continue-on-error: true
        shell: bash
        working-directory: cmux-tui
        run: |
          rustc +1.91.0-x86_64-pc-windows-gnu --test crates/ghostty-vt-sys/build_support.rs -o "$RUNNER_TEMP/ghostty-vt-build-support-tests.exe"
          "$RUNNER_TEMP/ghostty-vt-build-support-tests.exe" --exact tests::native_windows_gnu_keeps_the_explicit_gnu_abi

      - name: Test that unsupported terminal probes stay silent
        id: terminal-probe-test
        continue-on-error: true
        working-directory: cmux-tui
        run: cargo +1.91.0-x86_64-pc-windows-msvc test -p cmux-tui --bin cmux-tui --target x86_64-pc-windows-gnu --locked terminal_probe_does_not_write_queries_without_a_reply_reader

      - name: Test Windows file URL conversion
        id: file-url-test
        continue-on-error: true
        working-directory: cmux-tui
        run: cargo +1.91.0-x86_64-pc-windows-msvc test -p cmux-tui --bin cmux-tui --target x86_64-pc-windows-gnu --locked creates_windows_drive_file_url

      - name: Require all Windows GNU checks
        if: always()
        shell: bash
        env:
          NATIVE_BUILD_OUTCOME: ${{ steps.native-build.outcome }}
          ZIG_TARGET_TEST_OUTCOME: ${{ steps.zig-target-test.outcome }}
          TERMINAL_PROBE_TEST_OUTCOME: ${{ steps.terminal-probe-test.outcome }}
          FILE_URL_TEST_OUTCOME: ${{ steps.file-url-test.outcome }}
        run: |
          test "$NATIVE_BUILD_OUTCOME" = success
          test "$ZIG_TARGET_TEST_OUTCOME" = success
          test "$TERMINAL_PROBE_TEST_OUTCOME" = success
          test "$FILE_URL_TEST_OUTCOME" = success
