// This file is generated by cmux-tui/bindings/codegen. Do not edit by hand.
// Source: cmux-tui/spec/sdk-schema.json

// Protocol 12, IR e999c6a8ee616adfbcd5015a5355382d628772d63e3347e633c0632c304097a9.

import Foundation

extension CloudTuiGenerated {
    struct ApplyLayoutRequest: Codable, Sendable, Equatable {
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let layout: CloudTuiGenerated.DeclarativeLayout
        let name: CloudTuiGenerated.OptionalField<String>
        let rows: CloudTuiGenerated.OptionalField<UInt16>
        let workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, layout: CloudTuiGenerated.DeclarativeLayout, name: CloudTuiGenerated.OptionalField<String> = .missing, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing, workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.cols = cols
            self.layout = layout
            self.name = name
            self.rows = rows
            self.workspace = workspace
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case layout = "layout"
            case name = "name"
            case rows = "rows"
            case workspace = "workspace"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            self.layout = try container.decode(CloudTuiGenerated.DeclarativeLayout.self, forKey: .layout)
            if !container.contains(.name) {
                self.name = .missing
            } else if try container.decodeNil(forKey: .name) {
                self.name = .null
            } else {
                self.name = .value(try container.decode(String.self, forKey: .name))
            }
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
            if !container.contains(.workspace) {
                self.workspace = .missing
            } else if try container.decodeNil(forKey: .workspace) {
                self.workspace = .null
            } else {
                self.workspace = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .workspace))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            try container.encode(layout, forKey: .layout)
            switch name {
            case .missing: break
            case .null: try container.encodeNil(forKey: .name)
            case .value(let value): try container.encode(value, forKey: .name)
            }
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
            switch workspace {
            case .missing: break
            case .null: try container.encodeNil(forKey: .workspace)
            case .value(let value): try container.encode(value, forKey: .workspace)
            }
        }
    }

    struct AttachSurfaceRequest: Codable, Sendable, Equatable {
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let expectedGeneration: CloudTuiGenerated.OptionalField<String>
        let expectedTerminalId: CloudTuiGenerated.OptionalField<String>
        let mode: CloudTuiGenerated.OptionalField<CloudTuiGenerated.AttachSurfaceRequestMode>
        let rows: CloudTuiGenerated.OptionalField<UInt16>
        let surface: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, expectedGeneration: CloudTuiGenerated.OptionalField<String> = .missing, expectedTerminalId: CloudTuiGenerated.OptionalField<String> = .missing, mode: CloudTuiGenerated.OptionalField<CloudTuiGenerated.AttachSurfaceRequestMode> = .missing, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing, surface: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.cols = cols
            self.expectedGeneration = expectedGeneration
            self.expectedTerminalId = expectedTerminalId
            self.mode = mode
            self.rows = rows
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case expectedGeneration = "expected_generation"
            case expectedTerminalId = "expected_terminal_id"
            case mode = "mode"
            case rows = "rows"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            if !container.contains(.expectedGeneration) {
                self.expectedGeneration = .missing
            } else if try container.decodeNil(forKey: .expectedGeneration) {
                self.expectedGeneration = .null
            } else {
                self.expectedGeneration = .value(try container.decode(String.self, forKey: .expectedGeneration))
            }
            if !container.contains(.expectedTerminalId) {
                self.expectedTerminalId = .missing
            } else if try container.decodeNil(forKey: .expectedTerminalId) {
                self.expectedTerminalId = .null
            } else {
                self.expectedTerminalId = .value(try container.decode(String.self, forKey: .expectedTerminalId))
            }
            if !container.contains(.mode) {
                self.mode = .missing
            } else if try container.decodeNil(forKey: .mode) {
                self.mode = .null
            } else {
                self.mode = .value(try container.decode(CloudTuiGenerated.AttachSurfaceRequestMode.self, forKey: .mode))
            }
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
            if !container.contains(.surface) {
                self.surface = .missing
            } else if try container.decodeNil(forKey: .surface) {
                self.surface = .null
            } else {
                self.surface = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .surface))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            switch expectedGeneration {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedGeneration)
            case .value(let value): try container.encode(value, forKey: .expectedGeneration)
            }
            switch expectedTerminalId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedTerminalId)
            case .value(let value): try container.encode(value, forKey: .expectedTerminalId)
            }
            switch mode {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mode)
            case .value(let value): try container.encode(value, forKey: .mode)
            }
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
            switch surface {
            case .missing: break
            case .null: try container.encodeNil(forKey: .surface)
            case .value(let value): try container.encode(value, forKey: .surface)
            }
        }
    }

    struct BrowserActivateRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id

        init(surface: CloudTuiGenerated.Id) {
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct BrowserBackRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id

        init(surface: CloudTuiGenerated.Id) {
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct BrowserForwardRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id

        init(surface: CloudTuiGenerated.Id) {
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct BrowserFramePresentedRequest: Codable, Sendable, Equatable {
        let frameSeq: UInt64
        let surface: CloudTuiGenerated.Id

        init(frameSeq: UInt64, surface: CloudTuiGenerated.Id) {
            self.frameSeq = frameSeq
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case frameSeq = "frame_seq"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.frameSeq = try container.decode(UInt64.self, forKey: .frameSeq)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(frameSeq, forKey: .frameSeq)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct BrowserInsertTextRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id
        let text: String

        init(surface: CloudTuiGenerated.Id, text: String) {
            self.surface = surface
            self.text = text
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
            case text = "text"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            self.text = try container.decode(String.self, forKey: .text)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
            try container.encode(text, forKey: .text)
        }
    }

    struct BrowserKeyRequest: Codable, Sendable, Equatable {
        let code: String
        let key: String
        let kind: CloudTuiGenerated.BrowserKeyRequestKind
        let modifiers: UInt32
        let surface: CloudTuiGenerated.Id
        let text: CloudTuiGenerated.OptionalField<String>
        let windowsVirtualKeyCode: UInt32

        init(code: String, key: String, kind: CloudTuiGenerated.BrowserKeyRequestKind, modifiers: UInt32, surface: CloudTuiGenerated.Id, text: CloudTuiGenerated.OptionalField<String> = .missing, windowsVirtualKeyCode: UInt32) {
            self.code = code
            self.key = key
            self.kind = kind
            self.modifiers = modifiers
            self.surface = surface
            self.text = text
            self.windowsVirtualKeyCode = windowsVirtualKeyCode
        }

        enum CodingKeys: String, CodingKey {
            case code = "code"
            case key = "key"
            case kind = "kind"
            case modifiers = "modifiers"
            case surface = "surface"
            case text = "text"
            case windowsVirtualKeyCode = "windows_virtual_key_code"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.code = try container.decode(String.self, forKey: .code)
            self.key = try container.decode(String.self, forKey: .key)
            self.kind = try container.decode(CloudTuiGenerated.BrowserKeyRequestKind.self, forKey: .kind)
            self.modifiers = try container.decode(UInt32.self, forKey: .modifiers)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            if !container.contains(.text) {
                self.text = .missing
            } else if try container.decodeNil(forKey: .text) {
                self.text = .null
            } else {
                self.text = .value(try container.decode(String.self, forKey: .text))
            }
            self.windowsVirtualKeyCode = try container.decode(UInt32.self, forKey: .windowsVirtualKeyCode)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(code, forKey: .code)
            try container.encode(key, forKey: .key)
            try container.encode(kind, forKey: .kind)
            try container.encode(modifiers, forKey: .modifiers)
            try container.encode(surface, forKey: .surface)
            switch text {
            case .missing: break
            case .null: try container.encodeNil(forKey: .text)
            case .value(let value): try container.encode(value, forKey: .text)
            }
            try container.encode(windowsVirtualKeyCode, forKey: .windowsVirtualKeyCode)
        }
    }

    struct BrowserKeyPressRequest: Codable, Sendable, Equatable {
        let code: String
        let key: String
        let modifiers: UInt32
        let surface: CloudTuiGenerated.Id
        let text: CloudTuiGenerated.OptionalField<String>
        let windowsVirtualKeyCode: UInt32

        init(code: String, key: String, modifiers: UInt32, surface: CloudTuiGenerated.Id, text: CloudTuiGenerated.OptionalField<String> = .missing, windowsVirtualKeyCode: UInt32) {
            self.code = code
            self.key = key
            self.modifiers = modifiers
            self.surface = surface
            self.text = text
            self.windowsVirtualKeyCode = windowsVirtualKeyCode
        }

        enum CodingKeys: String, CodingKey {
            case code = "code"
            case key = "key"
            case modifiers = "modifiers"
            case surface = "surface"
            case text = "text"
            case windowsVirtualKeyCode = "windows_virtual_key_code"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.code = try container.decode(String.self, forKey: .code)
            self.key = try container.decode(String.self, forKey: .key)
            self.modifiers = try container.decode(UInt32.self, forKey: .modifiers)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            if !container.contains(.text) {
                self.text = .missing
            } else if try container.decodeNil(forKey: .text) {
                self.text = .null
            } else {
                self.text = .value(try container.decode(String.self, forKey: .text))
            }
            self.windowsVirtualKeyCode = try container.decode(UInt32.self, forKey: .windowsVirtualKeyCode)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(code, forKey: .code)
            try container.encode(key, forKey: .key)
            try container.encode(modifiers, forKey: .modifiers)
            try container.encode(surface, forKey: .surface)
            switch text {
            case .missing: break
            case .null: try container.encodeNil(forKey: .text)
            case .value(let value): try container.encode(value, forKey: .text)
            }
            try container.encode(windowsVirtualKeyCode, forKey: .windowsVirtualKeyCode)
        }
    }

    struct BrowserMouseRequest: Codable, Sendable, Equatable {
        let button: CloudTuiGenerated.OptionalField<String>
        let clickCount: CloudTuiGenerated.OptionalField<UInt32>
        let frameSeq: CloudTuiGenerated.OptionalField<UInt64>
        let kind: CloudTuiGenerated.BrowserMouseRequestKind
        let surface: CloudTuiGenerated.Id
        let xPx: Double
        let yPx: Double

        init(button: CloudTuiGenerated.OptionalField<String> = .missing, clickCount: CloudTuiGenerated.OptionalField<UInt32> = .missing, frameSeq: CloudTuiGenerated.OptionalField<UInt64> = .missing, kind: CloudTuiGenerated.BrowserMouseRequestKind, surface: CloudTuiGenerated.Id, xPx: Double, yPx: Double) {
            self.button = button
            self.clickCount = clickCount
            self.frameSeq = frameSeq
            self.kind = kind
            self.surface = surface
            self.xPx = xPx
            self.yPx = yPx
        }

        enum CodingKeys: String, CodingKey {
            case button = "button"
            case clickCount = "click_count"
            case frameSeq = "frame_seq"
            case kind = "kind"
            case surface = "surface"
            case xPx = "x_px"
            case yPx = "y_px"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.button) {
                self.button = .missing
            } else if try container.decodeNil(forKey: .button) {
                self.button = .null
            } else {
                self.button = .value(try container.decode(String.self, forKey: .button))
            }
            if !container.contains(.clickCount) {
                self.clickCount = .missing
            } else if try container.decodeNil(forKey: .clickCount) {
                self.clickCount = .null
            } else {
                self.clickCount = .value(try container.decode(UInt32.self, forKey: .clickCount))
            }
            if !container.contains(.frameSeq) {
                self.frameSeq = .missing
            } else if try container.decodeNil(forKey: .frameSeq) {
                self.frameSeq = .null
            } else {
                self.frameSeq = .value(try container.decode(UInt64.self, forKey: .frameSeq))
            }
            self.kind = try container.decode(CloudTuiGenerated.BrowserMouseRequestKind.self, forKey: .kind)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            self.xPx = try container.decode(Double.self, forKey: .xPx)
            self.yPx = try container.decode(Double.self, forKey: .yPx)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch button {
            case .missing: break
            case .null: try container.encodeNil(forKey: .button)
            case .value(let value): try container.encode(value, forKey: .button)
            }
            switch clickCount {
            case .missing: break
            case .null: try container.encodeNil(forKey: .clickCount)
            case .value(let value): try container.encode(value, forKey: .clickCount)
            }
            switch frameSeq {
            case .missing: break
            case .null: try container.encodeNil(forKey: .frameSeq)
            case .value(let value): try container.encode(value, forKey: .frameSeq)
            }
            try container.encode(kind, forKey: .kind)
            try container.encode(surface, forKey: .surface)
            try container.encode(xPx, forKey: .xPx)
            try container.encode(yPx, forKey: .yPx)
        }
    }

    struct BrowserMouseGuardedRequest: Codable, Sendable, Equatable {
        let button: CloudTuiGenerated.OptionalField<String>
        let clickCount: CloudTuiGenerated.OptionalField<UInt32>
        let frameSeq: UInt64
        let kind: CloudTuiGenerated.BrowserMouseGuardedRequestKind
        let surface: CloudTuiGenerated.Id
        let xPx: Double
        let yPx: Double

        init(button: CloudTuiGenerated.OptionalField<String> = .missing, clickCount: CloudTuiGenerated.OptionalField<UInt32> = .missing, frameSeq: UInt64, kind: CloudTuiGenerated.BrowserMouseGuardedRequestKind, surface: CloudTuiGenerated.Id, xPx: Double, yPx: Double) {
            self.button = button
            self.clickCount = clickCount
            self.frameSeq = frameSeq
            self.kind = kind
            self.surface = surface
            self.xPx = xPx
            self.yPx = yPx
        }

        enum CodingKeys: String, CodingKey {
            case button = "button"
            case clickCount = "click_count"
            case frameSeq = "frame_seq"
            case kind = "kind"
            case surface = "surface"
            case xPx = "x_px"
            case yPx = "y_px"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.button) {
                self.button = .missing
            } else if try container.decodeNil(forKey: .button) {
                self.button = .null
            } else {
                self.button = .value(try container.decode(String.self, forKey: .button))
            }
            if !container.contains(.clickCount) {
                self.clickCount = .missing
            } else if try container.decodeNil(forKey: .clickCount) {
                self.clickCount = .null
            } else {
                self.clickCount = .value(try container.decode(UInt32.self, forKey: .clickCount))
            }
            self.frameSeq = try container.decode(UInt64.self, forKey: .frameSeq)
            self.kind = try container.decode(CloudTuiGenerated.BrowserMouseGuardedRequestKind.self, forKey: .kind)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            self.xPx = try container.decode(Double.self, forKey: .xPx)
            self.yPx = try container.decode(Double.self, forKey: .yPx)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch button {
            case .missing: break
            case .null: try container.encodeNil(forKey: .button)
            case .value(let value): try container.encode(value, forKey: .button)
            }
            switch clickCount {
            case .missing: break
            case .null: try container.encodeNil(forKey: .clickCount)
            case .value(let value): try container.encode(value, forKey: .clickCount)
            }
            try container.encode(frameSeq, forKey: .frameSeq)
            try container.encode(kind, forKey: .kind)
            try container.encode(surface, forKey: .surface)
            try container.encode(xPx, forKey: .xPx)
            try container.encode(yPx, forKey: .yPx)
        }
    }

    struct BrowserNavigateRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id
        let url: String

        init(surface: CloudTuiGenerated.Id, url: String) {
            self.surface = surface
            self.url = url
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
            case url = "url"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            self.url = try container.decode(String.self, forKey: .url)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
            try container.encode(url, forKey: .url)
        }
    }

    struct BrowserReloadRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id

        init(surface: CloudTuiGenerated.Id) {
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct BrowserWheelRequest: Codable, Sendable, Equatable {
        let deltaYPx: Double
        let frameSeq: CloudTuiGenerated.OptionalField<UInt64>
        let surface: CloudTuiGenerated.Id
        let xPx: Double
        let yPx: Double

        init(deltaYPx: Double, frameSeq: CloudTuiGenerated.OptionalField<UInt64> = .missing, surface: CloudTuiGenerated.Id, xPx: Double, yPx: Double) {
            self.deltaYPx = deltaYPx
            self.frameSeq = frameSeq
            self.surface = surface
            self.xPx = xPx
            self.yPx = yPx
        }

        enum CodingKeys: String, CodingKey {
            case deltaYPx = "delta_y_px"
            case frameSeq = "frame_seq"
            case surface = "surface"
            case xPx = "x_px"
            case yPx = "y_px"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.deltaYPx = try container.decode(Double.self, forKey: .deltaYPx)
            if !container.contains(.frameSeq) {
                self.frameSeq = .missing
            } else if try container.decodeNil(forKey: .frameSeq) {
                self.frameSeq = .null
            } else {
                self.frameSeq = .value(try container.decode(UInt64.self, forKey: .frameSeq))
            }
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            self.xPx = try container.decode(Double.self, forKey: .xPx)
            self.yPx = try container.decode(Double.self, forKey: .yPx)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(deltaYPx, forKey: .deltaYPx)
            switch frameSeq {
            case .missing: break
            case .null: try container.encodeNil(forKey: .frameSeq)
            case .value(let value): try container.encode(value, forKey: .frameSeq)
            }
            try container.encode(surface, forKey: .surface)
            try container.encode(xPx, forKey: .xPx)
            try container.encode(yPx, forKey: .yPx)
        }
    }

    struct BrowserWheelGuardedRequest: Codable, Sendable, Equatable {
        let deltaYPx: Double
        let frameSeq: UInt64
        let surface: CloudTuiGenerated.Id
        let xPx: Double
        let yPx: Double

        init(deltaYPx: Double, frameSeq: UInt64, surface: CloudTuiGenerated.Id, xPx: Double, yPx: Double) {
            self.deltaYPx = deltaYPx
            self.frameSeq = frameSeq
            self.surface = surface
            self.xPx = xPx
            self.yPx = yPx
        }

        enum CodingKeys: String, CodingKey {
            case deltaYPx = "delta_y_px"
            case frameSeq = "frame_seq"
            case surface = "surface"
            case xPx = "x_px"
            case yPx = "y_px"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.deltaYPx = try container.decode(Double.self, forKey: .deltaYPx)
            self.frameSeq = try container.decode(UInt64.self, forKey: .frameSeq)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            self.xPx = try container.decode(Double.self, forKey: .xPx)
            self.yPx = try container.decode(Double.self, forKey: .yPx)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(deltaYPx, forKey: .deltaYPx)
            try container.encode(frameSeq, forKey: .frameSeq)
            try container.encode(surface, forKey: .surface)
            try container.encode(xPx, forKey: .xPx)
            try container.encode(yPx, forKey: .yPx)
        }
    }

    struct ClearHistoryRequest: Codable, Sendable, Equatable {
        let fallbackKey: CloudTuiGenerated.OptionalField<CloudTuiGenerated.TerminalKeyInput>
        let surface: CloudTuiGenerated.Id

        init(fallbackKey: CloudTuiGenerated.OptionalField<CloudTuiGenerated.TerminalKeyInput> = .missing, surface: CloudTuiGenerated.Id) {
            self.fallbackKey = fallbackKey
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case fallbackKey = "fallback_key"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.fallbackKey) {
                self.fallbackKey = .missing
            } else if try container.decodeNil(forKey: .fallbackKey) {
                self.fallbackKey = .null
            } else {
                self.fallbackKey = .value(try container.decode(CloudTuiGenerated.TerminalKeyInput.self, forKey: .fallbackKey))
            }
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch fallbackKey {
            case .missing: break
            case .null: try container.encodeNil(forKey: .fallbackKey)
            case .value(let value): try container.encode(value, forKey: .fallbackKey)
            }
            try container.encode(surface, forKey: .surface)
        }
    }

    struct ClearWindowTitleRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct ClientFocusRequest: Codable, Sendable, Equatable {
        let clientId: String

        init(clientId: String) {
            self.clientId = clientId
        }

        enum CodingKeys: String, CodingKey {
            case clientId = "client_id"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.clientId = try container.decode(String.self, forKey: .clientId)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(clientId, forKey: .clientId)
        }
    }

    struct ClosePaneRequest: Codable, Sendable, Equatable {
        let pane: CloudTuiGenerated.Id

        init(pane: CloudTuiGenerated.Id) {
            self.pane = pane
        }

        enum CodingKeys: String, CodingKey {
            case pane = "pane"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(pane, forKey: .pane)
        }
    }

    struct CloseProviderManagedWorkspaceRequest: Codable, Sendable, Equatable {
        let authority: String
        let key: String
        let workspace: CloudTuiGenerated.Id

        init(authority: String, key: String, workspace: CloudTuiGenerated.Id) {
            self.authority = authority
            self.key = key
            self.workspace = workspace
        }

        enum CodingKeys: String, CodingKey {
            case authority = "authority"
            case key = "key"
            case workspace = "workspace"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.authority = try container.decode(String.self, forKey: .authority)
            self.key = try container.decode(String.self, forKey: .key)
            self.workspace = try container.decode(CloudTuiGenerated.Id.self, forKey: .workspace)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(authority, forKey: .authority)
            try container.encode(key, forKey: .key)
            try container.encode(workspace, forKey: .workspace)
        }
    }

    struct CloseScreenRequest: Codable, Sendable, Equatable {
        let screen: CloudTuiGenerated.Id

        init(screen: CloudTuiGenerated.Id) {
            self.screen = screen
        }

        enum CodingKeys: String, CodingKey {
            case screen = "screen"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.screen = try container.decode(CloudTuiGenerated.Id.self, forKey: .screen)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(screen, forKey: .screen)
        }
    }

    struct CloseSurfaceRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id

        init(surface: CloudTuiGenerated.Id) {
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct CloseTerminalRequest: Codable, Sendable, Equatable {
        let expectedGeneration: CloudTuiGenerated.OptionalField<String>
        let expectedRevision: CloudTuiGenerated.OptionalField<UInt64>
        let mutationId: CloudTuiGenerated.OptionalField<String>
        let origin: CloudTuiGenerated.OptionalField<String>
        let terminalId: String
        let terminalIncarnation: CloudTuiGenerated.OptionalField<String>

        init(expectedGeneration: CloudTuiGenerated.OptionalField<String> = .missing, expectedRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing, mutationId: CloudTuiGenerated.OptionalField<String> = .missing, origin: CloudTuiGenerated.OptionalField<String> = .missing, terminalId: String, terminalIncarnation: CloudTuiGenerated.OptionalField<String> = .missing) {
            self.expectedGeneration = expectedGeneration
            self.expectedRevision = expectedRevision
            self.mutationId = mutationId
            self.origin = origin
            self.terminalId = terminalId
            self.terminalIncarnation = terminalIncarnation
        }

        enum CodingKeys: String, CodingKey {
            case expectedGeneration = "expected_generation"
            case expectedRevision = "expected_revision"
            case mutationId = "mutation_id"
            case origin = "origin"
            case terminalId = "terminal_id"
            case terminalIncarnation = "terminal_incarnation"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.expectedGeneration) {
                self.expectedGeneration = .missing
            } else if try container.decodeNil(forKey: .expectedGeneration) {
                self.expectedGeneration = .null
            } else {
                self.expectedGeneration = .value(try container.decode(String.self, forKey: .expectedGeneration))
            }
            if !container.contains(.expectedRevision) {
                self.expectedRevision = .missing
            } else if try container.decodeNil(forKey: .expectedRevision) {
                self.expectedRevision = .null
            } else {
                self.expectedRevision = .value(try container.decode(UInt64.self, forKey: .expectedRevision))
            }
            if !container.contains(.mutationId) {
                self.mutationId = .missing
            } else if try container.decodeNil(forKey: .mutationId) {
                self.mutationId = .null
            } else {
                self.mutationId = .value(try container.decode(String.self, forKey: .mutationId))
            }
            if !container.contains(.origin) {
                self.origin = .missing
            } else if try container.decodeNil(forKey: .origin) {
                self.origin = .null
            } else {
                self.origin = .value(try container.decode(String.self, forKey: .origin))
            }
            self.terminalId = try container.decode(String.self, forKey: .terminalId)
            if !container.contains(.terminalIncarnation) {
                self.terminalIncarnation = .missing
            } else if try container.decodeNil(forKey: .terminalIncarnation) {
                self.terminalIncarnation = .null
            } else {
                self.terminalIncarnation = .value(try container.decode(String.self, forKey: .terminalIncarnation))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch expectedGeneration {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedGeneration)
            case .value(let value): try container.encode(value, forKey: .expectedGeneration)
            }
            switch expectedRevision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedRevision)
            case .value(let value): try container.encode(value, forKey: .expectedRevision)
            }
            switch mutationId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mutationId)
            case .value(let value): try container.encode(value, forKey: .mutationId)
            }
            switch origin {
            case .missing: break
            case .null: try container.encodeNil(forKey: .origin)
            case .value(let value): try container.encode(value, forKey: .origin)
            }
            try container.encode(terminalId, forKey: .terminalId)
            switch terminalIncarnation {
            case .missing: break
            case .null: try container.encodeNil(forKey: .terminalIncarnation)
            case .value(let value): try container.encode(value, forKey: .terminalIncarnation)
            }
        }
    }

    struct CloseWorkspaceRequest: Codable, Sendable, Equatable {
        let expectedGeneration: CloudTuiGenerated.OptionalField<String>
        let expectedRevision: CloudTuiGenerated.OptionalField<UInt64>
        let key: CloudTuiGenerated.OptionalField<String>
        let mutationId: CloudTuiGenerated.OptionalField<String>
        let origin: CloudTuiGenerated.OptionalField<String>
        let workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(expectedGeneration: CloudTuiGenerated.OptionalField<String> = .missing, expectedRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing, key: CloudTuiGenerated.OptionalField<String> = .missing, mutationId: CloudTuiGenerated.OptionalField<String> = .missing, origin: CloudTuiGenerated.OptionalField<String> = .missing, workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.expectedGeneration = expectedGeneration
            self.expectedRevision = expectedRevision
            self.key = key
            self.mutationId = mutationId
            self.origin = origin
            self.workspace = workspace
        }

        enum CodingKeys: String, CodingKey {
            case expectedGeneration = "expected_generation"
            case expectedRevision = "expected_revision"
            case key = "key"
            case mutationId = "mutation_id"
            case origin = "origin"
            case workspace = "workspace"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.expectedGeneration) {
                self.expectedGeneration = .missing
            } else if try container.decodeNil(forKey: .expectedGeneration) {
                self.expectedGeneration = .null
            } else {
                self.expectedGeneration = .value(try container.decode(String.self, forKey: .expectedGeneration))
            }
            if !container.contains(.expectedRevision) {
                self.expectedRevision = .missing
            } else if try container.decodeNil(forKey: .expectedRevision) {
                self.expectedRevision = .null
            } else {
                self.expectedRevision = .value(try container.decode(UInt64.self, forKey: .expectedRevision))
            }
            if !container.contains(.key) {
                self.key = .missing
            } else if try container.decodeNil(forKey: .key) {
                self.key = .null
            } else {
                self.key = .value(try container.decode(String.self, forKey: .key))
            }
            if !container.contains(.mutationId) {
                self.mutationId = .missing
            } else if try container.decodeNil(forKey: .mutationId) {
                self.mutationId = .null
            } else {
                self.mutationId = .value(try container.decode(String.self, forKey: .mutationId))
            }
            if !container.contains(.origin) {
                self.origin = .missing
            } else if try container.decodeNil(forKey: .origin) {
                self.origin = .null
            } else {
                self.origin = .value(try container.decode(String.self, forKey: .origin))
            }
            if !container.contains(.workspace) {
                self.workspace = .missing
            } else if try container.decodeNil(forKey: .workspace) {
                self.workspace = .null
            } else {
                self.workspace = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .workspace))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch expectedGeneration {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedGeneration)
            case .value(let value): try container.encode(value, forKey: .expectedGeneration)
            }
            switch expectedRevision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedRevision)
            case .value(let value): try container.encode(value, forKey: .expectedRevision)
            }
            switch key {
            case .missing: break
            case .null: try container.encodeNil(forKey: .key)
            case .value(let value): try container.encode(value, forKey: .key)
            }
            switch mutationId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mutationId)
            case .value(let value): try container.encode(value, forKey: .mutationId)
            }
            switch origin {
            case .missing: break
            case .null: try container.encodeNil(forKey: .origin)
            case .value(let value): try container.encode(value, forKey: .origin)
            }
            switch workspace {
            case .missing: break
            case .null: try container.encodeNil(forKey: .workspace)
            case .value(let value): try container.encode(value, forKey: .workspace)
            }
        }
    }

    struct CopyRequest: Codable, Sendable, Equatable {
        let mode: CloudTuiGenerated.CopyRequestMode
        let surface: CloudTuiGenerated.Id

        init(mode: CloudTuiGenerated.CopyRequestMode, surface: CloudTuiGenerated.Id) {
            self.mode = mode
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case mode = "mode"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.mode = try container.decode(CloudTuiGenerated.CopyRequestMode.self, forKey: .mode)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(mode, forKey: .mode)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct CreateSurfaceWithReceiptRequest: Codable, Sendable, Equatable {
        let argv: CloudTuiGenerated.OptionalField<[String]>
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let cwd: CloudTuiGenerated.OptionalField<String>
        let idempotencyKey: CloudTuiGenerated.OptionalField<String>
        let operation: String
        let origin: String
        let pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>
        let receipt: String
        let rows: CloudTuiGenerated.OptionalField<UInt16>
        let selectorFallbacks: CloudTuiGenerated.OptionalField<[CloudTuiGenerated.ResourceSelectors]>
        let selectors: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ResourceSelectors>
        let url: CloudTuiGenerated.OptionalField<String>
        let width: CloudTuiGenerated.OptionalField<Float>
        let workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(argv: CloudTuiGenerated.OptionalField<[String]> = .missing, cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, cwd: CloudTuiGenerated.OptionalField<String> = .missing, idempotencyKey: CloudTuiGenerated.OptionalField<String> = .missing, operation: String, origin: String, pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing, receipt: String, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing, selectorFallbacks: CloudTuiGenerated.OptionalField<[CloudTuiGenerated.ResourceSelectors]> = .missing, selectors: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ResourceSelectors> = .missing, url: CloudTuiGenerated.OptionalField<String> = .missing, width: CloudTuiGenerated.OptionalField<Float> = .missing, workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.argv = argv
            self.cols = cols
            self.cwd = cwd
            self.idempotencyKey = idempotencyKey
            self.operation = operation
            self.origin = origin
            self.pane = pane
            self.receipt = receipt
            self.rows = rows
            self.selectorFallbacks = selectorFallbacks
            self.selectors = selectors
            self.url = url
            self.width = width
            self.workspace = workspace
        }

        enum CodingKeys: String, CodingKey {
            case argv = "argv"
            case cols = "cols"
            case cwd = "cwd"
            case idempotencyKey = "idempotency_key"
            case operation = "operation"
            case origin = "origin"
            case pane = "pane"
            case receipt = "receipt"
            case rows = "rows"
            case selectorFallbacks = "selector_fallbacks"
            case selectors = "selectors"
            case url = "url"
            case width = "width"
            case workspace = "workspace"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.argv) {
                self.argv = .missing
            } else if try container.decodeNil(forKey: .argv) {
                self.argv = .null
            } else {
                self.argv = .value(try container.decode([String].self, forKey: .argv))
            }
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            if !container.contains(.cwd) {
                self.cwd = .missing
            } else if try container.decodeNil(forKey: .cwd) {
                self.cwd = .null
            } else {
                self.cwd = .value(try container.decode(String.self, forKey: .cwd))
            }
            if !container.contains(.idempotencyKey) {
                self.idempotencyKey = .missing
            } else if try container.decodeNil(forKey: .idempotencyKey) {
                self.idempotencyKey = .null
            } else {
                self.idempotencyKey = .value(try container.decode(String.self, forKey: .idempotencyKey))
            }
            self.operation = try container.decode(String.self, forKey: .operation)
            self.origin = try container.decode(String.self, forKey: .origin)
            if !container.contains(.pane) {
                self.pane = .missing
            } else if try container.decodeNil(forKey: .pane) {
                self.pane = .null
            } else {
                self.pane = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .pane))
            }
            self.receipt = try container.decode(String.self, forKey: .receipt)
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
            if !container.contains(.selectorFallbacks) {
                self.selectorFallbacks = .missing
            } else if try container.decodeNil(forKey: .selectorFallbacks) {
                throw CodecError.invalidLiteral("selector_fallbacks")
            } else {
                self.selectorFallbacks = .value(try container.decode([CloudTuiGenerated.ResourceSelectors].self, forKey: .selectorFallbacks))
            }
            if !container.contains(.selectors) {
                self.selectors = .missing
            } else if try container.decodeNil(forKey: .selectors) {
                self.selectors = .null
            } else {
                self.selectors = .value(try container.decode(CloudTuiGenerated.ResourceSelectors.self, forKey: .selectors))
            }
            if !container.contains(.url) {
                self.url = .missing
            } else if try container.decodeNil(forKey: .url) {
                self.url = .null
            } else {
                self.url = .value(try container.decode(String.self, forKey: .url))
            }
            if !container.contains(.width) {
                self.width = .missing
            } else if try container.decodeNil(forKey: .width) {
                self.width = .null
            } else {
                self.width = .value(try container.decode(Float.self, forKey: .width))
            }
            if !container.contains(.workspace) {
                self.workspace = .missing
            } else if try container.decodeNil(forKey: .workspace) {
                self.workspace = .null
            } else {
                self.workspace = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .workspace))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch argv {
            case .missing: break
            case .null: try container.encodeNil(forKey: .argv)
            case .value(let value): try container.encode(value, forKey: .argv)
            }
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            switch cwd {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cwd)
            case .value(let value): try container.encode(value, forKey: .cwd)
            }
            switch idempotencyKey {
            case .missing: break
            case .null: try container.encodeNil(forKey: .idempotencyKey)
            case .value(let value): try container.encode(value, forKey: .idempotencyKey)
            }
            try container.encode(operation, forKey: .operation)
            try container.encode(origin, forKey: .origin)
            switch pane {
            case .missing: break
            case .null: try container.encodeNil(forKey: .pane)
            case .value(let value): try container.encode(value, forKey: .pane)
            }
            try container.encode(receipt, forKey: .receipt)
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
            switch selectorFallbacks {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("selector_fallbacks")
            case .value(let value): try container.encode(value, forKey: .selectorFallbacks)
            }
            switch selectors {
            case .missing: break
            case .null: try container.encodeNil(forKey: .selectors)
            case .value(let value): try container.encode(value, forKey: .selectors)
            }
            switch url {
            case .missing: break
            case .null: try container.encodeNil(forKey: .url)
            case .value(let value): try container.encode(value, forKey: .url)
            }
            switch width {
            case .missing: break
            case .null: try container.encodeNil(forKey: .width)
            case .value(let value): try container.encode(value, forKey: .width)
            }
            switch workspace {
            case .missing: break
            case .null: try container.encodeNil(forKey: .workspace)
            case .value(let value): try container.encode(value, forKey: .workspace)
            }
        }
    }

    struct CreateTerminalRequest: Codable, Sendable, Equatable {
        let argv: CloudTuiGenerated.OptionalField<[String]>
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let command: CloudTuiGenerated.OptionalField<String>
        let cwd: CloudTuiGenerated.OptionalField<String>
        let expectedGeneration: CloudTuiGenerated.OptionalField<String>
        let expectedRevision: CloudTuiGenerated.OptionalField<UInt64>
        let key: CloudTuiGenerated.OptionalField<String>
        let mutationId: CloudTuiGenerated.OptionalField<String>
        let name: CloudTuiGenerated.OptionalField<String>
        let origin: CloudTuiGenerated.OptionalField<String>
        let rows: CloudTuiGenerated.OptionalField<UInt16>
        let terminalId: CloudTuiGenerated.OptionalField<String>
        let workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(argv: CloudTuiGenerated.OptionalField<[String]> = .missing, cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, command: CloudTuiGenerated.OptionalField<String> = .missing, cwd: CloudTuiGenerated.OptionalField<String> = .missing, expectedGeneration: CloudTuiGenerated.OptionalField<String> = .missing, expectedRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing, key: CloudTuiGenerated.OptionalField<String> = .missing, mutationId: CloudTuiGenerated.OptionalField<String> = .missing, name: CloudTuiGenerated.OptionalField<String> = .missing, origin: CloudTuiGenerated.OptionalField<String> = .missing, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing, terminalId: CloudTuiGenerated.OptionalField<String> = .missing, workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.argv = argv
            self.cols = cols
            self.command = command
            self.cwd = cwd
            self.expectedGeneration = expectedGeneration
            self.expectedRevision = expectedRevision
            self.key = key
            self.mutationId = mutationId
            self.name = name
            self.origin = origin
            self.rows = rows
            self.terminalId = terminalId
            self.workspace = workspace
        }

        enum CodingKeys: String, CodingKey {
            case argv = "argv"
            case cols = "cols"
            case command = "command"
            case cwd = "cwd"
            case expectedGeneration = "expected_generation"
            case expectedRevision = "expected_revision"
            case key = "key"
            case mutationId = "mutation_id"
            case name = "name"
            case origin = "origin"
            case rows = "rows"
            case terminalId = "terminal_id"
            case workspace = "workspace"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.argv) {
                self.argv = .missing
            } else if try container.decodeNil(forKey: .argv) {
                self.argv = .null
            } else {
                self.argv = .value(try container.decode([String].self, forKey: .argv))
            }
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            if !container.contains(.command) {
                self.command = .missing
            } else if try container.decodeNil(forKey: .command) {
                self.command = .null
            } else {
                self.command = .value(try container.decode(String.self, forKey: .command))
            }
            if !container.contains(.cwd) {
                self.cwd = .missing
            } else if try container.decodeNil(forKey: .cwd) {
                self.cwd = .null
            } else {
                self.cwd = .value(try container.decode(String.self, forKey: .cwd))
            }
            if !container.contains(.expectedGeneration) {
                self.expectedGeneration = .missing
            } else if try container.decodeNil(forKey: .expectedGeneration) {
                self.expectedGeneration = .null
            } else {
                self.expectedGeneration = .value(try container.decode(String.self, forKey: .expectedGeneration))
            }
            if !container.contains(.expectedRevision) {
                self.expectedRevision = .missing
            } else if try container.decodeNil(forKey: .expectedRevision) {
                self.expectedRevision = .null
            } else {
                self.expectedRevision = .value(try container.decode(UInt64.self, forKey: .expectedRevision))
            }
            if !container.contains(.key) {
                self.key = .missing
            } else if try container.decodeNil(forKey: .key) {
                self.key = .null
            } else {
                self.key = .value(try container.decode(String.self, forKey: .key))
            }
            if !container.contains(.mutationId) {
                self.mutationId = .missing
            } else if try container.decodeNil(forKey: .mutationId) {
                self.mutationId = .null
            } else {
                self.mutationId = .value(try container.decode(String.self, forKey: .mutationId))
            }
            if !container.contains(.name) {
                self.name = .missing
            } else if try container.decodeNil(forKey: .name) {
                self.name = .null
            } else {
                self.name = .value(try container.decode(String.self, forKey: .name))
            }
            if !container.contains(.origin) {
                self.origin = .missing
            } else if try container.decodeNil(forKey: .origin) {
                self.origin = .null
            } else {
                self.origin = .value(try container.decode(String.self, forKey: .origin))
            }
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
            if !container.contains(.terminalId) {
                self.terminalId = .missing
            } else if try container.decodeNil(forKey: .terminalId) {
                self.terminalId = .null
            } else {
                self.terminalId = .value(try container.decode(String.self, forKey: .terminalId))
            }
            if !container.contains(.workspace) {
                self.workspace = .missing
            } else if try container.decodeNil(forKey: .workspace) {
                self.workspace = .null
            } else {
                self.workspace = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .workspace))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch argv {
            case .missing: break
            case .null: try container.encodeNil(forKey: .argv)
            case .value(let value): try container.encode(value, forKey: .argv)
            }
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            switch command {
            case .missing: break
            case .null: try container.encodeNil(forKey: .command)
            case .value(let value): try container.encode(value, forKey: .command)
            }
            switch cwd {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cwd)
            case .value(let value): try container.encode(value, forKey: .cwd)
            }
            switch expectedGeneration {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedGeneration)
            case .value(let value): try container.encode(value, forKey: .expectedGeneration)
            }
            switch expectedRevision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedRevision)
            case .value(let value): try container.encode(value, forKey: .expectedRevision)
            }
            switch key {
            case .missing: break
            case .null: try container.encodeNil(forKey: .key)
            case .value(let value): try container.encode(value, forKey: .key)
            }
            switch mutationId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mutationId)
            case .value(let value): try container.encode(value, forKey: .mutationId)
            }
            switch name {
            case .missing: break
            case .null: try container.encodeNil(forKey: .name)
            case .value(let value): try container.encode(value, forKey: .name)
            }
            switch origin {
            case .missing: break
            case .null: try container.encodeNil(forKey: .origin)
            case .value(let value): try container.encode(value, forKey: .origin)
            }
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
            switch terminalId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .terminalId)
            case .value(let value): try container.encode(value, forKey: .terminalId)
            }
            switch workspace {
            case .missing: break
            case .null: try container.encodeNil(forKey: .workspace)
            case .value(let value): try container.encode(value, forKey: .workspace)
            }
        }
    }

    struct CreateWorkspaceRequest: Codable, Sendable, Equatable {
        let expectedGeneration: CloudTuiGenerated.OptionalField<String>
        let expectedRevision: CloudTuiGenerated.OptionalField<UInt64>
        let key: CloudTuiGenerated.OptionalField<String>
        let mutationId: CloudTuiGenerated.OptionalField<String>
        let name: CloudTuiGenerated.OptionalField<String>
        let origin: CloudTuiGenerated.OptionalField<String>

        init(expectedGeneration: CloudTuiGenerated.OptionalField<String> = .missing, expectedRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing, key: CloudTuiGenerated.OptionalField<String> = .missing, mutationId: CloudTuiGenerated.OptionalField<String> = .missing, name: CloudTuiGenerated.OptionalField<String> = .missing, origin: CloudTuiGenerated.OptionalField<String> = .missing) {
            self.expectedGeneration = expectedGeneration
            self.expectedRevision = expectedRevision
            self.key = key
            self.mutationId = mutationId
            self.name = name
            self.origin = origin
        }

        enum CodingKeys: String, CodingKey {
            case expectedGeneration = "expected_generation"
            case expectedRevision = "expected_revision"
            case key = "key"
            case mutationId = "mutation_id"
            case name = "name"
            case origin = "origin"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.expectedGeneration) {
                self.expectedGeneration = .missing
            } else if try container.decodeNil(forKey: .expectedGeneration) {
                self.expectedGeneration = .null
            } else {
                self.expectedGeneration = .value(try container.decode(String.self, forKey: .expectedGeneration))
            }
            if !container.contains(.expectedRevision) {
                self.expectedRevision = .missing
            } else if try container.decodeNil(forKey: .expectedRevision) {
                self.expectedRevision = .null
            } else {
                self.expectedRevision = .value(try container.decode(UInt64.self, forKey: .expectedRevision))
            }
            if !container.contains(.key) {
                self.key = .missing
            } else if try container.decodeNil(forKey: .key) {
                self.key = .null
            } else {
                self.key = .value(try container.decode(String.self, forKey: .key))
            }
            if !container.contains(.mutationId) {
                self.mutationId = .missing
            } else if try container.decodeNil(forKey: .mutationId) {
                self.mutationId = .null
            } else {
                self.mutationId = .value(try container.decode(String.self, forKey: .mutationId))
            }
            if !container.contains(.name) {
                self.name = .missing
            } else if try container.decodeNil(forKey: .name) {
                self.name = .null
            } else {
                self.name = .value(try container.decode(String.self, forKey: .name))
            }
            if !container.contains(.origin) {
                self.origin = .missing
            } else if try container.decodeNil(forKey: .origin) {
                self.origin = .null
            } else {
                self.origin = .value(try container.decode(String.self, forKey: .origin))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch expectedGeneration {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedGeneration)
            case .value(let value): try container.encode(value, forKey: .expectedGeneration)
            }
            switch expectedRevision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedRevision)
            case .value(let value): try container.encode(value, forKey: .expectedRevision)
            }
            switch key {
            case .missing: break
            case .null: try container.encodeNil(forKey: .key)
            case .value(let value): try container.encode(value, forKey: .key)
            }
            switch mutationId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mutationId)
            case .value(let value): try container.encode(value, forKey: .mutationId)
            }
            switch name {
            case .missing: break
            case .null: try container.encodeNil(forKey: .name)
            case .value(let value): try container.encode(value, forKey: .name)
            }
            switch origin {
            case .missing: break
            case .null: try container.encodeNil(forKey: .origin)
            case .value(let value): try container.encode(value, forKey: .origin)
            }
        }
    }

    struct DetachAttachedViewRequest: Codable, Sendable, Equatable {
        let lease: String
        let surface: CloudTuiGenerated.Id

        init(lease: String, surface: CloudTuiGenerated.Id) {
            self.lease = lease
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case lease = "lease"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.lease = try container.decode(String.self, forKey: .lease)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(lease, forKey: .lease)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct DetachClientRequest: Codable, Sendable, Equatable {
        let client: UInt64

        init(client: UInt64) {
            self.client = client
        }

        enum CodingKeys: String, CodingKey {
            case client = "client"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.client = try container.decode(UInt64.self, forKey: .client)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(client, forKey: .client)
        }
    }

    struct ExportLayoutRequest: Codable, Sendable, Equatable {
        let screen: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(screen: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.screen = screen
        }

        enum CodingKeys: String, CodingKey {
            case screen = "screen"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.screen) {
                self.screen = .missing
            } else if try container.decodeNil(forKey: .screen) {
                self.screen = .null
            } else {
                self.screen = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .screen))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch screen {
            case .missing: break
            case .null: try container.encodeNil(forKey: .screen)
            case .value(let value): try container.encode(value, forKey: .screen)
            }
        }
    }

    struct FocusDirectionRequest: Codable, Sendable, Equatable {
        let dir: CloudTuiGenerated.PaneDirection
        let pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(dir: CloudTuiGenerated.PaneDirection, pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.dir = dir
            self.pane = pane
        }

        enum CodingKeys: String, CodingKey {
            case dir = "dir"
            case pane = "pane"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.dir = try container.decode(CloudTuiGenerated.PaneDirection.self, forKey: .dir)
            if !container.contains(.pane) {
                self.pane = .missing
            } else if try container.decodeNil(forKey: .pane) {
                self.pane = .null
            } else {
                self.pane = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .pane))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(dir, forKey: .dir)
            switch pane {
            case .missing: break
            case .null: try container.encodeNil(forKey: .pane)
            case .value(let value): try container.encode(value, forKey: .pane)
            }
        }
    }

    struct FocusPaneRequest: Codable, Sendable, Equatable {
        let pane: CloudTuiGenerated.Id

        init(pane: CloudTuiGenerated.Id) {
            self.pane = pane
        }

        enum CodingKeys: String, CodingKey {
            case pane = "pane"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(pane, forKey: .pane)
        }
    }

    struct GetBrowserProviderRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct GetCellPixelsRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct GetFrontendProjectionRequest: Codable, Sendable, Equatable {
        let frontend: String
        let scope: String
        let subjectKey: String

        init(frontend: String, scope: String, subjectKey: String) {
            self.frontend = frontend
            self.scope = scope
            self.subjectKey = subjectKey
        }

        enum CodingKeys: String, CodingKey {
            case frontend = "frontend"
            case scope = "scope"
            case subjectKey = "subject_key"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.frontend = try container.decode(String.self, forKey: .frontend)
            self.scope = try container.decode(String.self, forKey: .scope)
            self.subjectKey = try container.decode(String.self, forKey: .subjectKey)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(frontend, forKey: .frontend)
            try container.encode(scope, forKey: .scope)
            try container.encode(subjectKey, forKey: .subjectKey)
        }
    }

    struct IdentifyRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct IdsRequest: Codable, Sendable, Equatable {
        let kind: CloudTuiGenerated.OptionalField<CloudTuiGenerated.IdsRequestKind>

        init(kind: CloudTuiGenerated.OptionalField<CloudTuiGenerated.IdsRequestKind> = .missing) {
            self.kind = kind
        }

        enum CodingKeys: String, CodingKey {
            case kind = "kind"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.kind) {
                self.kind = .missing
            } else if try container.decodeNil(forKey: .kind) {
                self.kind = .null
            } else {
                self.kind = .value(try container.decode(CloudTuiGenerated.IdsRequestKind.self, forKey: .kind))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch kind {
            case .missing: break
            case .null: try container.encodeNil(forKey: .kind)
            case .value(let value): try container.encode(value, forKey: .kind)
            }
        }
    }

    struct JournalFrontendEventRequest: Codable, Sendable, Equatable {
        let event: CloudTuiGenerated.FrontendJournalEvent

        init(event: CloudTuiGenerated.FrontendJournalEvent) {
            self.event = event
        }

        enum CodingKeys: String, CodingKey {
            case event = "event"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.event = try container.decode(CloudTuiGenerated.FrontendJournalEvent.self, forKey: .event)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(event, forKey: .event)
        }
    }

    struct ListAgentsRequest: Codable, Sendable, Equatable {
        let state: CloudTuiGenerated.OptionalField<CloudTuiGenerated.AgentState>
        let surface: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(state: CloudTuiGenerated.OptionalField<CloudTuiGenerated.AgentState> = .missing, surface: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.state = state
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case state = "state"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.state) {
                self.state = .missing
            } else if try container.decodeNil(forKey: .state) {
                self.state = .null
            } else {
                self.state = .value(try container.decode(CloudTuiGenerated.AgentState.self, forKey: .state))
            }
            if !container.contains(.surface) {
                self.surface = .missing
            } else if try container.decodeNil(forKey: .surface) {
                self.surface = .null
            } else {
                self.surface = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .surface))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch state {
            case .missing: break
            case .null: try container.encodeNil(forKey: .state)
            case .value(let value): try container.encode(value, forKey: .state)
            }
            switch surface {
            case .missing: break
            case .null: try container.encodeNil(forKey: .surface)
            case .value(let value): try container.encode(value, forKey: .surface)
            }
        }
    }

    struct ListClientsRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct ListTerminalsRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct ListWorkspacesRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct MachineListeningTcpRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct MachineUsageRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct MarkWorkspacesProviderManagedRequest: Codable, Sendable, Equatable {
        let authority: String

        init(authority: String) {
            self.authority = authority
        }

        enum CodingKeys: String, CodingKey {
            case authority = "authority"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.authority = try container.decode(String.self, forKey: .authority)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(authority, forKey: .authority)
        }
    }

    struct MintTerminalRendererRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id
        let ttlMs: CloudTuiGenerated.OptionalField<UInt64>

        init(surface: CloudTuiGenerated.Id, ttlMs: CloudTuiGenerated.OptionalField<UInt64> = .missing) {
            self.surface = surface
            self.ttlMs = ttlMs
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
            case ttlMs = "ttl_ms"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            if !container.contains(.ttlMs) {
                self.ttlMs = .missing
            } else if try container.decodeNil(forKey: .ttlMs) {
                throw CodecError.invalidLiteral("ttl_ms")
            } else {
                self.ttlMs = .value(try container.decode(UInt64.self, forKey: .ttlMs))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
            switch ttlMs {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("ttl_ms")
            case .value(let value): try container.encode(value, forKey: .ttlMs)
            }
        }
    }

    struct MintTerminalRendererByTerminalRequest: Codable, Sendable, Equatable {
        let terminal: String
        let ttlMs: CloudTuiGenerated.OptionalField<UInt64>

        init(terminal: String, ttlMs: CloudTuiGenerated.OptionalField<UInt64> = .missing) {
            self.terminal = terminal
            self.ttlMs = ttlMs
        }

        enum CodingKeys: String, CodingKey {
            case terminal = "terminal"
            case ttlMs = "ttl_ms"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.terminal = try container.decode(String.self, forKey: .terminal)
            if !container.contains(.ttlMs) {
                self.ttlMs = .missing
            } else if try container.decodeNil(forKey: .ttlMs) {
                throw CodecError.invalidLiteral("ttl_ms")
            } else {
                self.ttlMs = .value(try container.decode(UInt64.self, forKey: .ttlMs))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(terminal, forKey: .terminal)
            switch ttlMs {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("ttl_ms")
            case .value(let value): try container.encode(value, forKey: .ttlMs)
            }
        }
    }

    struct MoveTabRequest: Codable, Sendable, Equatable {
        let index: UInt64
        let pane: CloudTuiGenerated.Id
        let surface: CloudTuiGenerated.Id

        init(index: UInt64, pane: CloudTuiGenerated.Id, surface: CloudTuiGenerated.Id) {
            self.index = index
            self.pane = pane
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case index = "index"
            case pane = "pane"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.index = try container.decode(UInt64.self, forKey: .index)
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(index, forKey: .index)
            try container.encode(pane, forKey: .pane)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct MoveTerminalRequest: Codable, Sendable, Equatable {
        let expectedGeneration: CloudTuiGenerated.OptionalField<String>
        let expectedRevision: CloudTuiGenerated.OptionalField<UInt64>
        let mutationId: CloudTuiGenerated.OptionalField<String>
        let origin: CloudTuiGenerated.OptionalField<String>
        let terminalId: String
        let terminalIncarnation: CloudTuiGenerated.OptionalField<String>
        let workspaceKey: String

        init(expectedGeneration: CloudTuiGenerated.OptionalField<String> = .missing, expectedRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing, mutationId: CloudTuiGenerated.OptionalField<String> = .missing, origin: CloudTuiGenerated.OptionalField<String> = .missing, terminalId: String, terminalIncarnation: CloudTuiGenerated.OptionalField<String> = .missing, workspaceKey: String) {
            self.expectedGeneration = expectedGeneration
            self.expectedRevision = expectedRevision
            self.mutationId = mutationId
            self.origin = origin
            self.terminalId = terminalId
            self.terminalIncarnation = terminalIncarnation
            self.workspaceKey = workspaceKey
        }

        enum CodingKeys: String, CodingKey {
            case expectedGeneration = "expected_generation"
            case expectedRevision = "expected_revision"
            case mutationId = "mutation_id"
            case origin = "origin"
            case terminalId = "terminal_id"
            case terminalIncarnation = "terminal_incarnation"
            case workspaceKey = "workspace_key"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.expectedGeneration) {
                self.expectedGeneration = .missing
            } else if try container.decodeNil(forKey: .expectedGeneration) {
                self.expectedGeneration = .null
            } else {
                self.expectedGeneration = .value(try container.decode(String.self, forKey: .expectedGeneration))
            }
            if !container.contains(.expectedRevision) {
                self.expectedRevision = .missing
            } else if try container.decodeNil(forKey: .expectedRevision) {
                self.expectedRevision = .null
            } else {
                self.expectedRevision = .value(try container.decode(UInt64.self, forKey: .expectedRevision))
            }
            if !container.contains(.mutationId) {
                self.mutationId = .missing
            } else if try container.decodeNil(forKey: .mutationId) {
                self.mutationId = .null
            } else {
                self.mutationId = .value(try container.decode(String.self, forKey: .mutationId))
            }
            if !container.contains(.origin) {
                self.origin = .missing
            } else if try container.decodeNil(forKey: .origin) {
                self.origin = .null
            } else {
                self.origin = .value(try container.decode(String.self, forKey: .origin))
            }
            self.terminalId = try container.decode(String.self, forKey: .terminalId)
            if !container.contains(.terminalIncarnation) {
                self.terminalIncarnation = .missing
            } else if try container.decodeNil(forKey: .terminalIncarnation) {
                self.terminalIncarnation = .null
            } else {
                self.terminalIncarnation = .value(try container.decode(String.self, forKey: .terminalIncarnation))
            }
            self.workspaceKey = try container.decode(String.self, forKey: .workspaceKey)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch expectedGeneration {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedGeneration)
            case .value(let value): try container.encode(value, forKey: .expectedGeneration)
            }
            switch expectedRevision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedRevision)
            case .value(let value): try container.encode(value, forKey: .expectedRevision)
            }
            switch mutationId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mutationId)
            case .value(let value): try container.encode(value, forKey: .mutationId)
            }
            switch origin {
            case .missing: break
            case .null: try container.encodeNil(forKey: .origin)
            case .value(let value): try container.encode(value, forKey: .origin)
            }
            try container.encode(terminalId, forKey: .terminalId)
            switch terminalIncarnation {
            case .missing: break
            case .null: try container.encodeNil(forKey: .terminalIncarnation)
            case .value(let value): try container.encode(value, forKey: .terminalIncarnation)
            }
            try container.encode(workspaceKey, forKey: .workspaceKey)
        }
    }

    struct MoveWorkspaceRequest: Codable, Sendable, Equatable {
        let expectedGeneration: CloudTuiGenerated.OptionalField<String>
        let expectedRevision: CloudTuiGenerated.OptionalField<UInt64>
        let index: UInt64
        let key: CloudTuiGenerated.OptionalField<String>
        let mutationId: CloudTuiGenerated.OptionalField<String>
        let origin: CloudTuiGenerated.OptionalField<String>
        let workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(expectedGeneration: CloudTuiGenerated.OptionalField<String> = .missing, expectedRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing, index: UInt64, key: CloudTuiGenerated.OptionalField<String> = .missing, mutationId: CloudTuiGenerated.OptionalField<String> = .missing, origin: CloudTuiGenerated.OptionalField<String> = .missing, workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.expectedGeneration = expectedGeneration
            self.expectedRevision = expectedRevision
            self.index = index
            self.key = key
            self.mutationId = mutationId
            self.origin = origin
            self.workspace = workspace
        }

        enum CodingKeys: String, CodingKey {
            case expectedGeneration = "expected_generation"
            case expectedRevision = "expected_revision"
            case index = "index"
            case key = "key"
            case mutationId = "mutation_id"
            case origin = "origin"
            case workspace = "workspace"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.expectedGeneration) {
                self.expectedGeneration = .missing
            } else if try container.decodeNil(forKey: .expectedGeneration) {
                self.expectedGeneration = .null
            } else {
                self.expectedGeneration = .value(try container.decode(String.self, forKey: .expectedGeneration))
            }
            if !container.contains(.expectedRevision) {
                self.expectedRevision = .missing
            } else if try container.decodeNil(forKey: .expectedRevision) {
                self.expectedRevision = .null
            } else {
                self.expectedRevision = .value(try container.decode(UInt64.self, forKey: .expectedRevision))
            }
            self.index = try container.decode(UInt64.self, forKey: .index)
            if !container.contains(.key) {
                self.key = .missing
            } else if try container.decodeNil(forKey: .key) {
                self.key = .null
            } else {
                self.key = .value(try container.decode(String.self, forKey: .key))
            }
            if !container.contains(.mutationId) {
                self.mutationId = .missing
            } else if try container.decodeNil(forKey: .mutationId) {
                self.mutationId = .null
            } else {
                self.mutationId = .value(try container.decode(String.self, forKey: .mutationId))
            }
            if !container.contains(.origin) {
                self.origin = .missing
            } else if try container.decodeNil(forKey: .origin) {
                self.origin = .null
            } else {
                self.origin = .value(try container.decode(String.self, forKey: .origin))
            }
            if !container.contains(.workspace) {
                self.workspace = .missing
            } else if try container.decodeNil(forKey: .workspace) {
                self.workspace = .null
            } else {
                self.workspace = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .workspace))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch expectedGeneration {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedGeneration)
            case .value(let value): try container.encode(value, forKey: .expectedGeneration)
            }
            switch expectedRevision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedRevision)
            case .value(let value): try container.encode(value, forKey: .expectedRevision)
            }
            try container.encode(index, forKey: .index)
            switch key {
            case .missing: break
            case .null: try container.encodeNil(forKey: .key)
            case .value(let value): try container.encode(value, forKey: .key)
            }
            switch mutationId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mutationId)
            case .value(let value): try container.encode(value, forKey: .mutationId)
            }
            switch origin {
            case .missing: break
            case .null: try container.encodeNil(forKey: .origin)
            case .value(let value): try container.encode(value, forKey: .origin)
            }
            switch workspace {
            case .missing: break
            case .null: try container.encodeNil(forKey: .workspace)
            case .value(let value): try container.encode(value, forKey: .workspace)
            }
        }
    }

    struct NewBrowserTabRequest: Codable, Sendable, Equatable {
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>
        let rows: CloudTuiGenerated.OptionalField<UInt16>
        let url: String

        init(cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing, url: String) {
            self.cols = cols
            self.pane = pane
            self.rows = rows
            self.url = url
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case pane = "pane"
            case rows = "rows"
            case url = "url"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            if !container.contains(.pane) {
                self.pane = .missing
            } else if try container.decodeNil(forKey: .pane) {
                self.pane = .null
            } else {
                self.pane = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .pane))
            }
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
            self.url = try container.decode(String.self, forKey: .url)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            switch pane {
            case .missing: break
            case .null: try container.encodeNil(forKey: .pane)
            case .value(let value): try container.encode(value, forKey: .pane)
            }
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
            try container.encode(url, forKey: .url)
        }
    }

    struct NewPaneRequest: Codable, Sendable, Equatable {
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let pane: CloudTuiGenerated.Id
        let rows: CloudTuiGenerated.OptionalField<UInt16>

        init(cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, pane: CloudTuiGenerated.Id, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing) {
            self.cols = cols
            self.pane = pane
            self.rows = rows
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case pane = "pane"
            case rows = "rows"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            try container.encode(pane, forKey: .pane)
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
        }
    }

    struct NewPaneRightRequest: Codable, Sendable, Equatable {
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let pane: CloudTuiGenerated.Id
        let rows: CloudTuiGenerated.OptionalField<UInt16>
        let width: CloudTuiGenerated.OptionalField<Float>

        init(cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, pane: CloudTuiGenerated.Id, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing, width: CloudTuiGenerated.OptionalField<Float> = .missing) {
            self.cols = cols
            self.pane = pane
            self.rows = rows
            self.width = width
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case pane = "pane"
            case rows = "rows"
            case width = "width"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
            if !container.contains(.width) {
                self.width = .missing
            } else if try container.decodeNil(forKey: .width) {
                self.width = .null
            } else {
                self.width = .value(try container.decode(Float.self, forKey: .width))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            try container.encode(pane, forKey: .pane)
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
            switch width {
            case .missing: break
            case .null: try container.encodeNil(forKey: .width)
            case .value(let value): try container.encode(value, forKey: .width)
            }
        }
    }

    struct NewScreenRequest: Codable, Sendable, Equatable {
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let rows: CloudTuiGenerated.OptionalField<UInt16>
        let workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing, workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.cols = cols
            self.rows = rows
            self.workspace = workspace
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case rows = "rows"
            case workspace = "workspace"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
            if !container.contains(.workspace) {
                self.workspace = .missing
            } else if try container.decodeNil(forKey: .workspace) {
                self.workspace = .null
            } else {
                self.workspace = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .workspace))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
            switch workspace {
            case .missing: break
            case .null: try container.encodeNil(forKey: .workspace)
            case .value(let value): try container.encode(value, forKey: .workspace)
            }
        }
    }

    struct NewTabRequest: Codable, Sendable, Equatable {
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let cwd: CloudTuiGenerated.OptionalField<String>
        let pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>
        let rows: CloudTuiGenerated.OptionalField<UInt16>

        init(cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, cwd: CloudTuiGenerated.OptionalField<String> = .missing, pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing) {
            self.cols = cols
            self.cwd = cwd
            self.pane = pane
            self.rows = rows
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case cwd = "cwd"
            case pane = "pane"
            case rows = "rows"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            if !container.contains(.cwd) {
                self.cwd = .missing
            } else if try container.decodeNil(forKey: .cwd) {
                self.cwd = .null
            } else {
                self.cwd = .value(try container.decode(String.self, forKey: .cwd))
            }
            if !container.contains(.pane) {
                self.pane = .missing
            } else if try container.decodeNil(forKey: .pane) {
                self.pane = .null
            } else {
                self.pane = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .pane))
            }
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            switch cwd {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cwd)
            case .value(let value): try container.encode(value, forKey: .cwd)
            }
            switch pane {
            case .missing: break
            case .null: try container.encodeNil(forKey: .pane)
            case .value(let value): try container.encode(value, forKey: .pane)
            }
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
        }
    }

    struct NewWorkspaceRequest: Codable, Sendable, Equatable {
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let name: CloudTuiGenerated.OptionalField<String>
        let rows: CloudTuiGenerated.OptionalField<UInt16>

        init(cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, name: CloudTuiGenerated.OptionalField<String> = .missing, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing) {
            self.cols = cols
            self.name = name
            self.rows = rows
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case name = "name"
            case rows = "rows"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            if !container.contains(.name) {
                self.name = .missing
            } else if try container.decodeNil(forKey: .name) {
                self.name = .null
            } else {
                self.name = .value(try container.decode(String.self, forKey: .name))
            }
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            switch name {
            case .missing: break
            case .null: try container.encodeNil(forKey: .name)
            case .value(let value): try container.encode(value, forKey: .name)
            }
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
        }
    }

    struct NotifyRequest: Codable, Sendable, Equatable {
        let body: String
        let level: CloudTuiGenerated.OptionalField<CloudTuiGenerated.NotificationLevel>
        let surface: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>
        let title: String

        init(body: String, level: CloudTuiGenerated.OptionalField<CloudTuiGenerated.NotificationLevel> = .missing, surface: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing, title: String) {
            self.body = body
            self.level = level
            self.surface = surface
            self.title = title
        }

        enum CodingKeys: String, CodingKey {
            case body = "body"
            case level = "level"
            case surface = "surface"
            case title = "title"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.body = try container.decode(String.self, forKey: .body)
            if !container.contains(.level) {
                self.level = .missing
            } else if try container.decodeNil(forKey: .level) {
                self.level = .null
            } else {
                self.level = .value(try container.decode(CloudTuiGenerated.NotificationLevel.self, forKey: .level))
            }
            if !container.contains(.surface) {
                self.surface = .missing
            } else if try container.decodeNil(forKey: .surface) {
                self.surface = .null
            } else {
                self.surface = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .surface))
            }
            self.title = try container.decode(String.self, forKey: .title)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(body, forKey: .body)
            switch level {
            case .missing: break
            case .null: try container.encodeNil(forKey: .level)
            case .value(let value): try container.encode(value, forKey: .level)
            }
            switch surface {
            case .missing: break
            case .null: try container.encodeNil(forKey: .surface)
            case .value(let value): try container.encode(value, forKey: .surface)
            }
            try container.encode(title, forKey: .title)
        }
    }

    struct PairingResponseRequest: Codable, Sendable, Equatable {
        let approve: Bool
        let request: UInt64

        init(approve: Bool, request: UInt64) {
            self.approve = approve
            self.request = request
        }

        enum CodingKeys: String, CodingKey {
            case approve = "approve"
            case request = "request"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.approve = try container.decode(Bool.self, forKey: .approve)
            self.request = try container.decode(UInt64.self, forKey: .request)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(approve, forKey: .approve)
            try container.encode(request, forKey: .request)
        }
    }

    struct PaneNeighborRequest: Codable, Sendable, Equatable {
        let dir: CloudTuiGenerated.PaneDirection
        let pane: CloudTuiGenerated.Id

        init(dir: CloudTuiGenerated.PaneDirection, pane: CloudTuiGenerated.Id) {
            self.dir = dir
            self.pane = pane
        }

        enum CodingKeys: String, CodingKey {
            case dir = "dir"
            case pane = "pane"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.dir = try container.decode(CloudTuiGenerated.PaneDirection.self, forKey: .dir)
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(dir, forKey: .dir)
            try container.encode(pane, forKey: .pane)
        }
    }

    struct PasteImageRequest: Codable, Sendable, Equatable {
        let data: CloudTuiGenerated.OptionalField<String>
        let lease: String
        let mime: CloudTuiGenerated.OptionalField<String>
        let offset: CloudTuiGenerated.OptionalField<UInt64>
        let op: String
        let size: CloudTuiGenerated.OptionalField<UInt64>
        let surface: CloudTuiGenerated.Id
        let terminalId: String
        let uploadId: String

        init(data: CloudTuiGenerated.OptionalField<String> = .missing, lease: String, mime: CloudTuiGenerated.OptionalField<String> = .missing, offset: CloudTuiGenerated.OptionalField<UInt64> = .missing, op: String, size: CloudTuiGenerated.OptionalField<UInt64> = .missing, surface: CloudTuiGenerated.Id, terminalId: String, uploadId: String) {
            self.data = data
            self.lease = lease
            self.mime = mime
            self.offset = offset
            self.op = op
            self.size = size
            self.surface = surface
            self.terminalId = terminalId
            self.uploadId = uploadId
        }

        enum CodingKeys: String, CodingKey {
            case data = "data"
            case lease = "lease"
            case mime = "mime"
            case offset = "offset"
            case op = "op"
            case size = "size"
            case surface = "surface"
            case terminalId = "terminal_id"
            case uploadId = "upload_id"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.data) {
                self.data = .missing
            } else if try container.decodeNil(forKey: .data) {
                self.data = .null
            } else {
                self.data = .value(try container.decode(String.self, forKey: .data))
            }
            self.lease = try container.decode(String.self, forKey: .lease)
            if !container.contains(.mime) {
                self.mime = .missing
            } else if try container.decodeNil(forKey: .mime) {
                self.mime = .null
            } else {
                self.mime = .value(try container.decode(String.self, forKey: .mime))
            }
            if !container.contains(.offset) {
                self.offset = .missing
            } else if try container.decodeNil(forKey: .offset) {
                self.offset = .null
            } else {
                self.offset = .value(try container.decode(UInt64.self, forKey: .offset))
            }
            self.op = try container.decode(String.self, forKey: .op)
            if !container.contains(.size) {
                self.size = .missing
            } else if try container.decodeNil(forKey: .size) {
                self.size = .null
            } else {
                self.size = .value(try container.decode(UInt64.self, forKey: .size))
            }
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            self.terminalId = try container.decode(String.self, forKey: .terminalId)
            self.uploadId = try container.decode(String.self, forKey: .uploadId)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch data {
            case .missing: break
            case .null: try container.encodeNil(forKey: .data)
            case .value(let value): try container.encode(value, forKey: .data)
            }
            try container.encode(lease, forKey: .lease)
            switch mime {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mime)
            case .value(let value): try container.encode(value, forKey: .mime)
            }
            switch offset {
            case .missing: break
            case .null: try container.encodeNil(forKey: .offset)
            case .value(let value): try container.encode(value, forKey: .offset)
            }
            try container.encode(op, forKey: .op)
            switch size {
            case .missing: break
            case .null: try container.encodeNil(forKey: .size)
            case .value(let value): try container.encode(value, forKey: .size)
            }
            try container.encode(surface, forKey: .surface)
            try container.encode(terminalId, forKey: .terminalId)
            try container.encode(uploadId, forKey: .uploadId)
        }
    }

    struct PingRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct PresenceClearRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct PresenceListRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct PresenceUpdateRequest: Codable, Sendable, Equatable {
        let highlight: CloudTuiGenerated.OptionalField<CloudTuiGenerated.PresenceHighlight>
        let pointer: CloudTuiGenerated.OptionalField<CloudTuiGenerated.PresenceAnchor>
        let surface: CloudTuiGenerated.Id

        init(highlight: CloudTuiGenerated.OptionalField<CloudTuiGenerated.PresenceHighlight> = .missing, pointer: CloudTuiGenerated.OptionalField<CloudTuiGenerated.PresenceAnchor> = .missing, surface: CloudTuiGenerated.Id) {
            self.highlight = highlight
            self.pointer = pointer
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case highlight = "highlight"
            case pointer = "pointer"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.highlight) {
                self.highlight = .missing
            } else if try container.decodeNil(forKey: .highlight) {
                self.highlight = .null
            } else {
                self.highlight = .value(try container.decode(CloudTuiGenerated.PresenceHighlight.self, forKey: .highlight))
            }
            if !container.contains(.pointer) {
                self.pointer = .missing
            } else if try container.decodeNil(forKey: .pointer) {
                self.pointer = .null
            } else {
                self.pointer = .value(try container.decode(CloudTuiGenerated.PresenceAnchor.self, forKey: .pointer))
            }
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch highlight {
            case .missing: break
            case .null: try container.encodeNil(forKey: .highlight)
            case .value(let value): try container.encode(value, forKey: .highlight)
            }
            switch pointer {
            case .missing: break
            case .null: try container.encodeNil(forKey: .pointer)
            case .value(let value): try container.encode(value, forKey: .pointer)
            }
            try container.encode(surface, forKey: .surface)
        }
    }

    struct ProcessInfoRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id

        init(surface: CloudTuiGenerated.Id) {
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct PutFrontendProjectionRequest: Codable, Sendable, Equatable {
        let expectedGeneration: CloudTuiGenerated.OptionalField<String>
        let expectedProjectionRevision: CloudTuiGenerated.OptionalField<UInt64>
        let expectedRevision: CloudTuiGenerated.OptionalField<UInt64>
        let frontend: String
        let mutationId: CloudTuiGenerated.OptionalField<String>
        let origin: CloudTuiGenerated.OptionalField<String>
        let projection: CloudTuiGenerated.JsonValue?
        let schemaVersion: UInt32
        let scope: String
        let subjectKey: String

        init(expectedGeneration: CloudTuiGenerated.OptionalField<String> = .missing, expectedProjectionRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing, expectedRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing, frontend: String, mutationId: CloudTuiGenerated.OptionalField<String> = .missing, origin: CloudTuiGenerated.OptionalField<String> = .missing, projection: CloudTuiGenerated.JsonValue?, schemaVersion: UInt32, scope: String, subjectKey: String) {
            self.expectedGeneration = expectedGeneration
            self.expectedProjectionRevision = expectedProjectionRevision
            self.expectedRevision = expectedRevision
            self.frontend = frontend
            self.mutationId = mutationId
            self.origin = origin
            self.projection = projection
            self.schemaVersion = schemaVersion
            self.scope = scope
            self.subjectKey = subjectKey
        }

        enum CodingKeys: String, CodingKey {
            case expectedGeneration = "expected_generation"
            case expectedProjectionRevision = "expected_projection_revision"
            case expectedRevision = "expected_revision"
            case frontend = "frontend"
            case mutationId = "mutation_id"
            case origin = "origin"
            case projection = "projection"
            case schemaVersion = "schema_version"
            case scope = "scope"
            case subjectKey = "subject_key"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.expectedGeneration) {
                self.expectedGeneration = .missing
            } else if try container.decodeNil(forKey: .expectedGeneration) {
                self.expectedGeneration = .null
            } else {
                self.expectedGeneration = .value(try container.decode(String.self, forKey: .expectedGeneration))
            }
            if !container.contains(.expectedProjectionRevision) {
                self.expectedProjectionRevision = .missing
            } else if try container.decodeNil(forKey: .expectedProjectionRevision) {
                self.expectedProjectionRevision = .null
            } else {
                self.expectedProjectionRevision = .value(try container.decode(UInt64.self, forKey: .expectedProjectionRevision))
            }
            if !container.contains(.expectedRevision) {
                self.expectedRevision = .missing
            } else if try container.decodeNil(forKey: .expectedRevision) {
                self.expectedRevision = .null
            } else {
                self.expectedRevision = .value(try container.decode(UInt64.self, forKey: .expectedRevision))
            }
            self.frontend = try container.decode(String.self, forKey: .frontend)
            if !container.contains(.mutationId) {
                self.mutationId = .missing
            } else if try container.decodeNil(forKey: .mutationId) {
                self.mutationId = .null
            } else {
                self.mutationId = .value(try container.decode(String.self, forKey: .mutationId))
            }
            if !container.contains(.origin) {
                self.origin = .missing
            } else if try container.decodeNil(forKey: .origin) {
                self.origin = .null
            } else {
                self.origin = .value(try container.decode(String.self, forKey: .origin))
            }
            guard container.contains(.projection) else { throw CodecError.missingRequiredField("projection") }
            self.projection = try container.decodeIfPresent(CloudTuiGenerated.JsonValue.self, forKey: .projection)
            self.schemaVersion = try container.decode(UInt32.self, forKey: .schemaVersion)
            self.scope = try container.decode(String.self, forKey: .scope)
            self.subjectKey = try container.decode(String.self, forKey: .subjectKey)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch expectedGeneration {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedGeneration)
            case .value(let value): try container.encode(value, forKey: .expectedGeneration)
            }
            switch expectedProjectionRevision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedProjectionRevision)
            case .value(let value): try container.encode(value, forKey: .expectedProjectionRevision)
            }
            switch expectedRevision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedRevision)
            case .value(let value): try container.encode(value, forKey: .expectedRevision)
            }
            try container.encode(frontend, forKey: .frontend)
            switch mutationId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mutationId)
            case .value(let value): try container.encode(value, forKey: .mutationId)
            }
            switch origin {
            case .missing: break
            case .null: try container.encodeNil(forKey: .origin)
            case .value(let value): try container.encode(value, forKey: .origin)
            }
            try container.encode(projection, forKey: .projection)
            try container.encode(schemaVersion, forKey: .schemaVersion)
            try container.encode(scope, forKey: .scope)
            try container.encode(subjectKey, forKey: .subjectKey)
        }
    }

    struct ReadScreenRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id

        init(surface: CloudTuiGenerated.Id) {
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct ReadScrollbackRequest: Codable, Sendable, Equatable {
        let count: UInt32
        let start: UInt32
        let surface: CloudTuiGenerated.Id

        init(count: UInt32, start: UInt32, surface: CloudTuiGenerated.Id) {
            self.count = count
            self.start = start
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case count = "count"
            case start = "start"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.count = try container.decode(UInt32.self, forKey: .count)
            self.start = try container.decode(UInt32.self, forKey: .start)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(count, forKey: .count)
            try container.encode(start, forKey: .start)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct RegisterBrowserProviderRequest: Codable, Sendable, Equatable {
        let authentication: CloudTuiGenerated.BrowserProviderAuthentication
        let bearerToken: CloudTuiGenerated.OptionalField<String>
        let endpoint: String
        let providerId: String
        let targets: [CloudTuiGenerated.BrowserProviderTarget]

        init(authentication: CloudTuiGenerated.BrowserProviderAuthentication, bearerToken: CloudTuiGenerated.OptionalField<String> = .missing, endpoint: String, providerId: String, targets: [CloudTuiGenerated.BrowserProviderTarget]) {
            self.authentication = authentication
            self.bearerToken = bearerToken
            self.endpoint = endpoint
            self.providerId = providerId
            self.targets = targets
        }

        enum CodingKeys: String, CodingKey {
            case authentication = "authentication"
            case bearerToken = "bearer_token"
            case endpoint = "endpoint"
            case providerId = "provider_id"
            case targets = "targets"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.authentication = try container.decode(CloudTuiGenerated.BrowserProviderAuthentication.self, forKey: .authentication)
            if !container.contains(.bearerToken) {
                self.bearerToken = .missing
            } else if try container.decodeNil(forKey: .bearerToken) {
                self.bearerToken = .null
            } else {
                self.bearerToken = .value(try container.decode(String.self, forKey: .bearerToken))
            }
            self.endpoint = try container.decode(String.self, forKey: .endpoint)
            self.providerId = try container.decode(String.self, forKey: .providerId)
            self.targets = try container.decode([CloudTuiGenerated.BrowserProviderTarget].self, forKey: .targets)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(authentication, forKey: .authentication)
            switch bearerToken {
            case .missing: break
            case .null: try container.encodeNil(forKey: .bearerToken)
            case .value(let value): try container.encode(value, forKey: .bearerToken)
            }
            try container.encode(endpoint, forKey: .endpoint)
            try container.encode(providerId, forKey: .providerId)
            try container.encode(targets, forKey: .targets)
        }
    }

    struct ReleaseAttachedViewSizeRequest: Codable, Sendable, Equatable {
        let lease: String
        let surface: CloudTuiGenerated.Id

        init(lease: String, surface: CloudTuiGenerated.Id) {
            self.lease = lease
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case lease = "lease"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.lease = try container.decode(String.self, forKey: .lease)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(lease, forKey: .lease)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct ReleaseSurfaceSizeRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id

        init(surface: CloudTuiGenerated.Id) {
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct ReloadConfigRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct RenamePaneRequest: Codable, Sendable, Equatable {
        let name: String
        let pane: CloudTuiGenerated.Id

        init(name: String, pane: CloudTuiGenerated.Id) {
            self.name = name
            self.pane = pane
        }

        enum CodingKeys: String, CodingKey {
            case name = "name"
            case pane = "pane"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decode(String.self, forKey: .name)
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(name, forKey: .name)
            try container.encode(pane, forKey: .pane)
        }
    }

    struct RenameProviderManagedWorkspaceRequest: Codable, Sendable, Equatable {
        let authority: String
        let key: String
        let name: String
        let workspace: CloudTuiGenerated.Id

        init(authority: String, key: String, name: String, workspace: CloudTuiGenerated.Id) {
            self.authority = authority
            self.key = key
            self.name = name
            self.workspace = workspace
        }

        enum CodingKeys: String, CodingKey {
            case authority = "authority"
            case key = "key"
            case name = "name"
            case workspace = "workspace"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.authority = try container.decode(String.self, forKey: .authority)
            self.key = try container.decode(String.self, forKey: .key)
            self.name = try container.decode(String.self, forKey: .name)
            self.workspace = try container.decode(CloudTuiGenerated.Id.self, forKey: .workspace)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(authority, forKey: .authority)
            try container.encode(key, forKey: .key)
            try container.encode(name, forKey: .name)
            try container.encode(workspace, forKey: .workspace)
        }
    }

    struct RenameScreenRequest: Codable, Sendable, Equatable {
        let name: String
        let screen: CloudTuiGenerated.Id

        init(name: String, screen: CloudTuiGenerated.Id) {
            self.name = name
            self.screen = screen
        }

        enum CodingKeys: String, CodingKey {
            case name = "name"
            case screen = "screen"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decode(String.self, forKey: .name)
            self.screen = try container.decode(CloudTuiGenerated.Id.self, forKey: .screen)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(name, forKey: .name)
            try container.encode(screen, forKey: .screen)
        }
    }

    struct RenameSurfaceRequest: Codable, Sendable, Equatable {
        let name: String
        let surface: CloudTuiGenerated.Id

        init(name: String, surface: CloudTuiGenerated.Id) {
            self.name = name
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case name = "name"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.name = try container.decode(String.self, forKey: .name)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(name, forKey: .name)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct RenameWorkspaceRequest: Codable, Sendable, Equatable {
        let expectedGeneration: CloudTuiGenerated.OptionalField<String>
        let expectedRevision: CloudTuiGenerated.OptionalField<UInt64>
        let key: CloudTuiGenerated.OptionalField<String>
        let mutationId: CloudTuiGenerated.OptionalField<String>
        let name: String
        let origin: CloudTuiGenerated.OptionalField<String>
        let workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(expectedGeneration: CloudTuiGenerated.OptionalField<String> = .missing, expectedRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing, key: CloudTuiGenerated.OptionalField<String> = .missing, mutationId: CloudTuiGenerated.OptionalField<String> = .missing, name: String, origin: CloudTuiGenerated.OptionalField<String> = .missing, workspace: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.expectedGeneration = expectedGeneration
            self.expectedRevision = expectedRevision
            self.key = key
            self.mutationId = mutationId
            self.name = name
            self.origin = origin
            self.workspace = workspace
        }

        enum CodingKeys: String, CodingKey {
            case expectedGeneration = "expected_generation"
            case expectedRevision = "expected_revision"
            case key = "key"
            case mutationId = "mutation_id"
            case name = "name"
            case origin = "origin"
            case workspace = "workspace"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.expectedGeneration) {
                self.expectedGeneration = .missing
            } else if try container.decodeNil(forKey: .expectedGeneration) {
                self.expectedGeneration = .null
            } else {
                self.expectedGeneration = .value(try container.decode(String.self, forKey: .expectedGeneration))
            }
            if !container.contains(.expectedRevision) {
                self.expectedRevision = .missing
            } else if try container.decodeNil(forKey: .expectedRevision) {
                self.expectedRevision = .null
            } else {
                self.expectedRevision = .value(try container.decode(UInt64.self, forKey: .expectedRevision))
            }
            if !container.contains(.key) {
                self.key = .missing
            } else if try container.decodeNil(forKey: .key) {
                self.key = .null
            } else {
                self.key = .value(try container.decode(String.self, forKey: .key))
            }
            if !container.contains(.mutationId) {
                self.mutationId = .missing
            } else if try container.decodeNil(forKey: .mutationId) {
                self.mutationId = .null
            } else {
                self.mutationId = .value(try container.decode(String.self, forKey: .mutationId))
            }
            self.name = try container.decode(String.self, forKey: .name)
            if !container.contains(.origin) {
                self.origin = .missing
            } else if try container.decodeNil(forKey: .origin) {
                self.origin = .null
            } else {
                self.origin = .value(try container.decode(String.self, forKey: .origin))
            }
            if !container.contains(.workspace) {
                self.workspace = .missing
            } else if try container.decodeNil(forKey: .workspace) {
                self.workspace = .null
            } else {
                self.workspace = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .workspace))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch expectedGeneration {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedGeneration)
            case .value(let value): try container.encode(value, forKey: .expectedGeneration)
            }
            switch expectedRevision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .expectedRevision)
            case .value(let value): try container.encode(value, forKey: .expectedRevision)
            }
            switch key {
            case .missing: break
            case .null: try container.encodeNil(forKey: .key)
            case .value(let value): try container.encode(value, forKey: .key)
            }
            switch mutationId {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mutationId)
            case .value(let value): try container.encode(value, forKey: .mutationId)
            }
            try container.encode(name, forKey: .name)
            switch origin {
            case .missing: break
            case .null: try container.encodeNil(forKey: .origin)
            case .value(let value): try container.encode(value, forKey: .origin)
            }
            switch workspace {
            case .missing: break
            case .null: try container.encodeNil(forKey: .workspace)
            case .value(let value): try container.encode(value, forKey: .workspace)
            }
        }
    }

    struct ReportAgentRequest: Codable, Sendable, Equatable {
        let session: CloudTuiGenerated.OptionalField<String>
        let source: CloudTuiGenerated.AgentReportSource
        let state: CloudTuiGenerated.AgentState
        let surface: CloudTuiGenerated.Id

        init(session: CloudTuiGenerated.OptionalField<String> = .missing, source: CloudTuiGenerated.AgentReportSource, state: CloudTuiGenerated.AgentState, surface: CloudTuiGenerated.Id) {
            self.session = session
            self.source = source
            self.state = state
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case session = "session"
            case source = "source"
            case state = "state"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.session) {
                self.session = .missing
            } else if try container.decodeNil(forKey: .session) {
                self.session = .null
            } else {
                self.session = .value(try container.decode(String.self, forKey: .session))
            }
            self.source = try container.decode(CloudTuiGenerated.AgentReportSource.self, forKey: .source)
            self.state = try container.decode(CloudTuiGenerated.AgentState.self, forKey: .state)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch session {
            case .missing: break
            case .null: try container.encodeNil(forKey: .session)
            case .value(let value): try container.encode(value, forKey: .session)
            }
            try container.encode(source, forKey: .source)
            try container.encode(state, forKey: .state)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct ReportFocusRequest: Codable, Sendable, Equatable {
        let clientId: String
        let pane: CloudTuiGenerated.Id
        let tab: CloudTuiGenerated.OptionalField<UInt64>

        init(clientId: String, pane: CloudTuiGenerated.Id, tab: CloudTuiGenerated.OptionalField<UInt64> = .missing) {
            self.clientId = clientId
            self.pane = pane
            self.tab = tab
        }

        enum CodingKeys: String, CodingKey {
            case clientId = "client_id"
            case pane = "pane"
            case tab = "tab"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.clientId = try container.decode(String.self, forKey: .clientId)
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
            if !container.contains(.tab) {
                self.tab = .missing
            } else if try container.decodeNil(forKey: .tab) {
                self.tab = .null
            } else {
                self.tab = .value(try container.decode(UInt64.self, forKey: .tab))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(clientId, forKey: .clientId)
            try container.encode(pane, forKey: .pane)
            switch tab {
            case .missing: break
            case .null: try container.encodeNil(forKey: .tab)
            case .value(let value): try container.encode(value, forKey: .tab)
            }
        }
    }

    struct ResizeAttachedViewRequest: Codable, Sendable, Equatable {
        let cols: UInt16
        let lease: String
        let rows: UInt16
        let surface: CloudTuiGenerated.Id

        init(cols: UInt16, lease: String, rows: UInt16, surface: CloudTuiGenerated.Id) {
            self.cols = cols
            self.lease = lease
            self.rows = rows
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case lease = "lease"
            case rows = "rows"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.cols = try container.decode(UInt16.self, forKey: .cols)
            self.lease = try container.decode(String.self, forKey: .lease)
            self.rows = try container.decode(UInt16.self, forKey: .rows)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(cols, forKey: .cols)
            try container.encode(lease, forKey: .lease)
            try container.encode(rows, forKey: .rows)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct ResizeSurfaceRequest: Codable, Sendable, Equatable {
        let cols: UInt16
        let rows: UInt16
        let surface: CloudTuiGenerated.Id

        init(cols: UInt16, rows: UInt16, surface: CloudTuiGenerated.Id) {
            self.cols = cols
            self.rows = rows
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case rows = "rows"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.cols = try container.decode(UInt16.self, forKey: .cols)
            self.rows = try container.decode(UInt16.self, forKey: .rows)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(cols, forKey: .cols)
            try container.encode(rows, forKey: .rows)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct ResolveTerminalRequest: Codable, Sendable, Equatable {
        let terminalId: String

        init(terminalId: String) {
            self.terminalId = terminalId
        }

        enum CodingKeys: String, CodingKey {
            case terminalId = "terminal_id"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.terminalId = try container.decode(String.self, forKey: .terminalId)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(terminalId, forKey: .terminalId)
        }
    }

    struct RunRequest: Codable, Sendable, Equatable {
        let argv: CloudTuiGenerated.OptionalField<[String]>
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let command: CloudTuiGenerated.OptionalField<String>
        let cwd: CloudTuiGenerated.OptionalField<String>
        let key: CloudTuiGenerated.OptionalField<String>
        let name: CloudTuiGenerated.OptionalField<String>
        let newWorkspace: CloudTuiGenerated.OptionalField<Bool>
        let pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>
        let rows: CloudTuiGenerated.OptionalField<UInt16>

        init(argv: CloudTuiGenerated.OptionalField<[String]> = .missing, cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, command: CloudTuiGenerated.OptionalField<String> = .missing, cwd: CloudTuiGenerated.OptionalField<String> = .missing, key: CloudTuiGenerated.OptionalField<String> = .missing, name: CloudTuiGenerated.OptionalField<String> = .missing, newWorkspace: CloudTuiGenerated.OptionalField<Bool> = .missing, pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing) {
            self.argv = argv
            self.cols = cols
            self.command = command
            self.cwd = cwd
            self.key = key
            self.name = name
            self.newWorkspace = newWorkspace
            self.pane = pane
            self.rows = rows
        }

        enum CodingKeys: String, CodingKey {
            case argv = "argv"
            case cols = "cols"
            case command = "command"
            case cwd = "cwd"
            case key = "key"
            case name = "name"
            case newWorkspace = "new_workspace"
            case pane = "pane"
            case rows = "rows"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.argv) {
                self.argv = .missing
            } else if try container.decodeNil(forKey: .argv) {
                self.argv = .null
            } else {
                self.argv = .value(try container.decode([String].self, forKey: .argv))
            }
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            if !container.contains(.command) {
                self.command = .missing
            } else if try container.decodeNil(forKey: .command) {
                self.command = .null
            } else {
                self.command = .value(try container.decode(String.self, forKey: .command))
            }
            if !container.contains(.cwd) {
                self.cwd = .missing
            } else if try container.decodeNil(forKey: .cwd) {
                self.cwd = .null
            } else {
                self.cwd = .value(try container.decode(String.self, forKey: .cwd))
            }
            if !container.contains(.key) {
                self.key = .missing
            } else if try container.decodeNil(forKey: .key) {
                self.key = .null
            } else {
                self.key = .value(try container.decode(String.self, forKey: .key))
            }
            if !container.contains(.name) {
                self.name = .missing
            } else if try container.decodeNil(forKey: .name) {
                self.name = .null
            } else {
                self.name = .value(try container.decode(String.self, forKey: .name))
            }
            if !container.contains(.newWorkspace) {
                self.newWorkspace = .missing
            } else if try container.decodeNil(forKey: .newWorkspace) {
                throw CodecError.invalidLiteral("new_workspace")
            } else {
                self.newWorkspace = .value(try container.decode(Bool.self, forKey: .newWorkspace))
            }
            if !container.contains(.pane) {
                self.pane = .missing
            } else if try container.decodeNil(forKey: .pane) {
                self.pane = .null
            } else {
                self.pane = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .pane))
            }
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch argv {
            case .missing: break
            case .null: try container.encodeNil(forKey: .argv)
            case .value(let value): try container.encode(value, forKey: .argv)
            }
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            switch command {
            case .missing: break
            case .null: try container.encodeNil(forKey: .command)
            case .value(let value): try container.encode(value, forKey: .command)
            }
            switch cwd {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cwd)
            case .value(let value): try container.encode(value, forKey: .cwd)
            }
            switch key {
            case .missing: break
            case .null: try container.encodeNil(forKey: .key)
            case .value(let value): try container.encode(value, forKey: .key)
            }
            switch name {
            case .missing: break
            case .null: try container.encodeNil(forKey: .name)
            case .value(let value): try container.encode(value, forKey: .name)
            }
            switch newWorkspace {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("new_workspace")
            case .value(let value): try container.encode(value, forKey: .newWorkspace)
            }
            switch pane {
            case .missing: break
            case .null: try container.encodeNil(forKey: .pane)
            case .value(let value): try container.encode(value, forKey: .pane)
            }
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
        }
    }

    struct ScrollSurfaceRequest: Codable, Sendable, Equatable {
        let delta: Int64
        let surface: CloudTuiGenerated.Id

        init(delta: Int64, surface: CloudTuiGenerated.Id) {
            self.delta = delta
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case delta = "delta"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.delta = try container.decode(Int64.self, forKey: .delta)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(delta, forKey: .delta)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct SelectScreenRequest: Codable, Sendable, Equatable {
        let delta: CloudTuiGenerated.OptionalField<Int64>
        let index: CloudTuiGenerated.OptionalField<UInt64>

        init(delta: CloudTuiGenerated.OptionalField<Int64> = .missing, index: CloudTuiGenerated.OptionalField<UInt64> = .missing) {
            self.delta = delta
            self.index = index
        }

        enum CodingKeys: String, CodingKey {
            case delta = "delta"
            case index = "index"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.delta) {
                self.delta = .missing
            } else if try container.decodeNil(forKey: .delta) {
                self.delta = .null
            } else {
                self.delta = .value(try container.decode(Int64.self, forKey: .delta))
            }
            if !container.contains(.index) {
                self.index = .missing
            } else if try container.decodeNil(forKey: .index) {
                self.index = .null
            } else {
                self.index = .value(try container.decode(UInt64.self, forKey: .index))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch delta {
            case .missing: break
            case .null: try container.encodeNil(forKey: .delta)
            case .value(let value): try container.encode(value, forKey: .delta)
            }
            switch index {
            case .missing: break
            case .null: try container.encodeNil(forKey: .index)
            case .value(let value): try container.encode(value, forKey: .index)
            }
        }
    }

    struct SelectTabRequest: Codable, Sendable, Equatable {
        let delta: CloudTuiGenerated.OptionalField<Int64>
        let index: CloudTuiGenerated.OptionalField<UInt64>
        let pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(delta: CloudTuiGenerated.OptionalField<Int64> = .missing, index: CloudTuiGenerated.OptionalField<UInt64> = .missing, pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.delta = delta
            self.index = index
            self.pane = pane
        }

        enum CodingKeys: String, CodingKey {
            case delta = "delta"
            case index = "index"
            case pane = "pane"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.delta) {
                self.delta = .missing
            } else if try container.decodeNil(forKey: .delta) {
                self.delta = .null
            } else {
                self.delta = .value(try container.decode(Int64.self, forKey: .delta))
            }
            if !container.contains(.index) {
                self.index = .missing
            } else if try container.decodeNil(forKey: .index) {
                self.index = .null
            } else {
                self.index = .value(try container.decode(UInt64.self, forKey: .index))
            }
            if !container.contains(.pane) {
                self.pane = .missing
            } else if try container.decodeNil(forKey: .pane) {
                self.pane = .null
            } else {
                self.pane = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .pane))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch delta {
            case .missing: break
            case .null: try container.encodeNil(forKey: .delta)
            case .value(let value): try container.encode(value, forKey: .delta)
            }
            switch index {
            case .missing: break
            case .null: try container.encodeNil(forKey: .index)
            case .value(let value): try container.encode(value, forKey: .index)
            }
            switch pane {
            case .missing: break
            case .null: try container.encodeNil(forKey: .pane)
            case .value(let value): try container.encode(value, forKey: .pane)
            }
        }
    }

    struct SelectWorkspaceRequest: Codable, Sendable, Equatable {
        let delta: CloudTuiGenerated.OptionalField<Int64>
        let index: CloudTuiGenerated.OptionalField<UInt64>

        init(delta: CloudTuiGenerated.OptionalField<Int64> = .missing, index: CloudTuiGenerated.OptionalField<UInt64> = .missing) {
            self.delta = delta
            self.index = index
        }

        enum CodingKeys: String, CodingKey {
            case delta = "delta"
            case index = "index"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.delta) {
                self.delta = .missing
            } else if try container.decodeNil(forKey: .delta) {
                self.delta = .null
            } else {
                self.delta = .value(try container.decode(Int64.self, forKey: .delta))
            }
            if !container.contains(.index) {
                self.index = .missing
            } else if try container.decodeNil(forKey: .index) {
                self.index = .null
            } else {
                self.index = .value(try container.decode(UInt64.self, forKey: .index))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch delta {
            case .missing: break
            case .null: try container.encodeNil(forKey: .delta)
            case .value(let value): try container.encode(value, forKey: .delta)
            }
            switch index {
            case .missing: break
            case .null: try container.encodeNil(forKey: .index)
            case .value(let value): try container.encode(value, forKey: .index)
            }
        }
    }

    struct SendRequest: Codable, Sendable, Equatable {
        let bytes: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Base64>
        let paste: CloudTuiGenerated.OptionalField<Bool>
        let surface: CloudTuiGenerated.Id
        let text: CloudTuiGenerated.OptionalField<String>

        init(bytes: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Base64> = .missing, paste: CloudTuiGenerated.OptionalField<Bool> = .missing, surface: CloudTuiGenerated.Id, text: CloudTuiGenerated.OptionalField<String> = .missing) {
            self.bytes = bytes
            self.paste = paste
            self.surface = surface
            self.text = text
        }

        enum CodingKeys: String, CodingKey {
            case bytes = "bytes"
            case paste = "paste"
            case surface = "surface"
            case text = "text"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.bytes) {
                self.bytes = .missing
            } else if try container.decodeNil(forKey: .bytes) {
                self.bytes = .null
            } else {
                self.bytes = .value(try container.decode(CloudTuiGenerated.Base64.self, forKey: .bytes))
            }
            if !container.contains(.paste) {
                self.paste = .missing
            } else if try container.decodeNil(forKey: .paste) {
                throw CodecError.invalidLiteral("paste")
            } else {
                self.paste = .value(try container.decode(Bool.self, forKey: .paste))
            }
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            if !container.contains(.text) {
                self.text = .missing
            } else if try container.decodeNil(forKey: .text) {
                self.text = .null
            } else {
                self.text = .value(try container.decode(String.self, forKey: .text))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch bytes {
            case .missing: break
            case .null: try container.encodeNil(forKey: .bytes)
            case .value(let value): try container.encode(value, forKey: .bytes)
            }
            switch paste {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("paste")
            case .value(let value): try container.encode(value, forKey: .paste)
            }
            try container.encode(surface, forKey: .surface)
            switch text {
            case .missing: break
            case .null: try container.encodeNil(forKey: .text)
            case .value(let value): try container.encode(value, forKey: .text)
            }
        }
    }

    struct SendKeyRequest: Codable, Sendable, Equatable {
        let keys: [String]
        let surface: CloudTuiGenerated.Id

        init(keys: [String], surface: CloudTuiGenerated.Id) {
            self.keys = keys
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case keys = "keys"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.keys = try container.decode([String].self, forKey: .keys)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(keys, forKey: .keys)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct ServerStatsRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct SetCellPixelsRequest: Codable, Sendable, Equatable {
        let heightPx: UInt16
        let widthPx: UInt16

        init(heightPx: UInt16, widthPx: UInt16) {
            self.heightPx = heightPx
            self.widthPx = widthPx
        }

        enum CodingKeys: String, CodingKey {
            case heightPx = "height_px"
            case widthPx = "width_px"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.heightPx = try container.decode(UInt16.self, forKey: .heightPx)
            self.widthPx = try container.decode(UInt16.self, forKey: .widthPx)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(heightPx, forKey: .heightPx)
            try container.encode(widthPx, forKey: .widthPx)
        }
    }

    struct SetClientInfoRequest: Codable, Sendable, Equatable {
        let capabilities: CloudTuiGenerated.OptionalField<[String]>
        let kind: CloudTuiGenerated.OptionalField<String>
        let name: CloudTuiGenerated.OptionalField<String>

        init(capabilities: CloudTuiGenerated.OptionalField<[String]> = .missing, kind: CloudTuiGenerated.OptionalField<String> = .missing, name: CloudTuiGenerated.OptionalField<String> = .missing) {
            self.capabilities = capabilities
            self.kind = kind
            self.name = name
        }

        enum CodingKeys: String, CodingKey {
            case capabilities = "capabilities"
            case kind = "kind"
            case name = "name"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.capabilities) {
                self.capabilities = .missing
            } else if try container.decodeNil(forKey: .capabilities) {
                self.capabilities = .null
            } else {
                self.capabilities = .value(try container.decode([String].self, forKey: .capabilities))
            }
            if !container.contains(.kind) {
                self.kind = .missing
            } else if try container.decodeNil(forKey: .kind) {
                self.kind = .null
            } else {
                self.kind = .value(try container.decode(String.self, forKey: .kind))
            }
            if !container.contains(.name) {
                self.name = .missing
            } else if try container.decodeNil(forKey: .name) {
                self.name = .null
            } else {
                self.name = .value(try container.decode(String.self, forKey: .name))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch capabilities {
            case .missing: break
            case .null: try container.encodeNil(forKey: .capabilities)
            case .value(let value): try container.encode(value, forKey: .capabilities)
            }
            switch kind {
            case .missing: break
            case .null: try container.encodeNil(forKey: .kind)
            case .value(let value): try container.encode(value, forKey: .kind)
            }
            switch name {
            case .missing: break
            case .null: try container.encodeNil(forKey: .name)
            case .value(let value): try container.encode(value, forKey: .name)
            }
        }
    }

    struct SetClientSizingRequest: Codable, Sendable, Equatable {
        let client: CloudTuiGenerated.OptionalField<UInt64>
        let enabled: Bool
        let exclusive: CloudTuiGenerated.OptionalField<Bool>
        let surface: CloudTuiGenerated.Id

        init(client: CloudTuiGenerated.OptionalField<UInt64> = .missing, enabled: Bool, exclusive: CloudTuiGenerated.OptionalField<Bool> = .missing, surface: CloudTuiGenerated.Id) {
            self.client = client
            self.enabled = enabled
            self.exclusive = exclusive
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case client = "client"
            case enabled = "enabled"
            case exclusive = "exclusive"
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.client) {
                self.client = .missing
            } else if try container.decodeNil(forKey: .client) {
                self.client = .null
            } else {
                self.client = .value(try container.decode(UInt64.self, forKey: .client))
            }
            self.enabled = try container.decode(Bool.self, forKey: .enabled)
            if !container.contains(.exclusive) {
                self.exclusive = .missing
            } else if try container.decodeNil(forKey: .exclusive) {
                throw CodecError.invalidLiteral("exclusive")
            } else {
                self.exclusive = .value(try container.decode(Bool.self, forKey: .exclusive))
            }
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch client {
            case .missing: break
            case .null: try container.encodeNil(forKey: .client)
            case .value(let value): try container.encode(value, forKey: .client)
            }
            try container.encode(enabled, forKey: .enabled)
            switch exclusive {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("exclusive")
            case .value(let value): try container.encode(value, forKey: .exclusive)
            }
            try container.encode(surface, forKey: .surface)
        }
    }

    struct SetDefaultColorsRequest: Codable, Sendable, Equatable {
        let bg: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex>
        let complete: CloudTuiGenerated.OptionalField<Bool>
        let cursor: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex>
        let cursorBlink: CloudTuiGenerated.OptionalField<Bool>
        let cursorStyle: CloudTuiGenerated.OptionalField<CloudTuiGenerated.CursorStyle>
        let fg: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex>
        let palette: CloudTuiGenerated.OptionalField<[String: CloudTuiGenerated.ColorHex]>
        let selectionBg: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex>
        let selectionFg: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex>

        init(bg: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex> = .missing, complete: CloudTuiGenerated.OptionalField<Bool> = .missing, cursor: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex> = .missing, cursorBlink: CloudTuiGenerated.OptionalField<Bool> = .missing, cursorStyle: CloudTuiGenerated.OptionalField<CloudTuiGenerated.CursorStyle> = .missing, fg: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex> = .missing, palette: CloudTuiGenerated.OptionalField<[String: CloudTuiGenerated.ColorHex]> = .missing, selectionBg: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex> = .missing, selectionFg: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ColorHex> = .missing) {
            self.bg = bg
            self.complete = complete
            self.cursor = cursor
            self.cursorBlink = cursorBlink
            self.cursorStyle = cursorStyle
            self.fg = fg
            self.palette = palette
            self.selectionBg = selectionBg
            self.selectionFg = selectionFg
        }

        enum CodingKeys: String, CodingKey {
            case bg = "bg"
            case complete = "complete"
            case cursor = "cursor"
            case cursorBlink = "cursor_blink"
            case cursorStyle = "cursor_style"
            case fg = "fg"
            case palette = "palette"
            case selectionBg = "selection_bg"
            case selectionFg = "selection_fg"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.bg) {
                self.bg = .missing
            } else if try container.decodeNil(forKey: .bg) {
                self.bg = .null
            } else {
                self.bg = .value(try container.decode(CloudTuiGenerated.ColorHex.self, forKey: .bg))
            }
            if !container.contains(.complete) {
                self.complete = .missing
            } else if try container.decodeNil(forKey: .complete) {
                throw CodecError.invalidLiteral("complete")
            } else {
                self.complete = .value(try container.decode(Bool.self, forKey: .complete))
            }
            if !container.contains(.cursor) {
                self.cursor = .missing
            } else if try container.decodeNil(forKey: .cursor) {
                self.cursor = .null
            } else {
                self.cursor = .value(try container.decode(CloudTuiGenerated.ColorHex.self, forKey: .cursor))
            }
            if !container.contains(.cursorBlink) {
                self.cursorBlink = .missing
            } else if try container.decodeNil(forKey: .cursorBlink) {
                self.cursorBlink = .null
            } else {
                self.cursorBlink = .value(try container.decode(Bool.self, forKey: .cursorBlink))
            }
            if !container.contains(.cursorStyle) {
                self.cursorStyle = .missing
            } else if try container.decodeNil(forKey: .cursorStyle) {
                self.cursorStyle = .null
            } else {
                self.cursorStyle = .value(try container.decode(CloudTuiGenerated.CursorStyle.self, forKey: .cursorStyle))
            }
            if !container.contains(.fg) {
                self.fg = .missing
            } else if try container.decodeNil(forKey: .fg) {
                self.fg = .null
            } else {
                self.fg = .value(try container.decode(CloudTuiGenerated.ColorHex.self, forKey: .fg))
            }
            if !container.contains(.palette) {
                self.palette = .missing
            } else if try container.decodeNil(forKey: .palette) {
                self.palette = .null
            } else {
                self.palette = .value(try container.decode([String: CloudTuiGenerated.ColorHex].self, forKey: .palette))
            }
            if !container.contains(.selectionBg) {
                self.selectionBg = .missing
            } else if try container.decodeNil(forKey: .selectionBg) {
                self.selectionBg = .null
            } else {
                self.selectionBg = .value(try container.decode(CloudTuiGenerated.ColorHex.self, forKey: .selectionBg))
            }
            if !container.contains(.selectionFg) {
                self.selectionFg = .missing
            } else if try container.decodeNil(forKey: .selectionFg) {
                self.selectionFg = .null
            } else {
                self.selectionFg = .value(try container.decode(CloudTuiGenerated.ColorHex.self, forKey: .selectionFg))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch bg {
            case .missing: break
            case .null: try container.encodeNil(forKey: .bg)
            case .value(let value): try container.encode(value, forKey: .bg)
            }
            switch complete {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("complete")
            case .value(let value): try container.encode(value, forKey: .complete)
            }
            switch cursor {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cursor)
            case .value(let value): try container.encode(value, forKey: .cursor)
            }
            switch cursorBlink {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cursorBlink)
            case .value(let value): try container.encode(value, forKey: .cursorBlink)
            }
            switch cursorStyle {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cursorStyle)
            case .value(let value): try container.encode(value, forKey: .cursorStyle)
            }
            switch fg {
            case .missing: break
            case .null: try container.encodeNil(forKey: .fg)
            case .value(let value): try container.encode(value, forKey: .fg)
            }
            switch palette {
            case .missing: break
            case .null: try container.encodeNil(forKey: .palette)
            case .value(let value): try container.encode(value, forKey: .palette)
            }
            switch selectionBg {
            case .missing: break
            case .null: try container.encodeNil(forKey: .selectionBg)
            case .value(let value): try container.encode(value, forKey: .selectionBg)
            }
            switch selectionFg {
            case .missing: break
            case .null: try container.encodeNil(forKey: .selectionFg)
            case .value(let value): try container.encode(value, forKey: .selectionFg)
            }
        }
    }

    struct SetRatioRequest: Codable, Sendable, Equatable {
        let dir: CloudTuiGenerated.SplitDirection
        let pane: CloudTuiGenerated.Id
        let ratio: Float

        init(dir: CloudTuiGenerated.SplitDirection, pane: CloudTuiGenerated.Id, ratio: Float) {
            self.dir = dir
            self.pane = pane
            self.ratio = ratio
        }

        enum CodingKeys: String, CodingKey {
            case dir = "dir"
            case pane = "pane"
            case ratio = "ratio"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.dir = try container.decode(CloudTuiGenerated.SplitDirection.self, forKey: .dir)
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
            self.ratio = try container.decode(Float.self, forKey: .ratio)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(dir, forKey: .dir)
            try container.encode(pane, forKey: .pane)
            try container.encode(ratio, forKey: .ratio)
        }
    }

    struct SetSplitRatioRequest: Codable, Sendable, Equatable {
        let ratio: Float
        let split: CloudTuiGenerated.Id
        let transaction: CloudTuiGenerated.OptionalField<UInt64>

        init(ratio: Float, split: CloudTuiGenerated.Id, transaction: CloudTuiGenerated.OptionalField<UInt64> = .missing) {
            self.ratio = ratio
            self.split = split
            self.transaction = transaction
        }

        enum CodingKeys: String, CodingKey {
            case ratio = "ratio"
            case split = "split"
            case transaction = "transaction"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.ratio = try container.decode(Float.self, forKey: .ratio)
            self.split = try container.decode(CloudTuiGenerated.Id.self, forKey: .split)
            if !container.contains(.transaction) {
                self.transaction = .missing
            } else if try container.decodeNil(forKey: .transaction) {
                self.transaction = .null
            } else {
                self.transaction = .value(try container.decode(UInt64.self, forKey: .transaction))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(ratio, forKey: .ratio)
            try container.encode(split, forKey: .split)
            switch transaction {
            case .missing: break
            case .null: try container.encodeNil(forKey: .transaction)
            case .value(let value): try container.encode(value, forKey: .transaction)
            }
        }
    }

    struct SetViewportPaneWidthRequest: Codable, Sendable, Equatable {
        let pane: CloudTuiGenerated.Id
        let transaction: CloudTuiGenerated.OptionalField<UInt64>
        let width: Float

        init(pane: CloudTuiGenerated.Id, transaction: CloudTuiGenerated.OptionalField<UInt64> = .missing, width: Float) {
            self.pane = pane
            self.transaction = transaction
            self.width = width
        }

        enum CodingKeys: String, CodingKey {
            case pane = "pane"
            case transaction = "transaction"
            case width = "width"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
            if !container.contains(.transaction) {
                self.transaction = .missing
            } else if try container.decodeNil(forKey: .transaction) {
                self.transaction = .null
            } else {
                self.transaction = .value(try container.decode(UInt64.self, forKey: .transaction))
            }
            self.width = try container.decode(Float.self, forKey: .width)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(pane, forKey: .pane)
            switch transaction {
            case .missing: break
            case .null: try container.encodeNil(forKey: .transaction)
            case .value(let value): try container.encode(value, forKey: .transaction)
            }
            try container.encode(width, forKey: .width)
        }
    }

    struct SetWindowTitleRequest: Codable, Sendable, Equatable {
        let title: String

        init(title: String) {
            self.title = title
        }

        enum CodingKeys: String, CodingKey {
            case title = "title"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.title = try container.decode(String.self, forKey: .title)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(title, forKey: .title)
        }
    }

    struct ShutdownDaemonRequest: Codable, Sendable, Equatable {
        let force: CloudTuiGenerated.OptionalField<Bool>
        let generation: String
        let pid: UInt32

        init(force: CloudTuiGenerated.OptionalField<Bool> = .missing, generation: String, pid: UInt32) {
            self.force = force
            self.generation = generation
            self.pid = pid
        }

        enum CodingKeys: String, CodingKey {
            case force = "force"
            case generation = "generation"
            case pid = "pid"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.force) {
                self.force = .missing
            } else if try container.decodeNil(forKey: .force) {
                throw CodecError.invalidLiteral("force")
            } else {
                self.force = .value(try container.decode(Bool.self, forKey: .force))
            }
            self.generation = try container.decode(String.self, forKey: .generation)
            self.pid = try container.decode(UInt32.self, forKey: .pid)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch force {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("force")
            case .value(let value): try container.encode(value, forKey: .force)
            }
            try container.encode(generation, forKey: .generation)
            try container.encode(pid, forKey: .pid)
        }
    }

    struct SidebarPluginRequest: Codable, Sendable, Equatable {
        let cols: UInt16
        let relaunch: CloudTuiGenerated.OptionalField<Bool>
        let rows: UInt16

        init(cols: UInt16, relaunch: CloudTuiGenerated.OptionalField<Bool> = .missing, rows: UInt16) {
            self.cols = cols
            self.relaunch = relaunch
            self.rows = rows
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case relaunch = "relaunch"
            case rows = "rows"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.cols = try container.decode(UInt16.self, forKey: .cols)
            if !container.contains(.relaunch) {
                self.relaunch = .missing
            } else if try container.decodeNil(forKey: .relaunch) {
                throw CodecError.invalidLiteral("relaunch")
            } else {
                self.relaunch = .value(try container.decode(Bool.self, forKey: .relaunch))
            }
            self.rows = try container.decode(UInt16.self, forKey: .rows)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(cols, forKey: .cols)
            switch relaunch {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("relaunch")
            case .value(let value): try container.encode(value, forKey: .relaunch)
            }
            try container.encode(rows, forKey: .rows)
        }
    }

    struct SplitRequest: Codable, Sendable, Equatable {
        let cols: CloudTuiGenerated.OptionalField<UInt16>
        let dir: CloudTuiGenerated.SplitDirection
        let pane: CloudTuiGenerated.Id
        let rows: CloudTuiGenerated.OptionalField<UInt16>

        init(cols: CloudTuiGenerated.OptionalField<UInt16> = .missing, dir: CloudTuiGenerated.SplitDirection, pane: CloudTuiGenerated.Id, rows: CloudTuiGenerated.OptionalField<UInt16> = .missing) {
            self.cols = cols
            self.dir = dir
            self.pane = pane
            self.rows = rows
        }

        enum CodingKeys: String, CodingKey {
            case cols = "cols"
            case dir = "dir"
            case pane = "pane"
            case rows = "rows"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.cols) {
                self.cols = .missing
            } else if try container.decodeNil(forKey: .cols) {
                self.cols = .null
            } else {
                self.cols = .value(try container.decode(UInt16.self, forKey: .cols))
            }
            self.dir = try container.decode(CloudTuiGenerated.SplitDirection.self, forKey: .dir)
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
            if !container.contains(.rows) {
                self.rows = .missing
            } else if try container.decodeNil(forKey: .rows) {
                self.rows = .null
            } else {
                self.rows = .value(try container.decode(UInt16.self, forKey: .rows))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch cols {
            case .missing: break
            case .null: try container.encodeNil(forKey: .cols)
            case .value(let value): try container.encode(value, forKey: .cols)
            }
            try container.encode(dir, forKey: .dir)
            try container.encode(pane, forKey: .pane)
            switch rows {
            case .missing: break
            case .null: try container.encodeNil(forKey: .rows)
            case .value(let value): try container.encode(value, forKey: .rows)
            }
        }
    }

    struct SubscribeRequest: Codable, Sendable, Equatable {
        let presenceOnly: CloudTuiGenerated.OptionalField<Bool>
        let surface: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>
        let treeEvents: CloudTuiGenerated.OptionalField<CloudTuiGenerated.SubscribeRequestTreeEvents>

        init(presenceOnly: CloudTuiGenerated.OptionalField<Bool> = .missing, surface: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing, treeEvents: CloudTuiGenerated.OptionalField<CloudTuiGenerated.SubscribeRequestTreeEvents> = .missing) {
            self.presenceOnly = presenceOnly
            self.surface = surface
            self.treeEvents = treeEvents
        }

        enum CodingKeys: String, CodingKey {
            case presenceOnly = "presence_only"
            case surface = "surface"
            case treeEvents = "tree_events"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.presenceOnly) {
                self.presenceOnly = .missing
            } else if try container.decodeNil(forKey: .presenceOnly) {
                self.presenceOnly = .null
            } else {
                self.presenceOnly = .value(try container.decode(Bool.self, forKey: .presenceOnly))
            }
            if !container.contains(.surface) {
                self.surface = .missing
            } else if try container.decodeNil(forKey: .surface) {
                self.surface = .null
            } else {
                self.surface = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .surface))
            }
            if !container.contains(.treeEvents) {
                self.treeEvents = .missing
            } else if try container.decodeNil(forKey: .treeEvents) {
                self.treeEvents = .null
            } else {
                self.treeEvents = .value(try container.decode(CloudTuiGenerated.SubscribeRequestTreeEvents.self, forKey: .treeEvents))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch presenceOnly {
            case .missing: break
            case .null: try container.encodeNil(forKey: .presenceOnly)
            case .value(let value): try container.encode(value, forKey: .presenceOnly)
            }
            switch surface {
            case .missing: break
            case .null: try container.encodeNil(forKey: .surface)
            case .value(let value): try container.encode(value, forKey: .surface)
            }
            switch treeEvents {
            case .missing: break
            case .null: try container.encodeNil(forKey: .treeEvents)
            case .value(let value): try container.encode(value, forKey: .treeEvents)
            }
        }
    }

    struct SwapPaneRequest: Codable, Sendable, Equatable {
        let dir: CloudTuiGenerated.OptionalField<CloudTuiGenerated.PaneDirection>
        let pane: CloudTuiGenerated.Id
        let target: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(dir: CloudTuiGenerated.OptionalField<CloudTuiGenerated.PaneDirection> = .missing, pane: CloudTuiGenerated.Id, target: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.dir = dir
            self.pane = pane
            self.target = target
        }

        enum CodingKeys: String, CodingKey {
            case dir = "dir"
            case pane = "pane"
            case target = "target"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.dir) {
                self.dir = .missing
            } else if try container.decodeNil(forKey: .dir) {
                self.dir = .null
            } else {
                self.dir = .value(try container.decode(CloudTuiGenerated.PaneDirection.self, forKey: .dir))
            }
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
            if !container.contains(.target) {
                self.target = .missing
            } else if try container.decodeNil(forKey: .target) {
                self.target = .null
            } else {
                self.target = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .target))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch dir {
            case .missing: break
            case .null: try container.encodeNil(forKey: .dir)
            case .value(let value): try container.encode(value, forKey: .dir)
            }
            try container.encode(pane, forKey: .pane)
            switch target {
            case .missing: break
            case .null: try container.encodeNil(forKey: .target)
            case .value(let value): try container.encode(value, forKey: .target)
            }
        }
    }

    struct TerminalEventsRequest: Codable, Sendable, Equatable {
        let afterRevision: CloudTuiGenerated.OptionalField<UInt64>

        init(afterRevision: CloudTuiGenerated.OptionalField<UInt64> = .missing) {
            self.afterRevision = afterRevision
        }

        enum CodingKeys: String, CodingKey {
            case afterRevision = "after_revision"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.afterRevision) {
                self.afterRevision = .missing
            } else if try container.decodeNil(forKey: .afterRevision) {
                throw CodecError.invalidLiteral("after_revision")
            } else {
                self.afterRevision = .value(try container.decode(UInt64.self, forKey: .afterRevision))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch afterRevision {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("after_revision")
            case .value(let value): try container.encode(value, forKey: .afterRevision)
            }
        }
    }

    struct UndoLayoutRequest: Codable, Sendable, Equatable {
        let confirmClose: CloudTuiGenerated.OptionalField<Bool>
        let pane: CloudTuiGenerated.Id
        let revision: CloudTuiGenerated.OptionalField<UInt64>

        init(confirmClose: CloudTuiGenerated.OptionalField<Bool> = .missing, pane: CloudTuiGenerated.Id, revision: CloudTuiGenerated.OptionalField<UInt64> = .missing) {
            self.confirmClose = confirmClose
            self.pane = pane
            self.revision = revision
        }

        enum CodingKeys: String, CodingKey {
            case confirmClose = "confirm_close"
            case pane = "pane"
            case revision = "revision"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.confirmClose) {
                self.confirmClose = .missing
            } else if try container.decodeNil(forKey: .confirmClose) {
                throw CodecError.invalidLiteral("confirm_close")
            } else {
                self.confirmClose = .value(try container.decode(Bool.self, forKey: .confirmClose))
            }
            self.pane = try container.decode(CloudTuiGenerated.Id.self, forKey: .pane)
            if !container.contains(.revision) {
                self.revision = .missing
            } else if try container.decodeNil(forKey: .revision) {
                self.revision = .null
            } else {
                self.revision = .value(try container.decode(UInt64.self, forKey: .revision))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch confirmClose {
            case .missing: break
            case .null: throw CodecError.invalidLiteral("confirm_close")
            case .value(let value): try container.encode(value, forKey: .confirmClose)
            }
            try container.encode(pane, forKey: .pane)
            switch revision {
            case .missing: break
            case .null: try container.encodeNil(forKey: .revision)
            case .value(let value): try container.encode(value, forKey: .revision)
            }
        }
    }

    struct UnregisterBrowserProviderRequest: Codable, Sendable, Equatable {

        init() {
        }

        init(from decoder: Decoder) throws {
            _ = decoder
        }

        func encode(to encoder: Encoder) throws {
            _ = encoder
        }
    }

    struct UrlOpenRequest: Codable, Sendable, Equatable {
        let terminalId: String
        let url: String

        init(terminalId: String, url: String) {
            self.terminalId = terminalId
            self.url = url
        }

        enum CodingKeys: String, CodingKey {
            case terminalId = "terminal_id"
            case url = "url"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.terminalId = try container.decode(String.self, forKey: .terminalId)
            self.url = try container.decode(String.self, forKey: .url)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(terminalId, forKey: .terminalId)
            try container.encode(url, forKey: .url)
        }
    }

    struct UrlOpenClaimRequest: Codable, Sendable, Equatable {
        let requestId: String

        init(requestId: String) {
            self.requestId = requestId
        }

        enum CodingKeys: String, CodingKey {
            case requestId = "request_id"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.requestId = try container.decode(String.self, forKey: .requestId)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(requestId, forKey: .requestId)
        }
    }

    struct UrlOpenResultRequest: Codable, Sendable, Equatable {
        let opened: Bool
        let requestId: String

        init(opened: Bool, requestId: String) {
            self.opened = opened
            self.requestId = requestId
        }

        enum CodingKeys: String, CodingKey {
            case opened = "opened"
            case requestId = "request_id"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.opened = try container.decode(Bool.self, forKey: .opened)
            self.requestId = try container.decode(String.self, forKey: .requestId)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(opened, forKey: .opened)
            try container.encode(requestId, forKey: .requestId)
        }
    }

    struct UrlOpenSubscribeRequest: Codable, Sendable, Equatable {
        let terminalIds: [String]

        init(terminalIds: [String]) {
            self.terminalIds = terminalIds
        }

        enum CodingKeys: String, CodingKey {
            case terminalIds = "terminal_ids"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.terminalIds = try container.decode([String].self, forKey: .terminalIds)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(terminalIds, forKey: .terminalIds)
        }
    }

    struct VtStateRequest: Codable, Sendable, Equatable {
        let surface: CloudTuiGenerated.Id

        init(surface: CloudTuiGenerated.Id) {
            self.surface = surface
        }

        enum CodingKeys: String, CodingKey {
            case surface = "surface"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(surface, forKey: .surface)
        }
    }

    struct WaitForRequest: Codable, Sendable, Equatable {
        let pattern: String
        let surface: CloudTuiGenerated.Id
        let timeoutMs: UInt64

        init(pattern: String, surface: CloudTuiGenerated.Id, timeoutMs: UInt64) {
            self.pattern = pattern
            self.surface = surface
            self.timeoutMs = timeoutMs
        }

        enum CodingKeys: String, CodingKey {
            case pattern = "pattern"
            case surface = "surface"
            case timeoutMs = "timeout_ms"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            self.pattern = try container.decode(String.self, forKey: .pattern)
            self.surface = try container.decode(CloudTuiGenerated.Id.self, forKey: .surface)
            self.timeoutMs = try container.decode(UInt64.self, forKey: .timeoutMs)
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(pattern, forKey: .pattern)
            try container.encode(surface, forKey: .surface)
            try container.encode(timeoutMs, forKey: .timeoutMs)
        }
    }

    struct ZoomPaneRequest: Codable, Sendable, Equatable {
        let mode: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ZoomPaneRequestMode>
        let pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id>

        init(mode: CloudTuiGenerated.OptionalField<CloudTuiGenerated.ZoomPaneRequestMode> = .missing, pane: CloudTuiGenerated.OptionalField<CloudTuiGenerated.Id> = .missing) {
            self.mode = mode
            self.pane = pane
        }

        enum CodingKeys: String, CodingKey {
            case mode = "mode"
            case pane = "pane"
        }

        init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            if !container.contains(.mode) {
                self.mode = .missing
            } else if try container.decodeNil(forKey: .mode) {
                self.mode = .null
            } else {
                self.mode = .value(try container.decode(CloudTuiGenerated.ZoomPaneRequestMode.self, forKey: .mode))
            }
            if !container.contains(.pane) {
                self.pane = .missing
            } else if try container.decodeNil(forKey: .pane) {
                self.pane = .null
            } else {
                self.pane = .value(try container.decode(CloudTuiGenerated.Id.self, forKey: .pane))
            }
        }

        func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            switch mode {
            case .missing: break
            case .null: try container.encodeNil(forKey: .mode)
            case .value(let value): try container.encode(value, forKey: .mode)
            }
            switch pane {
            case .missing: break
            case .null: try container.encodeNil(forKey: .pane)
            case .value(let value): try container.encode(value, forKey: .pane)
            }
        }
    }

    enum Command: Sendable {
        case applyLayout(id: UInt64, request: ApplyLayoutRequest)
        case attachSurface(id: UInt64, request: AttachSurfaceRequest)
        case browserActivate(id: UInt64, request: BrowserActivateRequest)
        case browserBack(id: UInt64, request: BrowserBackRequest)
        case browserForward(id: UInt64, request: BrowserForwardRequest)
        case browserFramePresented(id: UInt64, request: BrowserFramePresentedRequest)
        case browserInsertText(id: UInt64, request: BrowserInsertTextRequest)
        case browserKey(id: UInt64, request: BrowserKeyRequest)
        case browserKeyPress(id: UInt64, request: BrowserKeyPressRequest)
        case browserMouse(id: UInt64, request: BrowserMouseRequest)
        case browserMouseGuarded(id: UInt64, request: BrowserMouseGuardedRequest)
        case browserNavigate(id: UInt64, request: BrowserNavigateRequest)
        case browserReload(id: UInt64, request: BrowserReloadRequest)
        case browserWheel(id: UInt64, request: BrowserWheelRequest)
        case browserWheelGuarded(id: UInt64, request: BrowserWheelGuardedRequest)
        case clearHistory(id: UInt64, request: ClearHistoryRequest)
        case clearWindowTitle(id: UInt64, request: ClearWindowTitleRequest)
        case clientFocus(id: UInt64, request: ClientFocusRequest)
        case closePane(id: UInt64, request: ClosePaneRequest)
        case closeProviderManagedWorkspace(id: UInt64, request: CloseProviderManagedWorkspaceRequest)
        case closeScreen(id: UInt64, request: CloseScreenRequest)
        case closeSurface(id: UInt64, request: CloseSurfaceRequest)
        case closeTerminal(id: UInt64, request: CloseTerminalRequest)
        case closeWorkspace(id: UInt64, request: CloseWorkspaceRequest)
        case copy(id: UInt64, request: CopyRequest)
        case createSurfaceWithReceipt(id: UInt64, request: CreateSurfaceWithReceiptRequest)
        case createTerminal(id: UInt64, request: CreateTerminalRequest)
        case createWorkspace(id: UInt64, request: CreateWorkspaceRequest)
        case detachAttachedView(id: UInt64, request: DetachAttachedViewRequest)
        case detachClient(id: UInt64, request: DetachClientRequest)
        case exportLayout(id: UInt64, request: ExportLayoutRequest)
        case focusDirection(id: UInt64, request: FocusDirectionRequest)
        case focusPane(id: UInt64, request: FocusPaneRequest)
        case getBrowserProvider(id: UInt64, request: GetBrowserProviderRequest)
        case getCellPixels(id: UInt64, request: GetCellPixelsRequest)
        case getFrontendProjection(id: UInt64, request: GetFrontendProjectionRequest)
        case identify(id: UInt64, request: IdentifyRequest)
        case ids(id: UInt64, request: IdsRequest)
        case journalFrontendEvent(id: UInt64, request: JournalFrontendEventRequest)
        case listAgents(id: UInt64, request: ListAgentsRequest)
        case listClients(id: UInt64, request: ListClientsRequest)
        case listTerminals(id: UInt64, request: ListTerminalsRequest)
        case listWorkspaces(id: UInt64, request: ListWorkspacesRequest)
        case machineListeningTcp(id: UInt64, request: MachineListeningTcpRequest)
        case machineUsage(id: UInt64, request: MachineUsageRequest)
        case markWorkspacesProviderManaged(id: UInt64, request: MarkWorkspacesProviderManagedRequest)
        case mintTerminalRenderer(id: UInt64, request: MintTerminalRendererRequest)
        case mintTerminalRendererByTerminal(id: UInt64, request: MintTerminalRendererByTerminalRequest)
        case moveTab(id: UInt64, request: MoveTabRequest)
        case moveTerminal(id: UInt64, request: MoveTerminalRequest)
        case moveWorkspace(id: UInt64, request: MoveWorkspaceRequest)
        case newBrowserTab(id: UInt64, request: NewBrowserTabRequest)
        case newPane(id: UInt64, request: NewPaneRequest)
        case newPaneRight(id: UInt64, request: NewPaneRightRequest)
        case newScreen(id: UInt64, request: NewScreenRequest)
        case newTab(id: UInt64, request: NewTabRequest)
        case newWorkspace(id: UInt64, request: NewWorkspaceRequest)
        case notify(id: UInt64, request: NotifyRequest)
        case pairingResponse(id: UInt64, request: PairingResponseRequest)
        case paneNeighbor(id: UInt64, request: PaneNeighborRequest)
        case pasteImage(id: UInt64, request: PasteImageRequest)
        case ping(id: UInt64, request: PingRequest)
        case presenceClear(id: UInt64, request: PresenceClearRequest)
        case presenceList(id: UInt64, request: PresenceListRequest)
        case presenceUpdate(id: UInt64, request: PresenceUpdateRequest)
        case processInfo(id: UInt64, request: ProcessInfoRequest)
        case putFrontendProjection(id: UInt64, request: PutFrontendProjectionRequest)
        case readScreen(id: UInt64, request: ReadScreenRequest)
        case readScrollback(id: UInt64, request: ReadScrollbackRequest)
        case registerBrowserProvider(id: UInt64, request: RegisterBrowserProviderRequest)
        case releaseAttachedViewSize(id: UInt64, request: ReleaseAttachedViewSizeRequest)
        case releaseSurfaceSize(id: UInt64, request: ReleaseSurfaceSizeRequest)
        case reloadConfig(id: UInt64, request: ReloadConfigRequest)
        case renamePane(id: UInt64, request: RenamePaneRequest)
        case renameProviderManagedWorkspace(id: UInt64, request: RenameProviderManagedWorkspaceRequest)
        case renameScreen(id: UInt64, request: RenameScreenRequest)
        case renameSurface(id: UInt64, request: RenameSurfaceRequest)
        case renameWorkspace(id: UInt64, request: RenameWorkspaceRequest)
        case reportAgent(id: UInt64, request: ReportAgentRequest)
        case reportFocus(id: UInt64, request: ReportFocusRequest)
        case resizeAttachedView(id: UInt64, request: ResizeAttachedViewRequest)
        case resizeSurface(id: UInt64, request: ResizeSurfaceRequest)
        case resolveTerminal(id: UInt64, request: ResolveTerminalRequest)
        case run(id: UInt64, request: RunRequest)
        case scrollSurface(id: UInt64, request: ScrollSurfaceRequest)
        case selectScreen(id: UInt64, request: SelectScreenRequest)
        case selectTab(id: UInt64, request: SelectTabRequest)
        case selectWorkspace(id: UInt64, request: SelectWorkspaceRequest)
        case send(id: UInt64, request: SendRequest)
        case sendKey(id: UInt64, request: SendKeyRequest)
        case serverStats(id: UInt64, request: ServerStatsRequest)
        case setCellPixels(id: UInt64, request: SetCellPixelsRequest)
        case setClientInfo(id: UInt64, request: SetClientInfoRequest)
        case setClientSizing(id: UInt64, request: SetClientSizingRequest)
        case setDefaultColors(id: UInt64, request: SetDefaultColorsRequest)
        case setRatio(id: UInt64, request: SetRatioRequest)
        case setSplitRatio(id: UInt64, request: SetSplitRatioRequest)
        case setViewportPaneWidth(id: UInt64, request: SetViewportPaneWidthRequest)
        case setWindowTitle(id: UInt64, request: SetWindowTitleRequest)
        case shutdownDaemon(id: UInt64, request: ShutdownDaemonRequest)
        case sidebarPlugin(id: UInt64, request: SidebarPluginRequest)
        case split(id: UInt64, request: SplitRequest)
        case subscribe(id: UInt64, request: SubscribeRequest)
        case swapPane(id: UInt64, request: SwapPaneRequest)
        case terminalEvents(id: UInt64, request: TerminalEventsRequest)
        case undoLayout(id: UInt64, request: UndoLayoutRequest)
        case unregisterBrowserProvider(id: UInt64, request: UnregisterBrowserProviderRequest)
        case urlOpen(id: UInt64, request: UrlOpenRequest)
        case urlOpenClaim(id: UInt64, request: UrlOpenClaimRequest)
        case urlOpenResult(id: UInt64, request: UrlOpenResultRequest)
        case urlOpenSubscribe(id: UInt64, request: UrlOpenSubscribeRequest)
        case vtState(id: UInt64, request: VtStateRequest)
        case waitFor(id: UInt64, request: WaitForRequest)
        case zoomPane(id: UInt64, request: ZoomPaneRequest)

        var wireName: String {
            switch self {
            case .applyLayout: return "apply-layout"
            case .attachSurface: return "attach-surface"
            case .browserActivate: return "browser-activate"
            case .browserBack: return "browser-back"
            case .browserForward: return "browser-forward"
            case .browserFramePresented: return "browser-frame-presented"
            case .browserInsertText: return "browser-insert-text"
            case .browserKey: return "browser-key"
            case .browserKeyPress: return "browser-key-press"
            case .browserMouse: return "browser-mouse"
            case .browserMouseGuarded: return "browser-mouse-guarded"
            case .browserNavigate: return "browser-navigate"
            case .browserReload: return "browser-reload"
            case .browserWheel: return "browser-wheel"
            case .browserWheelGuarded: return "browser-wheel-guarded"
            case .clearHistory: return "clear-history"
            case .clearWindowTitle: return "clear-window-title"
            case .clientFocus: return "client-focus"
            case .closePane: return "close-pane"
            case .closeProviderManagedWorkspace: return "close-provider-managed-workspace"
            case .closeScreen: return "close-screen"
            case .closeSurface: return "close-surface"
            case .closeTerminal: return "close-terminal"
            case .closeWorkspace: return "close-workspace"
            case .copy: return "copy"
            case .createSurfaceWithReceipt: return "create-surface-with-receipt"
            case .createTerminal: return "create-terminal"
            case .createWorkspace: return "create-workspace"
            case .detachAttachedView: return "detach-attached-view"
            case .detachClient: return "detach-client"
            case .exportLayout: return "export-layout"
            case .focusDirection: return "focus-direction"
            case .focusPane: return "focus-pane"
            case .getBrowserProvider: return "get-browser-provider"
            case .getCellPixels: return "get-cell-pixels"
            case .getFrontendProjection: return "get-frontend-projection"
            case .identify: return "identify"
            case .ids: return "ids"
            case .journalFrontendEvent: return "journal-frontend-event"
            case .listAgents: return "list-agents"
            case .listClients: return "list-clients"
            case .listTerminals: return "list-terminals"
            case .listWorkspaces: return "list-workspaces"
            case .machineListeningTcp: return "machine-listening-tcp"
            case .machineUsage: return "machine-usage"
            case .markWorkspacesProviderManaged: return "mark-workspaces-provider-managed"
            case .mintTerminalRenderer: return "mint-terminal-renderer"
            case .mintTerminalRendererByTerminal: return "mint-terminal-renderer-by-terminal"
            case .moveTab: return "move-tab"
            case .moveTerminal: return "move-terminal"
            case .moveWorkspace: return "move-workspace"
            case .newBrowserTab: return "new-browser-tab"
            case .newPane: return "new-pane"
            case .newPaneRight: return "new-pane-right"
            case .newScreen: return "new-screen"
            case .newTab: return "new-tab"
            case .newWorkspace: return "new-workspace"
            case .notify: return "notify"
            case .pairingResponse: return "pairing-response"
            case .paneNeighbor: return "pane-neighbor"
            case .pasteImage: return "paste-image"
            case .ping: return "ping"
            case .presenceClear: return "presence-clear"
            case .presenceList: return "presence-list"
            case .presenceUpdate: return "presence-update"
            case .processInfo: return "process-info"
            case .putFrontendProjection: return "put-frontend-projection"
            case .readScreen: return "read-screen"
            case .readScrollback: return "read-scrollback"
            case .registerBrowserProvider: return "register-browser-provider"
            case .releaseAttachedViewSize: return "release-attached-view-size"
            case .releaseSurfaceSize: return "release-surface-size"
            case .reloadConfig: return "reload-config"
            case .renamePane: return "rename-pane"
            case .renameProviderManagedWorkspace: return "rename-provider-managed-workspace"
            case .renameScreen: return "rename-screen"
            case .renameSurface: return "rename-surface"
            case .renameWorkspace: return "rename-workspace"
            case .reportAgent: return "report-agent"
            case .reportFocus: return "report-focus"
            case .resizeAttachedView: return "resize-attached-view"
            case .resizeSurface: return "resize-surface"
            case .resolveTerminal: return "resolve-terminal"
            case .run: return "run"
            case .scrollSurface: return "scroll-surface"
            case .selectScreen: return "select-screen"
            case .selectTab: return "select-tab"
            case .selectWorkspace: return "select-workspace"
            case .send: return "send"
            case .sendKey: return "send-key"
            case .serverStats: return "server-stats"
            case .setCellPixels: return "set-cell-pixels"
            case .setClientInfo: return "set-client-info"
            case .setClientSizing: return "set-client-sizing"
            case .setDefaultColors: return "set-default-colors"
            case .setRatio: return "set-ratio"
            case .setSplitRatio: return "set-split-ratio"
            case .setViewportPaneWidth: return "set-viewport-pane-width"
            case .setWindowTitle: return "set-window-title"
            case .shutdownDaemon: return "shutdown-daemon"
            case .sidebarPlugin: return "sidebar-plugin"
            case .split: return "split"
            case .subscribe: return "subscribe"
            case .swapPane: return "swap-pane"
            case .terminalEvents: return "terminal-events"
            case .undoLayout: return "undo-layout"
            case .unregisterBrowserProvider: return "unregister-browser-provider"
            case .urlOpen: return "url-open"
            case .urlOpenClaim: return "url-open-claim"
            case .urlOpenResult: return "url-open-result"
            case .urlOpenSubscribe: return "url-open-subscribe"
            case .vtState: return "vt-state"
            case .waitFor: return "wait-for"
            case .zoomPane: return "zoom-pane"
            }
        }

        func line() throws -> Data {
            switch self {
            case .applyLayout(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "apply-layout", payload: request)
            case .attachSurface(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "attach-surface", payload: request)
            case .browserActivate(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-activate", payload: request)
            case .browserBack(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-back", payload: request)
            case .browserForward(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-forward", payload: request)
            case .browserFramePresented(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-frame-presented", payload: request)
            case .browserInsertText(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-insert-text", payload: request)
            case .browserKey(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-key", payload: request)
            case .browserKeyPress(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-key-press", payload: request)
            case .browserMouse(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-mouse", payload: request)
            case .browserMouseGuarded(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-mouse-guarded", payload: request)
            case .browserNavigate(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-navigate", payload: request)
            case .browserReload(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-reload", payload: request)
            case .browserWheel(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-wheel", payload: request)
            case .browserWheelGuarded(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "browser-wheel-guarded", payload: request)
            case .clearHistory(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "clear-history", payload: request)
            case .clearWindowTitle(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "clear-window-title", payload: request)
            case .clientFocus(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "client-focus", payload: request)
            case .closePane(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "close-pane", payload: request)
            case .closeProviderManagedWorkspace(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "close-provider-managed-workspace", payload: request)
            case .closeScreen(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "close-screen", payload: request)
            case .closeSurface(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "close-surface", payload: request)
            case .closeTerminal(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "close-terminal", payload: request)
            case .closeWorkspace(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "close-workspace", payload: request)
            case .copy(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "copy", payload: request)
            case .createSurfaceWithReceipt(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "create-surface-with-receipt", payload: request)
            case .createTerminal(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "create-terminal", payload: request)
            case .createWorkspace(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "create-workspace", payload: request)
            case .detachAttachedView(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "detach-attached-view", payload: request)
            case .detachClient(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "detach-client", payload: request)
            case .exportLayout(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "export-layout", payload: request)
            case .focusDirection(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "focus-direction", payload: request)
            case .focusPane(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "focus-pane", payload: request)
            case .getBrowserProvider(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "get-browser-provider", payload: request)
            case .getCellPixels(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "get-cell-pixels", payload: request)
            case .getFrontendProjection(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "get-frontend-projection", payload: request)
            case .identify(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "identify", payload: request)
            case .ids(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "ids", payload: request)
            case .journalFrontendEvent(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "journal-frontend-event", payload: request)
            case .listAgents(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "list-agents", payload: request)
            case .listClients(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "list-clients", payload: request)
            case .listTerminals(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "list-terminals", payload: request)
            case .listWorkspaces(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "list-workspaces", payload: request)
            case .machineListeningTcp(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "machine-listening-tcp", payload: request)
            case .machineUsage(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "machine-usage", payload: request)
            case .markWorkspacesProviderManaged(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "mark-workspaces-provider-managed", payload: request)
            case .mintTerminalRenderer(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "mint-terminal-renderer", payload: request)
            case .mintTerminalRendererByTerminal(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "mint-terminal-renderer-by-terminal", payload: request)
            case .moveTab(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "move-tab", payload: request)
            case .moveTerminal(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "move-terminal", payload: request)
            case .moveWorkspace(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "move-workspace", payload: request)
            case .newBrowserTab(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "new-browser-tab", payload: request)
            case .newPane(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "new-pane", payload: request)
            case .newPaneRight(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "new-pane-right", payload: request)
            case .newScreen(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "new-screen", payload: request)
            case .newTab(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "new-tab", payload: request)
            case .newWorkspace(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "new-workspace", payload: request)
            case .notify(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "notify", payload: request)
            case .pairingResponse(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "pairing-response", payload: request)
            case .paneNeighbor(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "pane-neighbor", payload: request)
            case .pasteImage(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "paste-image", payload: request)
            case .ping(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "ping", payload: request)
            case .presenceClear(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "presence-clear", payload: request)
            case .presenceList(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "presence-list", payload: request)
            case .presenceUpdate(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "presence-update", payload: request)
            case .processInfo(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "process-info", payload: request)
            case .putFrontendProjection(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "put-frontend-projection", payload: request)
            case .readScreen(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "read-screen", payload: request)
            case .readScrollback(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "read-scrollback", payload: request)
            case .registerBrowserProvider(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "register-browser-provider", payload: request)
            case .releaseAttachedViewSize(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "release-attached-view-size", payload: request)
            case .releaseSurfaceSize(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "release-surface-size", payload: request)
            case .reloadConfig(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "reload-config", payload: request)
            case .renamePane(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "rename-pane", payload: request)
            case .renameProviderManagedWorkspace(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "rename-provider-managed-workspace", payload: request)
            case .renameScreen(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "rename-screen", payload: request)
            case .renameSurface(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "rename-surface", payload: request)
            case .renameWorkspace(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "rename-workspace", payload: request)
            case .reportAgent(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "report-agent", payload: request)
            case .reportFocus(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "report-focus", payload: request)
            case .resizeAttachedView(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "resize-attached-view", payload: request)
            case .resizeSurface(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "resize-surface", payload: request)
            case .resolveTerminal(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "resolve-terminal", payload: request)
            case .run(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "run", payload: request)
            case .scrollSurface(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "scroll-surface", payload: request)
            case .selectScreen(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "select-screen", payload: request)
            case .selectTab(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "select-tab", payload: request)
            case .selectWorkspace(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "select-workspace", payload: request)
            case .send(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "send", payload: request)
            case .sendKey(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "send-key", payload: request)
            case .serverStats(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "server-stats", payload: request)
            case .setCellPixels(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "set-cell-pixels", payload: request)
            case .setClientInfo(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "set-client-info", payload: request)
            case .setClientSizing(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "set-client-sizing", payload: request)
            case .setDefaultColors(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "set-default-colors", payload: request)
            case .setRatio(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "set-ratio", payload: request)
            case .setSplitRatio(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "set-split-ratio", payload: request)
            case .setViewportPaneWidth(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "set-viewport-pane-width", payload: request)
            case .setWindowTitle(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "set-window-title", payload: request)
            case .shutdownDaemon(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "shutdown-daemon", payload: request)
            case .sidebarPlugin(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "sidebar-plugin", payload: request)
            case .split(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "split", payload: request)
            case .subscribe(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "subscribe", payload: request)
            case .swapPane(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "swap-pane", payload: request)
            case .terminalEvents(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "terminal-events", payload: request)
            case .undoLayout(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "undo-layout", payload: request)
            case .unregisterBrowserProvider(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "unregister-browser-provider", payload: request)
            case .urlOpen(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "url-open", payload: request)
            case .urlOpenClaim(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "url-open-claim", payload: request)
            case .urlOpenResult(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "url-open-result", payload: request)
            case .urlOpenSubscribe(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "url-open-subscribe", payload: request)
            case .vtState(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "vt-state", payload: request)
            case .waitFor(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "wait-for", payload: request)
            case .zoomPane(let id, let request): return try CloudTuiGenerated.encodeCommand(id: id, command: "zoom-pane", payload: request)
            }
        }
    }
}
