[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cmux"
version = "0.2.0"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["cmux"]
