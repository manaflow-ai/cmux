merge-conflict gate: clean against origin/main
autoreview wrapper target: branch
cmux aziz prompt: enabled
swift review addendum: enabled
cmux policy: /Users/abdulazizalbahar/Dev/Manaflow/cmuxterm-hq/skills/autoreview/scripts/cmux-policy-check --mode branch --base origin/main
autoreview target: branch
branch: feat-iroh-ios-keychain-device-id
engine: codex
model: gpt-5.6-sol
thinking: high
tools: on
web_search: on
ref: origin/main
bundle: 205219 chars
autoreview findings: 2
[P1] Continuity evidence is created before it is checked
ios/cmuxPackage/Sources/cmuxFeature/MobileIrohRuntimeComposition.swift:51
On the activation path, `identities.identity(...)` creates a new `AfterFirstUnlockThisDeviceOnly` iroh identity before `deviceID()` invokes this probe. Consequently, on a new phone restored from a pre-witness backup, the probe returns true for the identity created moments earlier and adopts the migrated UserDefaults device ID. Both phones can then share one `(user, device, tag)` binding slot, causing registrations to overwrite or misroute each other. Capture the continuity result before identity creation, or resolve the durable device ID first.

[P2] Preserve generation only for revalidation, not same-account sign-in
Packages/Shared/CmuxAuthRuntime/Sources/CmuxAuthRuntime/Coordinator/AuthCoordinator.swift:597
This condition cannot distinguish foreground revalidation from a new credential exchange for the already-published account. Calling any public sign-in path while authenticated as the same user replaces the token session but leaves `sessionGeneration` unchanged. Operations pinned to the prior session—especially the forget flow's frozen credential pair—continue passing `sessionMatches`, defeating the documented session-replacement fence. Pass the publication reason into this method so only revalidation preserves the generation; every completed sign-in should advance it.

overall: patch is incorrect (0.97)
The change introduces an ordering-dependent device-continuity check that can clone a restored phone's device identity, and it weakens session-generation fencing for same-account reauthentication. Both affect correctness-critical identity handling.
