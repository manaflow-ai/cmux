merge-conflict gate: clean against origin/main
autoreview wrapper target: branch
cmux aziz prompt: enabled
swift review addendum: enabled
cmux policy: /Users/abdulazizalbahar/Dev/Manaflow/cmuxterm-hq/skills/autoreview/scripts/cmux-policy-check --mode branch --base origin/main
autoreview target: branch
branch: feat-iroh-ios-keychain-device-id
engine: codex
model: gpt-5.6-sol
thinking: high
tools: on
web_search: on
ref: origin/main
bundle: 205041 chars
autoreview findings: 3
[P1] [P1] Do not trust a mirror with no device witness
Packages/iOS/CmuxMobileShell/Sources/CmuxMobileShell/DeviceRegistryService.swift:284
When the Keychain is authoritatively absent, this treats every pre-witness `UserDefaults` value as belonging to the current phone. A backup created before this change contains exactly that state, so restoring it onto a new phone adopts the old phone's device ID. Both phones then share and overwrite the same `(user, device, tag)` binding slot. Absence of a witness is not reliable identity evidence; this path must fail closed or use a non-migrating migration signal rather than adopting the mirror.

[P1] [P1] Do not report success while an unknown-destination tombstone is parked
Packages/iOS/CmuxMobileShell/Sources/CmuxMobileShell/BackingUpPairedMacStore.swift:1167
For a team-less row without an upload/restore echo, this branch leaves the tombstone in the nil-team scope without sending it. The forget path nevertheless clears the hidden marker and returns success. On a later launch with a concrete selected team, that team's restore does not consult the nil-team pending set, so the undeleted backup can resurrect the supposedly forgotten computer. Keep the item hidden and surface a retryable failure until its destination is known, or make all destination restores suppress account-wide parked tombstones.

[P2] [P2] Skip the post-forget refresh when batched cleanup fails
Packages/iOS/CmuxMobileShell/Sources/CmuxMobileShell/MobileShellComposite+HiddenMacs.swift:207
`removeExactScopes` deliberately attempts every row and can delete some rows before throwing for another. This unconditional refresh then reloads the list even when `cleaned` is false; legacy-marker migration can see the successfully removed primary row as rowless and clear its hidden marker. The UI reports failure, but the hidden entry used to retry disappears while a failed sibling row with a revoked binding remains. Refresh only after complete success, or make the batch atomic/preserve a retry owner for partial failures.

overall: patch is incorrect (0.97)
The change improves credential coherence and backup routing, but it introduces two identity/forget correctness failures: restored pre-witness defaults can clone a physical-device identity, and unknown backup destinations are silently treated as successful deletion. Partial batched cleanup can also remove the user's retry path.
