merge-conflict gate: clean against origin/main
autoreview wrapper target: branch
cmux aziz prompt: enabled
swift review addendum: enabled
cmux policy: /Users/abdulazizalbahar/Dev/Manaflow/cmuxterm-hq/skills/autoreview/scripts/cmux-policy-check --mode branch --base origin/main
autoreview target: branch
branch: feat-iroh-ios-keychain-device-id
engine: codex
model: gpt-5.6-sol
thinking: high
tools: on
web_search: on
ref: origin/main
bundle: 205574 chars
autoreview findings: 2
[P1] Preserve the legacy device ID during the Keychain migration
Packages/iOS/CmuxMobileShell/Sources/CmuxMobileShell/DeviceRegistryService.swift:176
On the first launch after upgrading from the base version, this Keychain item is necessarily absent while UserDefaults contains the ID already used by the phone's existing iroh binding. This branch discards that ID, mints a new one, and overwrites the mirror, so an ordinary in-place upgrade changes the device identity, creates a second `(user, device, tag)` binding, and strands the old binding. The restored-backup collision concern needs a migration/provenance mechanism or explicit cleanup of the prior binding; treating every absent Keychain item as a new phone breaks all existing installations once.

[P1] Pin the macOS broker token source to the activating session
Sources/Mobile/MobileHostIrohRuntime+Activation.swift:97
This credential closure returns a coherent pair from whichever auth session is live, but never verifies that it still belongs to the `accountID` used to construct this runtime. During an A-to-B account switch, the existing runtime can issue another broker request before lifecycle reconciliation finishes; that request then combines account B's credentials with account A's endpoint/device state. Registration is authorized by the token, so it can create or refresh a binding under B and cache the result as A. Use an authenticated session snapshot and validate its generation/account on every request, as the new iOS broker source does, so the old runtime fails closed immediately when auth changes.

overall: patch is incorrect (0.97)
The change improves credential pairing and scoped deletion, but it introduces a one-time identity break for every existing iOS installation and leaves the changed macOS broker credential source vulnerable to cross-session credential confusion. Both affect correctness-critical device and account identity.
