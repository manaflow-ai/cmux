merge-conflict gate: clean against origin/main
autoreview wrapper target: branch
cmux aziz prompt: enabled
swift review addendum: enabled
cmux policy: /Users/abdulazizalbahar/Dev/Manaflow/cmuxterm-hq/skills/autoreview/scripts/cmux-policy-check --mode branch --base origin/main
autoreview target: branch
branch: feat-iroh-ios-keychain-device-id
engine: codex
model: gpt-5.6-sol
thinking: high
tools: on
web_search: on
ref: origin/main
bundle: 205279 chars
autoreview findings: 3
[P1] [P1] Key backup echoes by the row’s actual team scope
Packages/iOS/CmuxMobileShell/Sources/CmuxMobileShell/BackingUpPairedMacStore.swift:974
`team` is the request/display scope, not necessarily the matched row’s stored scope: `loadAll(teamID:)` deliberately includes legacy team-less rows. If such a row is updated while team A is selected, this saves the echoed destination under a team-A key, but a later exact forget looks it up with the row’s actual `teamID == nil`. The echo is missed, so the delete is merely parked and may never reach the server if that scope was already restored; other devices can then restore a computer the user forgot. Build the mapping key with `mac.teamID` instead.

[P2] [P2] Fence parked deletes against a concurrent re-pair
Packages/iOS/CmuxMobileShell/Sources/CmuxMobileShell/BackingUpPairedMacStore.swift:158
This snapshots the parked intents and then suspends while uploading their deletes. Because the actor is reentrant, an `upsert` can meanwhile clear the same parked intent and issue a revive. If this older delete request reaches the server after the revive, it tombstones the newly paired record, and the intent has already been cleared so nothing repairs the backup. Serialize backup mutations per pairing or use a generation/request-id reconciliation that prevents a stale delete from winning after revival.

[P2] [P2] Preserve successful scopes across a partial batch retry
Packages/iOS/CmuxMobileShell/Sources/CmuxMobileShell/MobileShellComposite+HiddenMacs.swift:302
`removeExactScopes` intentionally attempts every row and may throw after deleting some of them, but this catch returns before clearing any markers or recording which scopes succeeded. On retry, `loadAllInstances` cannot rediscover the already-deleted sibling scopes, so their per-team hidden markers are never cleared; if that Mac re-registers in one of those teams, it remains unexpectedly hidden. Return/persist per-scope results so completed rows’ markers can be cleared while failed scopes retain an explicit retry owner.

overall: patch is incorrect (0.96)
The change improves credential coherence and backup routing, but three edge cases still break the forget/re-pair contract: legacy team-less rows can store their routing echo under the wrong key, parked deletes can race a revival, and partial batch cleanup loses the information needed to clear successful siblings’ markers.
